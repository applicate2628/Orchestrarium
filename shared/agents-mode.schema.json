{
  "version": 1,
  "productionAutoProviders": ["codex", "claude"],
  "exampleOnlyProviders": ["gemini", "qwen"],
  "advisoryReviewSupplementalProviders": ["reserve"],
  "scalarKeys": [
    {
      "name": "consultantMode",
      "default": "disabled",
      "allowed": ["external", "internal", "disabled"]
    },
    {
      "name": "delegationMode",
      "default": "manual",
      "allowed": ["manual", "auto", "force"]
    },
    {
      "name": "parallelMode",
      "default": "auto",
      "allowed": ["manual", "auto", "force"]
    },
    {
      "name": "mcpMode",
      "default": "auto",
      "allowed": ["auto", "force"]
    },
    {
      "name": "preferExternalWorker",
      "default": "false",
      "allowed": ["false", "true"]
    },
    {
      "name": "preferExternalReviewer",
      "default": "false",
      "allowed": ["false", "true"]
    },
    {
      "name": "externalProvider",
      "default": "auto",
      "allowed": ["auto", "codex", "claude", "gemini", "qwen"]
    },
    {
      "name": "externalPriorityProfile",
      "default": "balanced",
      "allowed": ["balanced", "<repo-local production profile>"]
    },
    {
      "name": "reserveResolver",
      "default": "claude-sonnet",
      "allowed": ["disabled", "claude-sonnet", "claude-wrapper", "wrapper:<command>"]
    },
    {
      "name": "externalCodexWorkdirMode",
      "default": "neutral",
      "allowed": ["neutral", "project"]
    },
    {
      "name": "externalClaudeWorkdirMode",
      "default": "neutral",
      "allowed": ["neutral", "project"]
    },
    {
      "name": "externalModelMode",
      "default": "runtime-default",
      "allowed": ["runtime-default", "pinned-top-pro"]
    },
    {
      "name": "externalClaudeProfile",
      "default": "opus-max",
      "allowed": ["sonnet-high", "opus-max"],
      "providers": ["codex"]
    }
  ],
  "priorityProfiles": {
    "balanced": {
      "advisory.repo-understanding": ["claude", "codex", "reserve"],
      "advisory.design-adr": ["claude", "codex", "reserve"],
      "design.ui-ux-structure": ["codex", "claude"],
      "worker.reasoning-constraints": ["claude", "codex"],
      "worker.default-implementation": ["codex", "claude"],
      "worker.systems-performance-implementation": ["claude", "codex"],
      "worker.ui-implementation": ["claude", "codex"],
      "worker.visual-graphics-visualization": ["claude", "codex"],
      "review.pre-pr": ["claude", "codex", "reserve"],
      "review.security": ["claude", "codex", "reserve"],
      "review.performance-architecture": ["codex", "claude", "reserve"],
      "review.ui-visual-correctness": ["codex", "claude", "reserve"]
    }
  },
  "externalOpinionCounts": {
    "advisory.repo-understanding": 1,
    "advisory.design-adr": 1,
    "design.ui-ux-structure": 1,
    "worker.reasoning-constraints": 1,
    "worker.default-implementation": 1,
    "worker.systems-performance-implementation": 1,
    "worker.ui-implementation": 1,
    "worker.visual-graphics-visualization": 1,
    "review.pre-pr": 1,
    "review.security": 1,
    "review.performance-architecture": 1,
    "review.ui-visual-correctness": 1
  }
}
