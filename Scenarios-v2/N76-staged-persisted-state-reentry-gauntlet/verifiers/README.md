# Verifier

Run from the scenario root:

```powershell
python verifiers/check_persisted_state_replay_migration.py --bundle-shape-only
python verifiers/check_persisted_state_replay_migration.py --expect-start-state
```
