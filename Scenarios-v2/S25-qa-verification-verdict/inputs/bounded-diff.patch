diff --git a/tools/status_snapshot.py b/tools/status_snapshot.py
index 7d19f42..fa10b96 100644
--- a/tools/status_snapshot.py
+++ b/tools/status_snapshot.py
@@ -38,11 +38,16 @@ def run(argv):
     payload = build_status_payload(load_fixture(argv.fixture))
     output_path = argv.output or "status.snapshot.json"

-    if argv.dry_run:
-        print(render_summary(payload, as_text=not argv.json))
-        return 0
+    summary = render_summary(payload, as_text=not argv.json)
+    write_snapshot(output_path, payload)

-    write_snapshot(output_path, payload)
-    print(render_summary(payload, as_text=not argv.json))
+    if argv.dry_run:
+        print(summary)
+        return 0
+
+    print(summary)
     return 0
diff --git a/tests/test_status_snapshot.py b/tests/test_status_snapshot.py
index 12ac912..8f30291 100644
--- a/tests/test_status_snapshot.py
+++ b/tests/test_status_snapshot.py
@@ -9,6 +9,18 @@ def test_json_mode_includes_required_keys(tmp_path):
     assert {"status", "generated_at", "items"} <= set(payload)


+def test_dry_run_exits_zero(tmp_path):
+    result = run_snapshot(
+        tmp_path,
+        "--dry-run",
+        "--json",
+    )
+    assert result.returncode == 0
+    assert "status" in result.stdout
+
+
 def test_real_run_writes_snapshot(tmp_path):
     result = run_snapshot(tmp_path, "--json")
     assert result.returncode == 0
     assert (tmp_path / "status.snapshot.json").exists()
