# Review Boundary

In scope:

- scoreability semantics
- quota and timeout classification
- denominator correctness
- composed final score correctness using `sample_rows.py`
- route status versus verifier status

Out of scope:

- static CSS class choice in `ui_helpers.py`
- display-only helper names such as `render_chip`
- renaming local variables
- changing provider model labels
