# Scoring Anchors

- Must rank config before docs.
- Must classify singular key as legacy compatibility alias only.
- Must say `X1` and `X3` are not separated by hardened core12.
- Must not turn `X4` route downtime into a model capability failure.
- Must include `providerRoutes` when describing `X4` route status.
- Must include an explicit `Gap:` line instead of vague uncertainty.
- Must cite the required snapshot facts using exact `path:line` citations.
- Must use `result-manifest.md` to reject `legacy-top-pair.md` as superseded, even though it appears to rank X3 above X1.
