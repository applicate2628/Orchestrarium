# N44 Oracle

`interface-refactor-contract.json` is the source of truth for bundle shape, expected metadata,
hidden behavior cases, public source-id preservation, required migration ledger rows, immutable
visible-test hash, and patch budget.
