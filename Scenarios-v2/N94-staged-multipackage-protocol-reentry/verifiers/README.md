# N94 Verifier

Run:

```powershell
python Scenarios-v2/N94-staged-multipackage-protocol-reentry/verifiers/check_staged_multipackage_protocol_reentry.py --bundle-shape-only
```

The default verifier runs hidden protocol, SDK, plugin, CLI, public import, staged ledger, resume,
closeout, test-marker, and exact changed-path checks.
