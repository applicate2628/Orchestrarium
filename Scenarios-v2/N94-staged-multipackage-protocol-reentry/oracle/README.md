# N94 Oracle

The verifier owns hidden runtime checks for the protocol, SDK, plugin, CLI, ledgers, public exports,
fresh-session resume, staged closeout, and exact changed-path scope.
`staged-multipackage-protocol-contract.json` exposes the shape and scoring anchors without exposing
every hidden runtime assertion.
