# Phase 04 - Tests And Closeout

Allowed edits for this phase:

- `candidate/workspace/tests/console-contract.test.mjs`
- `candidate/workspace/implementation-ledger.json`
- `candidate/workspace/closure.json`

Update regression tests so they exercise:

- command-key ownership and disabled command skip behavior
- per-record dirty navigation guard
- validation, failed save, success save, discard, and focus return
- rendered accessibility cues
- compact and desktop layout geometry
- raster gap, selected/alert overlay order, legend order, and PPM metadata

Finish `implementation-ledger.json` with:

- `planFingerprint`: `n38-ui-visual-state-v1`
- phase entries for all four phase ids
- source ids `S1` through `S12`
- stale-source rejections from `inputs/stale-visual-advice.md`
- validation commands for the scenario verifier, scope verifier, and Node test file

Finish `closure.json` with exact changed paths, validation commands, outcome, residual risk, and
the same plan fingerprint.
