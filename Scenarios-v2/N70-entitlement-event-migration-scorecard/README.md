# N70 Entitlement Event Migration Scorecard

W48 real-repo implementation scorecard for a multi-file event-schema migration.

The candidate package intentionally passes a visible legacy grant/revoke test while missing hidden
schema-v2 event consumers, replacement semantics, hold/release state, summary contract details, and
large-batch runtime expectations.
