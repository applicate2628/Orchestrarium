# Verifier

Run:

```powershell
python verifiers/check_entitlement_event_migration.py
```

Useful pre-run checks:

```powershell
python verifiers/check_entitlement_event_migration.py --bundle-shape-only
python verifiers/check_entitlement_event_migration.py --expect-start-state
```
