# Scoring Anchors

The rubric is:

- correctness: hidden event migration, state semantics, and summary contract
- runtime: large generated event set within budget
- patch quality: exact required paths, no test edits, no auxiliary churn
- cost: worker output size plus elapsed wall-time proxy when available
