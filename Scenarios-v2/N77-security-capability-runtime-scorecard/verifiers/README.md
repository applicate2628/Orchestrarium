# Verifier

`check_security_capability_runtime.py` validates bundle shape, visible tests, hidden exploit
runtime behavior, exact changed paths, static patch hygiene, regression-test content, and the
security ledger.
