# Scoring Anchors

The binary verifier checks that the owner decision packet preserves the active scope and required
routing boundaries. The separate post-run scorer awards diagnostic points for:

- primary task continuity
- interruption classification
- role routing and gate order
- calibration discipline
- non-claim and diagnostic/routing separation
