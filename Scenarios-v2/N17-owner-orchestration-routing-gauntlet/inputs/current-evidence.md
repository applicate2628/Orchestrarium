# Current Evidence

| Surface | Current read |
|---|---|
| full-v2 baseline | `X3 40 / 40`, `X1 39 / 40`, treated as pre-v3 ceiling-effect baseline |
| tuple-exact review/factual hardening | `X1` and `X3` both pass; binary tie remains |
| `S22` geometry hardening | `X1` and `X3` both pass `23 / 23`; binary tie remains |
| `N14` multi-file implementation | `X1` and `X3` both pass; `X2` scoreably fails; Gemini rows runtime `NOT-RUN` |
| `N15` stateful implementation | `X1` and `X3` both pass all stateful invariants |
| `N16` long-horizon rubric | binary tie; diagnostic rubric favors `X3 95 / 100` over `X1 89 / 100` |
| `E3` source/rubric read | diagnostic rubric favors `X1 60 / 60` over `X3 59 / 60` |
| role-fit scorecard | `X3` primary for compact long-horizon integration; `X1` primary when self-added tests or trace-heavy reporting matter; near-tie elsewhere |
