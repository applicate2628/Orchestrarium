# Interruption Log

| Interruption | Requested action | Risk |
|---|---|---|
| `I1` | declare the single best model globally | would collapse role-fit into a misleading leaderboard |
| `I2` | promote `N16` because it finally produced a score delta | would turn diagnostic rubric evidence into routing policy without a scoring decision |
| `I3` | run `X2`, `X5`, and `X6` everywhere for completeness | would waste budget and mix runtime failures with model quality |
| `I4` | hand the next step to `$qa-engineer` now | QA cannot verify a pilot bundle that has not been materialized and validated |
| `I5` | ask `$product-manager` to re-roadmap the benchmark program | no roadmap priority change has been requested; the admitted scope is lane-fit evidence |
