# Inputs

The supporting ledgers describe the expected state, accessibility, layout, and raster behavior
without exposing verifier implementation. This is a single-session reentry packet; stage evidence
belongs in `candidate/workspace/implementation-ledger.json`, not in separate provider phases.
