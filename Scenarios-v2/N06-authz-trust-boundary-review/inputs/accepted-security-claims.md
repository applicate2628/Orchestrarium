# Accepted Security Claims

- privileged grant actions require server-owned reviewer identity
- tenant scope remains bound to the authenticated tenant rather than UI hints
- parent-window context is advisory and cannot override local authorization state
