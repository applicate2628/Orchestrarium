# Review Target

This subtree is the read-only implementation artifact under review.

It represents a bounded additive patch that materializes a routing-matrix summary for the review
lanes. The reviewer should inspect these files together with the accepted constraints in
`inputs/accepted-review-constraints.md`.
