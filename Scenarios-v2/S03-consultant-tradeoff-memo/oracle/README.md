# Oracle

The oracle material defines the preferred consultant direction for `S03`.

## Advisory truth

The strongest memo recommends `Option A - one local advisory pilot first`. The accepted packet is
complete enough to justify one small reversible step, but incomplete enough that the memo must stay
explicitly uncertain and non-blocking.

## Included oracle files

- `consultant-contract.json` provides machine-readable bundle and memo anchors for the verifier
- `admissible-directions.md` explains the preferred direction and why the alternatives lose points
- `anti-patterns.md` lists role drift and contract-breaking failures
- `scoring-anchors.md` turns the shared scoring model into `S03`-specific pass and fail signals
