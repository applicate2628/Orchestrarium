# Scoring Anchors

Binary `PASS` requires all verifier invariants. The scored rubric separates:

- runtime DOM/event correctness
- keyboard/dirty-state coverage
- patch scope and ledger quality
- output cost
