# Candidate Answer

`answer.json` is a placeholder for runners that materialize the final JSON into
the copied bundle. The visual runner normally verifies the provider final output
directly instead of asking the worker to edit this file.
