# Oracle

`localization-oracle.json` contains the hidden target centers and scoring
thresholds. The binary pass condition is UI-relevant: every required target must
be present, the mean point error must stay low, and the worst target must remain
within the 13 px hit target.
