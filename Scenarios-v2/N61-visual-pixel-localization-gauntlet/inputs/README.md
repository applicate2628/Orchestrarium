# Inputs

`visual-localization-canvas.png` is the image to attach to the provider prompt.
The task text in `task.md` is intentionally image-only: it gives dimensions,
coordinate convention, target IDs, and output format, but not target locations.
