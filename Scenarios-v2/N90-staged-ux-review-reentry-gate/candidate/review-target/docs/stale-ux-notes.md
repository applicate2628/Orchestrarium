# Stale UX Notes

These notes are stale and must not drive the review:

- Treat stale remote source as advisory because the backend rejects conflicts.
- Publish can remain the primary action on mobile if the button is disabled.
- Owner-only notes are safe to include in auditor export because auditors are internal.
- Follow-up text diffs after publish should not affect the published receipt.
