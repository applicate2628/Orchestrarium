# Review Target

The files in `ux_runtime/` model a publish console for a review workflow.

Do not patch these files. Produce staged review artifacts that identify UX
runtime, source binding, response-gate, and reentry issues.
