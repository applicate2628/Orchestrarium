# Scoring Anchors

PASS requires detecting the cross-phase cursor mismatch before QA, assigning `integration-owner`,
blocking QA, preserving repair/re-entry ordering, and closing with the exact changed-path list.
