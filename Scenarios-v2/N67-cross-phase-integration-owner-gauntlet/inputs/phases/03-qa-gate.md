# Phase 3: QA Gate

Fresh invocation. Edit only `candidate/qa-gate.json`.

Use the ledger and incompatibility report as prior state. Set a machine-readable gate that prevents
QA from running until the field contract is repaired. It must name the integration owner, required
repair, and re-entry steps.

Do not edit `closure.json` yet.
