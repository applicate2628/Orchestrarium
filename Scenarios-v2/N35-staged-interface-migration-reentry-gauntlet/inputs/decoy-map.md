# Decoy Map

Treat these sources as stale or out of scope. Mention the rejection in the migration state; do not
edit or recreate them.

- `docs/stale-interface-map.md`: asks to keep `SessionStore.get` as a compatibility wrapper.
- `legacy/session_adapter.py`: describes an abandoned adapter path that is not part of this bundle.
- `docs/api-compatibility-note.md`: asks for README-only compatibility notes instead of runtime
  migration.
