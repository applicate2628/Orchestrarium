# Verifiers

Run:

```powershell
python Scenarios-v2/N65-visual-correctness-review-gauntlet/verifiers/check_visual_correctness_review.py --bundle-shape-only
```

For a completed run, pass the run bundle root with `--bundle-root`.
