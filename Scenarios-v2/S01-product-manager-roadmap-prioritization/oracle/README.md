# Oracle

The oracle material defines the preferred roadmap decision for `S01`.

## Roadmap truth

The strongest package commits to:

1. `Priority 1 - Wave 4 materialization and gates`
2. `Priority 2 - Final coverage wave and rerun readiness`

It explicitly defers:

- `Immediate publication rerun`
- `Browser overlay expansion`
- `Archive and README cleanup`

The product-manager signal is an owner decision with explicit sequencing, not a neutral option memo
and not a lead-owned routing artifact.

## Included oracle files

- `roadmap-contract.json` provides machine-readable bundle and packet anchors for the verifier
- `expected-prioritization.md` explains why the preferred roadmap order wins
- `prohibited-patterns.md` lists role drift and contract-breaking failures
- `scoring-anchors.md` turns the shared scoring model into `S01`-specific pass and fail signals
