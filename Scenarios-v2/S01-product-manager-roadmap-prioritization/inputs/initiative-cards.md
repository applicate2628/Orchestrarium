# Initiative Cards

## 1. Wave 4 materialization and gates

- Scope: materialize `S01`, `S08`, `S13`, `S20`, `S24`, and `S31`, then run
  `integration -> QA -> architecture-review`.
- Product value: closes the missing roadmap-owner surface and adds distinct modality coverage
  across `P01..P06`.
- Why stakeholders care: it turns the accepted wave into on-disk benchmark evidence instead of
  leaving the roadmap visibly incomplete.
- Effort read: one full roadmap milestone.
- Risk if delayed: later publication still points at a known missing owner lane and a still-open
  mixed-modality gap.

## 2. Final coverage wave and rerun readiness

- Scope: materialize `S05`, `S14`, `S15`, `S18`, `S27`, `S28`, and `S30`, then prepare one clean
  rerun window after the wave clears its gates.
- Product value: closes the remaining semantic coverage gaps before spending publication effort.
- Why stakeholders care: it creates one coherent story for a later role-first public preview rather
  than a visibly partial one.
- Effort read: one full roadmap milestone after wave 4 passes.
- Risk if delayed: the benchmark remains knowingly incomplete and the single rerun window becomes
  harder to spend well.

## 3. Immediate publication rerun

- Scope: rerun the current provider cohort as soon as wave 4 passes and publish refreshed
  role-first tables immediately.
- Product value: fastest visible external signal.
- Why stakeholders care: it gives GTM a headline sooner.
- Effort read: most of one roadmap milestone plus the only clean June rerun window.
- Risk if chosen early: it hardens incomplete coverage into public comparison tables and spends the
  rerun budget before the final gaps are closed.

## 4. Browser overlay expansion

- Scope: add more browser-required overlays, screenshot fixtures, and runner work beyond the
  already intrinsic web lane.
- Product value: broader UI proof points.
- Why stakeholders care: it increases visual confidence for browser-heavy scenarios.
- Effort read: roughly half to three quarters of a roadmap milestone.
- Risk if chosen early: it increases runtime variance without closing the missing role-completeness
  gaps.

## 5. Archive and README cleanup

- Scope: pointer hygiene for benchmark docs and archive navigation only.
- Product value: lowers operator confusion.
- Why stakeholders care: it reduces avoidable navigation drift.
- Effort read: small carryalong item.
- Risk if chosen early: it uses attention that does not move launch-critical roadmap coverage.
