# Verifier

`check_visual_regression_diff.py` validates bundle shape, changed-path scope, JSON answer shape,
exact visual regression ids, calibrated coordinates, severity, non-findings, and false positives.
