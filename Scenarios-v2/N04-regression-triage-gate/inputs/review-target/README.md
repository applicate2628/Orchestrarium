# Review Target

This read-only snapshot contains the changed files referenced by the `N04` triage packet.

## Included files

- `src/status_snapshot_cli.py`
- `src/report_formatter.py`
- `src/alert_digest.py`

The candidate should cite these files but must not modify them.
