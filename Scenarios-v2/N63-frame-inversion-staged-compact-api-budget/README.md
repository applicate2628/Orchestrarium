# N63 Frame-Inversion Staged Compact API Budget Gauntlet

This diagnostic bundle tests whether the top pair can complete the compact BillingMesh-style API
migration requirements when the prompt is framed as an explicit staged migration.

This is a frame-inversion diagnostic. The visible task offers staged planning and closeout language,
but the verifier still scores the compact class requirements from N57: hidden repo consumers, exact
patch scope, source-bound migration ledgers, review-response decisions, generated-cache isolation,
and the visible `40000` byte operator-output budget.

The intended question is whether operator-budget and low-noise failures persist when the task is not
framed as a compact one-shot patch. A fail here is evidence for a real operator-output or compact API
migration weakness, not just a style mismatch with compact task wording.
