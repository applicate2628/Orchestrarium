# N62 Oracle

`staged-interface-contract.json` is the source of truth for bundle shape, expected metadata,
hidden behavior cases, staged migration ledger rows, review-response decisions, patch budget, and
frame-inversion scoring intent.
