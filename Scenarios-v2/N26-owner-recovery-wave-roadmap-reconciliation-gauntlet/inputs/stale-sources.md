# Stale And Decoy Sources

| Stale source | Trap |
|---|---|
| `ST1` | old full-v2 leaderboard says `X3 is the global winner`; this is stale and not admitted as a global routing policy |
| `ST2` | old rerun note says `X1 is the global winner`; old denominator rows are not the live routing model |
| `ST3` | old N16 summary says `N16 proves the global winner`; N16 is diagnostic E6 only |
| `ST4` | old N23 closeout says `N23 is hard owner policy`; current scorecard keeps owner recovery provisional pending N26 |
| `ST5` | broad calibration request says `Run X2, X5, and X6 on every scenario`; current policy keeps calibration bounded |
| `ST6` | N25 enthusiasm says `X5 is the global UI default`; current surface says X5 is a UI contender only |
| `ST7` | runtime shortcut says `runtime failures are model failures`; runtime failures stay separate from verifier failures |
| `ST8` | premature handoff says `$product-manager owns the next step` and `Send the output to QA now`; current N26 materialization stays with `$lead` until validation gates pass |

Every stale source must be rejected explicitly in the candidate packet.
