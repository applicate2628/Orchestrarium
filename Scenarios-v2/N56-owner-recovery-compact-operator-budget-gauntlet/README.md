# N56 Owner Recovery Compact Operator-Budget Gauntlet

This diagnostic probe repeats the N26 owner/orchestration recovery packet with the compact
operator-budget requirement made visible and scoreable. The candidate must reconcile current lane
state, reject stale winner and denominator claims, keep the primary hardening task alive, choose the
next owner and gates, keep calibration rows bounded, preserve spawn/result-file discipline, and
complete the run within the visible `worker-output <= 40000` byte budget.

The scenario is document-only. It is not a code patch and should not edit inputs, oracle,
verifiers, or protected decoy files.
