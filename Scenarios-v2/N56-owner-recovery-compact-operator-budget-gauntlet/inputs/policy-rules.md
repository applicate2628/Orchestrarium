# Policy Rules

- Maintain exactly one primary task: X1/X3 lane-fit hardening by waves.
- Source of truth is current live surfaces, not stale or archived score rows.
- Diagnostic rubrics may influence role-fit reads but do not become routing lanes by themselves.
- Do not merge N14..N55 into the old full-v2 denominator.
- Systems/toolchain, UI, visual graphics, long-horizon incident repair, and compact owner recovery are execution-shape-specific reads, not a global leaderboard.
- Compact owner recovery is X3 primary after N23/N26; staged owner recovery is X1 primary after N40.
- X5 is a UI contender after N25 and a single-session owner contender after N26, not a global default or promotion over X3 without another owner-family pass.
- `$lead` owns the next wave admission and materialization.
- `$qa-engineer` gates after N56 bundle/verifier/scorer/reference/operator-budget validation.
- `$architecture-reviewer` gates only after a routing-policy surface materially changes.
- Calibration rows are bounded by lane impact and route health; X2 and X6 run together after completed tasks, while X5 stays quota-deferred until route health returns.
- Runtime, quota, wrapper, missing-summary, and provider tool-loop failures are not model verifier failures.
- Spawns may produce read-only or disjoint sidecar proposals, but the main owner keeps the roadmap, live result surfaces, and final gate decisions.
- Update live surfaces in place; do not create stale parallel result files.
