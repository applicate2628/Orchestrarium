# Interruption Log

| Interruption | Event |
|---|---|
| `I1` | User questioned why earlier X1 score looked better after hardening; owner must clarify denominator/surface drift instead of publishing stale ranking. |
| `I2` | User asked where diagnostic E3 belongs; owner must keep it as rubric-only evidence, not a routing lane. |
| `I3` | User asked for commits; owner must preserve benchmark primary task state before switching or committing. |
| `I4` | User asked to continue in cycles until useful results; owner must keep the hardening wave loop active. |
| `I5` | User authorized spawns; owner may use read-only/disjoint spawns but main owner keeps roadmap and live surfaces. |
| `I6` | W4/N25 made X5 look strong in UI and W6/N26 made X5 strong in owner recovery; owner must mark X5 as contender only, not a cross-lane default or promotion over X3. |
| `I7` | N23/N26 and N40 split owner recovery by execution shape; owner must not collapse compact and staged owner packets into one global policy. |
| `I8` | The next roadmap candidate is N56; owner must materialize, validate, run, score, and update live surfaces instead of creating stale side files, and must treat the visible operator budget as scoreable. |
