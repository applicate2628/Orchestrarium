# Oracle

The oracle requires exact output structure, source-bound rows, command evidence, bounded action rows,
non-claims, and a product re-intake trigger. The verifier allows natural wording but checks required
terms in each row.
