# Verifier

Run from the bundle root:

```powershell
python verifiers/check_evidence_conflict_action_plan.py
```

Useful preflight modes:

```powershell
python verifiers/check_evidence_conflict_action_plan.py --bundle-shape-only
python verifiers/check_evidence_conflict_action_plan.py --expect-start-state
```
