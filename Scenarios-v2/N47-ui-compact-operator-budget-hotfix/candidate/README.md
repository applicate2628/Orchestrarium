# Candidate

Repair the bundle under `workspace/` only through the production implementation files in the
allowed change surface in `scenario.yaml`. The visible test file is protected immutable CI input.
