# Accepted Security Claims

- preview access tokens are short-lived and never persisted beyond the current page session
- parent-window messaging is origin-scoped
- UI traces do not expose bearer-token material
