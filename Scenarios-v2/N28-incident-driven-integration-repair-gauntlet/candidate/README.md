# Candidate

Patch `workspace/src/deploygrid/`, tests when useful, and `candidate/reconciliation-note.md` within
the allowed change surface. The public API and decoys are protected by the verifier and scope guard.
