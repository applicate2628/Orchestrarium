# Scoring Anchors

The binary verifier requires the completed candidate to pass all runtime integration invariants and
the source-bound reconciliation note checks. The separate post-run scorer computes diagnostic score,
elapsed proxy, output-size cost proxy, reconciliation quality, and patch-quality metrics from run
roots.
