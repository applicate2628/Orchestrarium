# Candidate Root

This directory is copied per execution. Only `transport-execution-report.md` is mutable for `S32`.

Do not add semantic implementation files here. The scored artifact is the adapter report only.
