# Oracle

This directory records the ground truth for the transport-only outcome in `S32`.

- `expected-routing-outcome.md` names the correct disabled-route result for this run
- `provenance-contract.json` is the machine-checkable report contract
- `prohibited-patterns.md` lists invalid fallback and scope-drift behaviors
- `scoring-anchors.md` keeps adapter scoring separate from semantic role scoring
