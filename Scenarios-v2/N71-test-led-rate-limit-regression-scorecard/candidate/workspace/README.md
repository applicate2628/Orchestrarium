# FlowLimit Workspace

Small fixed-window rate limiter used by N71.

Visible tests cover one user. Hidden consumers check cross-user isolation, precise retry-after, and
the required regression-test artifact.
