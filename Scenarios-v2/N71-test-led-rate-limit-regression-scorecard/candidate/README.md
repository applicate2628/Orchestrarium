# Candidate

Patch only the allowed production file, the required regression test, and `test-ledger.json`.

Do not edit visible tests, models, package exports, oracle files, verifier files, task text, or
metadata.
