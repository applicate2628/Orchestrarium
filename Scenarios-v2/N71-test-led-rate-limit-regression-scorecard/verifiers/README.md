# Verifier

Run:

```powershell
python verifiers/check_test_led_rate_limit.py
```

Useful pre-run checks:

```powershell
python verifiers/check_test_led_rate_limit.py --bundle-shape-only
python verifiers/check_test_led_rate_limit.py --expect-start-state
```
