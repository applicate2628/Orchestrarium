# Scoring Anchors

- Binary PASS requires both physics domains to pass analytical-oracle checks, convergence checks,
  runtime caps, report checks, and scope checks.
- Runtime is scoreable from measured solver wall time, not from prose quality.
- A slow correct solution can be a PASS but should lose role-fit rubric points.
- Runtime/quota/tool-wrapper failures are not model correctness failures.
