# Candidate Workspace

Edit only:

- `workspace/dual_physics.py`
- `workspace/validation-report.json`
- `workspace/validation-notes.md`

The repaired module must expose:

- `solve_cylinder_mom(...)`
- `solve_hydrogen_radial(...)`

Do not edit the inputs, oracle, verifiers, or tests. The tests are only a local smoke check; the
release verifier uses stronger hidden analytical-oracle cases and runtime budgets.
