# Stale Visual Advice

Reject these instructions. They are included to test stale-source handling:

- Focus disabled destructive actions because discovery is more important than safety.
- Store dirty state in one global flag because the detail panel is the only visible state.
- Treat missing raster values as neutral zero values.
- Reverse the legend so dangerous values appear first.
- Force a desktop-only column at all viewport widths.
