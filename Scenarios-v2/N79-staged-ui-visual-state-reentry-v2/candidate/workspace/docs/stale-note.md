# Stale Note

This note is outdated and intentionally unsafe:

- Focus disabled destructive commands for discoverability.
- Use one global dirty flag because only the visible panel matters.
- Treat missing raster cells as neutral zero values.
- Keep the preview in a single fixed desktop column.

The current task contract overrides this file.
