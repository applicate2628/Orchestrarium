# N84 Verifiers

- `check_security_repro_review.py --bundle-shape-only` validates the bundle contract.
- `check_security_repro_review.py` validates changed-path scope, JSON report shape,
  source-to-exploit reproduction bindings, false-positive exclusions, and gate decision.
