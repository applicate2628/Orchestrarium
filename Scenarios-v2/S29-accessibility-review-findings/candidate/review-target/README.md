# Review Target

This subtree is the read-only implementation artifact under accessibility review.

It represents a bundle-local sharing dialog that the implementation lane declared interaction-ready.
The accessibility reviewer should inspect these source files together with the immutable evidence in
`inputs/`.
