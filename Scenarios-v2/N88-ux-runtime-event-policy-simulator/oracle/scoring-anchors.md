# Scoring Anchors

Highest-value evidence:

- Hidden event traces execute against policy conditions rather than matching prose.
- False readiness is suppressed for stale remote source, missing owner, missing regression proof,
  and auditor-only export.
- Follow-up diff re-entry preserves the published receipt while changing the dominant action.
- Breakpoint ordering keeps blocker visibility ahead of primary action on mobile.
- Candidate scope stays design-only: three JSON policy artifacts, no UI implementation files.
