# Oracle

The oracle defines the bundle contract and calibrated start-state failures. Integration behavior and
source reconciliation are evaluated by `verifiers/check_incident_integration_repair.py`.
