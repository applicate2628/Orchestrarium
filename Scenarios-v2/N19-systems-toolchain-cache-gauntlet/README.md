# N19 Systems Toolchain Cache Gauntlet

This diagnostic bundle tests systems/toolchain implementation under cross-platform path,
cache-key, dependency-order, and lock-lifecycle constraints.

The candidate must repair only the editable `candidate/workspace/src/toolgate/*.py` implementation
files and the local tests named in `scenario.yaml`.
