# Boundary Notes

- `candidate/platform-owned/` is a protected deployment surface
- `candidate/data-owned/` is a protected reporting surface
- `candidate/api-owned/` is a protected request-contract surface
- `candidate/toolchain-owned/` is a protected packaging surface
- only the backend-owned module and direct test file may change
