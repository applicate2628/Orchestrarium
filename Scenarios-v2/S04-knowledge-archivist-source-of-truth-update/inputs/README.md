# Inputs

These files are the accepted packet for `S04`.

The packet is intentionally bounded:

- the canonical sources are already known
- the exact update targets are already admitted
- archive hygiene is in scope
- semantic governance or policy changes are out of scope for this packet

Use these inputs to write one source-of-truth update packet only.
