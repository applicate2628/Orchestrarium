# Oracle

The oracle material defines the expected reconciliation for `S04`.

## Stewardship truth

The strongest packet reconciles the exact three admitted target docs against the accepted canonical
source map, preserves archive history as history, and refuses to smuggle governance changes into a
documentation-maintenance task.

## Included oracle files

- `source-of-truth-contract.json` provides machine-readable bundle and packet anchors for the
  verifier
- `expected-reconciliation.md` explains the preferred reconciliation and target handling
- `prohibited-patterns.md` lists role drift and source-of-truth failures
- `scoring-anchors.md` turns the shared scoring model into `S04`-specific pass and fail signals
