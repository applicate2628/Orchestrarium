# Scoring Anchors

The diagnostic scorer should reward:

- real raster correctness from verifier pass
- scope discipline inside `candidate/visual-owned`
- source generalization rather than hardcoded full-frame output
- optional direct tests that cover the visual contract
- compact output and runtime cleanliness
