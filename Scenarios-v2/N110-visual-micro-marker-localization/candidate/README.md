# Candidate

Write the answer to `answer.json`.

Only `candidate/answer.json` is in the allowed change surface.

## Terms and Abbreviations

- `JSON`: JavaScript Object Notation; the machine-readable answer format.
