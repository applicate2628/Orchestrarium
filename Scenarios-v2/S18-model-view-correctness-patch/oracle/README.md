# Oracle

- `model-view-contract.json` defines the required S18 bundle shape and metadata
- `model-view-oracle.json` defines the expected projected states for each case
- `forbidden-widening.md` lists out-of-scope surfaces
- `scoring-anchors.md` explains what strong, middling, and weak S18 runs look like
