# Boundary Notes

- `candidate/qt-ui-owned/` is a protected widget surface
- `candidate/graphics-owned/` is a protected rendering surface
- `candidate/visualization-owned/` is a protected semantics surface
- `candidate/scorer-hooks/` is a protected scoring surface
- only the model-view-owned module and direct test file may change
