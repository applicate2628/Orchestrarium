# Inputs

- `task.md` defines the model-view-engineer ask and allowed change surface
- `model-view-brief.md` describes the view-projection seam the bundle owns
- `selection-cases.json` defines the row-projection cases the patch must satisfy
- `start-state-observations.md` captures the known failing behavior
- `boundary-notes.md` defines the protected neighboring roots
