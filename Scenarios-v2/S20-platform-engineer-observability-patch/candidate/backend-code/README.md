# Backend Code

This directory is read-only context for `S20`.

The application already exposes `/metrics` and does not require instrumentation changes for this
bundle. Editing backend code is scope drift.
