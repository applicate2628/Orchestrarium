# Shared Runners

This directory is read-only context for `S20`.

Shared execution wrappers are intentionally out of scope. The repair belongs in the platform-owned
config, not in shared runner glue.
