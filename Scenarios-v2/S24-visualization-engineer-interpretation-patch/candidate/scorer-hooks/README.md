# Scorer Hooks Decoy

This read-only decoy exists to make the boundary explicit: `S24` is not a scorer or metadata task.
Do not change benchmark contracts, scenario metadata, or scoring surfaces to compensate for a bad
visualization encoding.
