# Noisy Intake Notes

These notes are intentionally unverified and may conflict with the repo slice.

- A teammate thinks the collector still scans both `Scenarios-v1` and `Scenarios-v2` because an
  old archive index still exists.
- Another note says result publication probably still reads
  `benchmarks/publication/legacy_score_profiles.py`.
- Someone else points at `benchmarks/registry/role_matrix.yaml` and assumes it decides which bundle
  is selected for `R06`.
- One operator note says the live runtime path is "documented in
  `benchmarks/docs/legacy-routing-notes.md`."
- The same thread admits nobody checked the current tests after the migration freeze.

Use these as leads to verify or reject.
