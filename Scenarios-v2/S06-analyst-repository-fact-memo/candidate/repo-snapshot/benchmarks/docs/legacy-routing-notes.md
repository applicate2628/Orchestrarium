# Legacy Routing Notes

These notes were copied forward during the migration window and may no longer describe the live
collector path.

- Older runners scanned both `Scenarios-v1` and `Scenarios-v2`.
- The archived `scenario_index_v1.yaml` file used to help restore missing metadata rows.
- Publication once relied on a `legacy_score_profiles.py` shim during the transition.
