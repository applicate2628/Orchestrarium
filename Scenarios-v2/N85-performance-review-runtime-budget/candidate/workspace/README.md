# QuotePerf Mini-Repo

`quoteperf` is a small pricing subsystem with a deliberately slow but semantically correct `QuoteEngine` implementation. The task is to optimize the batch hot path while keeping the API stable.
