# Candidate Workspace

Patch only the allowed change surface in `scenario.yaml`. Preserve the `quoteperf` public API and
update the required state, ledger, and closure artifacts after the implementation. Keep the runner
transcript under the visible operator-output budget in `inputs/task.md`.
