# Oracle

`perf-cache-contract.json` defines bundle shape, scenario metadata, hidden semantic requirements,
runtime budget, and operator-output budget. Verifiers keep the exact generated workload private in
code while exposing the behavioral contract.
