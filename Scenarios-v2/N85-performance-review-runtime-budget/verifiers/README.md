# Verifiers

- `check_performance_cache.py` checks bundle shape, hidden pricing semantics, batch runtime, and evidence artifacts.
- `check_operator_budget.py` checks the visible worker-output byte budget from runner metadata.
- `check_scope.py` checks exact changed-path and patch-quality budget from runner-provided changed paths.
