# Candidate Workspace

Edit only the files listed in `inputs/task.md`.

The existing code intentionally passes the visible happy-path test while failing hidden exploit
checks and staged artifact gates.
