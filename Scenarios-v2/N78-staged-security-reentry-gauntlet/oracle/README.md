# Oracle

The oracle is runtime-first and staged: hidden exploit attempts must fail, legitimate requests must
pass, the patch must remain inside the allowed surface, and staged threat/validation/reentry/closeout
artifacts must stay coherent.
