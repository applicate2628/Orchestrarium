# N40 Scoring Anchors

The scorer awards source binding, stale-source rejection, lane routing, runtime classification,
phase-path discipline, exact scope, time, and output/cost points. Runtime failures stay separate
from scoreable semantic failures.
