# Oracle

`toolchain-staging-contract.json` defines bundle shape, start-state failure set, staged phase
path rules, implementation-ledger markers, closeout markers, and exact changed-path budget. The
verifier encodes the functional oracle.
