# Verifiers

- `check_stagegate_systems.py` validates bundle shape, start-state failures, completed behavior,
  implementation ledger, and closeout.
- `check_scope.py` validates exact cumulative changed paths for staged runs.
