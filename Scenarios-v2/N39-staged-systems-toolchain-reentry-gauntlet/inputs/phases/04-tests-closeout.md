# Phase 04 - Tests And Closeout

Allowed edits for this phase:

- `candidate/workspace/tests/test_stagegate.py`
- `candidate/workspace/implementation-ledger.json`
- `candidate/workspace/closure.json`

Update tests for path/config, fingerprint/dependencies, mode conflicts, lease release, cache
restore, and summary source trace.

Finish `implementation-ledger.json` with:

- `planFingerprint`: `n39-staged-toolchain-reentry-v1`
- phase entries for all four phase ids
- source ids `S1` through `S12`
- stale-source rejections from `inputs/stale-advice.md` and `candidate/workspace/docs/stale-staging-runbook.md`
- validation commands for the scenario verifier, scope verifier, and unit tests

Finish `closure.json` with exact changed paths, validation commands, outcome, residual risk, and
the same plan fingerprint.
