# N93 Oracle

The verifier owns hidden runtime checks for the protocol, SDK, plugin, CLI, ledgers, public exports,
and exact changed-path scope. `multipackage-protocol-contract.json` exposes the shape and scoring
anchors without exposing every hidden runtime assertion.
