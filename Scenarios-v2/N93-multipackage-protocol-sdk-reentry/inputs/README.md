# N93 Inputs

Read `task.md`, `source-conflict.md`, `downstream-sdk-contract.md`, `review-feedback.md`, and the
phase files before editing the candidate bundle. The verifier treats these files as protected
source material.
