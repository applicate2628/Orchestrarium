# N93 Verifier

Run:

```powershell
python Scenarios-v2/N93-multipackage-protocol-sdk-reentry/verifiers/check_multipackage_protocol_reentry.py --bundle-shape-only
```

The default verifier runs hidden protocol, SDK, plugin, CLI, public import, ledger, test-marker, and
exact changed-path checks.
