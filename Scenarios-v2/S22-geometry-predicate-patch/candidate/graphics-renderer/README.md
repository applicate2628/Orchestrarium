# Graphics Renderer

This directory is intentionally out of scope for `S22`.

The geometry predicate fix must stay inside `candidate/geometry-owned/` and direct tests. Changing
renderer behavior to mask predicate failures is a scope violation.
