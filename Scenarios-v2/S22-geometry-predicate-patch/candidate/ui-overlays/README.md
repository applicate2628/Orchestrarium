# UI Overlays

This directory is intentionally out of scope for `S22`.

The scenario is non-web and non-UI. No overlay, interaction, or rendering edits are permitted as
part of the geometry predicate patch.
