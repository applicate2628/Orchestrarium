# N43 UI Dirty State Immutable-Test Hotfix

This diagnostic bundle hardens the UI implementation signal from `N20` and `N25` with an
immutable visible-test constraint. The worker must repair editor state, rendering, and CSS behavior
without changing the fixed visible test baseline.
