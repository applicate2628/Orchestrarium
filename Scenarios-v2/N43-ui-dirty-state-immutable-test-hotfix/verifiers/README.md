# Verifiers

- `check_ui_dirty_state.py` validates bundle shape, start-state failures, and completed behavior.
- `check_scope.py` validates that changed paths stay inside `scenario.yaml` allowed surfaces.
