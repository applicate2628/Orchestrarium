# Stale Release Plan

This archived plan says `legacyProfile` should override `activeProfile` during freeze windows.

That is stale. The live owner is `src/releaseflow/config.py`, and `activeProfile` is authoritative.
