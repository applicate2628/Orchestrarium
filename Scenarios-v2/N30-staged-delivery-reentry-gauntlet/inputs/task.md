# Staged Task Overview

You are repairing the ReleaseFlow candidate in four separate worker sessions. Later sessions will not
receive earlier chat context. Persist the plan, decisions, review responses, and final closeout in
the candidate JSON artifacts.

Required plan fingerprint:

`n30-plan-active-profile-ledger-audit-v1`

Do not read or modify `oracle/` or `verifiers/`. Do not use oracle case IDs in candidate code.
