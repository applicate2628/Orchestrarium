# Stale Benchmark Note

Old small-sample benchmark, superseded:

> Option A was fastest in a ten-row local smoke run. Rounded-rank p95 was close enough, and any
> negative variance can be clamped to zero before display.

This note is intentionally stale. It must not override `inputs/cases.json`, `inputs/options.md`, or
`inputs/hard-constraints.md`.
