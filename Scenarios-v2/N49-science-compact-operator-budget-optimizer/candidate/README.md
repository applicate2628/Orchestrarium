# Candidate Workspace

The starter state is intentionally incomplete. Repair only the allowed files listed in `scenario.yaml`.

Use `candidate/workspace/tests/test_dual_physics.py` as a small smoke test, then rely on the verifier for the high-load analytical-oracle checks.
