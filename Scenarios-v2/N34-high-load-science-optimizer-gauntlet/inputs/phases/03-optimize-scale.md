# 03 Optimize Scale

Measure and record the high-load cases in `candidate/workspace/perf-ledger.json`.

The ledger must include all verifier case IDs, the total solver runtime, the EM segment counts, the hydrogenic grid sizes, and notes about the optimization route.
