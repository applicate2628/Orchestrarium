# Inputs

The input packet defines the staged incident repair task, stale requirements, review feedback,
phase prompts, and protected decoy surfaces. `task.md` is the operational contract; stale
requirements are traps only.
