# Phase 04: Final Closeout

Update only:

- `candidate/reentry-state.json`
- `candidate/closeout.json`

Write the final changed-path list, validation commands, outcome, resume point, and residual risk.
The closeout must name the staged batch boundary `N38,N39,N40,N41` and must distinguish scoreable
semantic failures from quota, wrapper timeout, route failure, and missing-summary runtime caveats.

Do not edit implementation files in this final phase.
