# Phase 03: Review Response And Budget

Update only:

- `candidate/repair-ledger.json`
- `candidate/reentry-state.json`
- `candidate/workspace/tests/test_deploygrid.py`

Record source decisions, `R1` through `R5` review responses, validation commands, and exact
`patchBudget`:

- `maxChangedPaths`: `6`
- `requiredChangedPaths`: the six paths listed in `inputs/task.md`

Reject stale UI, legacy-helper, and notification-count directions explicitly. Preserve the runtime
fix from phase 02.
