# Oracle

The oracle defines the bundle contract and calibrated start-state failures. Integration behavior,
source reconciliation, staged re-entry state, and closeout are evaluated by
`verifiers/check_staged_incident_budget.py`.
