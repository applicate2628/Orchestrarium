# N57 Oracle

`compact-api-contract.json` is the source of truth for bundle shape, expected metadata, hidden API
behavior cases, migration ledger rows, review-response decisions, exact patch budget, generated-cache
ignore rules, and visible operator-output budget.
