# N86 Oracle

`real-interface-downstream-contract.json` is the source of truth for bundle shape, expected
metadata, hidden API behavior cases, hidden downstream public-app behavior, migration ledger rows,
review-response decisions, exact changed-path budget, and generated-cache ignore rules.
