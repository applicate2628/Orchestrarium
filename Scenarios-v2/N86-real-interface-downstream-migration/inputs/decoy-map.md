# Decoy Map

Treat these sources as stale or out of scope. Mention the rejection in the migration state; do not
edit or recreate them.

- `docs/stale-billing-api.md`: asks to keep `get_account`, `check`, and `publish` as compatibility
  shims.
- `legacy/billing_adapter.py`: describes an abandoned adapter path that is not part of this bundle.
- `docs/reporting-compatibility-note.md`: asks for a README/reporting-only compatibility note
  instead of runtime migration.
