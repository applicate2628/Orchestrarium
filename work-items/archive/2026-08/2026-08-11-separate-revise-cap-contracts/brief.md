# Brief — separate REVISE cap contracts

The live tree uses the value `3` for a generic same-role/same-artifact correction limit and for autonomous review-loop rounds, while one Claude contract describes a third, per-stage quantity. The task is to remove that ambiguity, preserve the two real policies, and make prose duplication mechanically drift-gated.

The parked git-push p95 item, upstream Codex/Claude runtime constraints, and unrelated governance constants are outside scope.

## Terms and Abbreviations

- **Drift gate:** a test that fails when duplicated contract text disagrees with its owner.
