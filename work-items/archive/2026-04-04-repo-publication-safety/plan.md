# Phase Plan

Status note:
- Keep publication-safe summaries only.
- Do not include raw logs, transcripts, or machine-specific paths.

Selected direction:
- Add a repo-wide publication-safety reference.
- Add a root `.gitignore` with a local-only scratch boundary.
- Link the policy from repo docs and task-memory docs.
- Record the work in a dedicated tracked work item.

Checklist:
- [x] Inspect the existing repository guidance and safety gaps.
  Acceptance criteria: the remaining repo-wide safety gap is explicit.
- [x] Add the repo-wide safety policy and local scratch boundary.
  Acceptance criteria: tracked docs point to the same publication-safe contract.
- [x] Update task-memory docs and work-item index links.
  Acceptance criteria: recovery paths and ownership stay coherent.
- [x] Verify the repo-relative links and tracked-vs-local boundary.
  Acceptance criteria: the new policy is discoverable from the main docs.
