# Phase Plan

Status note:
- Claude second opinion selected the final shape for the four fixes: add `$planner` to the main index, hard-cap `REVISE` at 2, split publication gate into `lead` scan operation plus `knowledge-archivist` approval, and allow an additive fast lane only under strict local-change conditions.

Selected direction:
- Keep the patch docs-only and narrow.
- Update the main role map, lead contract, repository publication-safety policy, and operating-model references together.
- Treat the additive fast lane as a constrained shortcut for clearly local additive work, not as a replacement for the full route.

Checklist:
- [x] Inspect current state and confirm scope.
  Acceptance criteria: the source of truth and affected files are explicit.
- [x] Capture the selected direction and required files.
  Acceptance criteria: the plan is specific enough to execute without relying on chat memory.
- [x] Implement the scoped changes.
  Acceptance criteria: the planned files are updated and remain coherent.
- [x] Verify the documentation and links.
  Acceptance criteria: cross-references and mandatory artifact rules are consistent.
