# Canonical Brief

- Item: durable task memory and mandatory documentation governance
- Roadmap source: `roadmap.md`
- Goal: make repository work recoverable after interruption by storing the roadmap, brief, status, plan, and related notes in one canonical repo-local location
- Scope:
  - formalize `work-items/` as the canonical tracked task-memory root
  - add `active/`, `archive/`, `templates/`, and `index.md`
  - document the mandatory artifact set, owners, recovery rule, and enforcement rule
  - update repository source-of-truth docs to point at the new model
- Out of scope:
  - migrating all legacy `.plans/*.md` files into the new layout
  - adding new automation or Git hooks
  - changing role ownership beyond task-memory governance
- Constraints and assumptions:
  - keep accepted artifacts near the code
  - preserve existing `.plans/` history as legacy local material
  - keep the change documentation-first and narrowly scoped
- Accepted extension seams:
  - repo-wide reference docs in `references/`
  - repo entrypoint docs in `README.md` and `AGENTS.md`
  - lead guidance in `skills/lead/`
  - tracked work-memory store in `work-items/`
- Must-not-break surfaces:
  - the existing role map
  - current operating-model pipeline semantics
  - legacy `.plans/` history
- Critical risks and owners:
  - task-memory ambiguity: `$lead`
  - archive and structure consistency: `$knowledge-archivist`
  - documentation drift across source-of-truth docs: `$knowledge-archivist`
- Required roles and reviewers:
  - `$lead`
  - `$knowledge-archivist`
- Integration owner: `$lead`
- Current stage: Implementation
- Next stage: Verification
- Open blockers:
  - external Claude consultation hit a quota limit; proceed locally and record the failure in the plan artifact
