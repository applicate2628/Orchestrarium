# Plan

- Work item: `work-items/active/2026-04-12-control-plane-sync-cleanup`
- Role: `$planner`
- Planning basis: current `roadmap.md`, `brief.md`, `status.md`, the accepted brigade factual audit, and one locally verified likely truth-source drift in `codex/README.md`
- Goal: turn the widened deep truth-sync wave into small, independently reviewable documentation phases that remove split-brain operator guidance without redesigning the accepted preset, routing, or provider semantics
- Previously accepted foundation:
  - Phase A recovery hygiene is already accepted history
  - Phase B canonical operator/reference truth sync is already accepted history
  - Phase C prior standalone-reference truth sync is already accepted history

## New wave phases

| Phase | Goal | Write scope | Allowed change surface | Must-not-break surfaces | Dependencies | Acceptance criteria | Checks | Rollback / stop rule |
|---|---|---|---|---|---|---|---|---|
| D. Monorepo addendum operator-pointer sync | Bring the remaining monorepo Claude/Codex addenda up to the same operator-reference and routing-framing truth standard already used in the accepted canon | `Orchestrarium/references-claude/README.md`; `Orchestrarium/references-codex/README.md`; `Orchestrarium/references-claude/repository-task-memory.md`; `Orchestrarium/references-codex/repository-task-memory.md`; `Orchestrarium/references-claude/subagent-operating-model.md`; `Orchestrarium/references-claude/ru/subagent-operating-model.md` | Add only the missing `docs/agents-mode-reference.md` cross-link or pointer, and remove the stale Claude monorepo addendum wording that still describes the current runtime as `template-based routing`; keep the change scoped to truth-sync wording in the named sections only | shared design canon in `shared/references/`; current tracked-task-memory policy; accepted routing canon; reference-tree inventory wording outside the missing pointer; Claude addendum framing outside the stale routing sentence | accepted current canon only; no new design work | both monorepo READMEs now cross-link `docs/agents-mode-reference.md`; both monorepo task-memory docs include the same operator-reference pointer; the EN and RU Claude monorepo addendum files no longer describe the accepted current runtime with stale `template-based routing` framing; wording stays additive or corrective and does not introduce new governance semantics | targeted readback of the six edited sections; `rg -n "docs/agents-mode-reference|template-based routing" d:/dev/Orchestrator/Orchestrarium/references-claude/README.md d:/dev/Orchestrator/Orchestrarium/references-codex/README.md d:/dev/Orchestrator/Orchestrarium/references-claude/repository-task-memory.md d:/dev/Orchestrator/Orchestrarium/references-codex/repository-task-memory.md d:/dev/Orchestrator/Orchestrarium/references-claude/subagent-operating-model.md d:/dev/Orchestrator/Orchestrarium/references-claude/ru/subagent-operating-model.md`; `git -C d:/dev/Orchestrator/Orchestrarium diff --check` | if correcting the stale routing wording would require wider Claude runtime-design reinterpretation, stop and escalate rather than widening this phase |
| E. Standalone Claude diagram and README parity sync | Remove the remaining standalone Claude wording drift in the diagram and README inventory without widening into new translations or semantics | `claude/references-claude/operating-model-diagram.md`; `claude/references-claude/ru/operating-model-diagram.md`; `claude/references-claude/README.md` | Update only the direct-peer edge note, the workflow-selection delivery-loop row, and the README inventory or note needed to resolve the `evidence-based-answer-pipeline` parity gap; prefer the smallest truthful README-only patch over adding new translated content | existing standalone Claude addendum framing; accepted delivery-loop ownership; current RU file inventory outside the one parity note | none beyond accepted canon; independent of Phase D | the EN and RU direct-peer note explicitly mention approval of the edge, scope, and artifact boundary; the delivery-loop row ends with `-> lead` in both EN and RU; the README resolves the evidence-pipeline parity gap truthfully, with a bounded inventory note if the file remains English-only | targeted readback of the edited note, table row, and README inventory text; `rg -n "artifact boundary|Delivery loop|lead|evidence-based-answer-pipeline|English-only" d:/dev/Orchestrator/claude/references-claude/operating-model-diagram.md d:/dev/Orchestrator/claude/references-claude/ru/operating-model-diagram.md d:/dev/Orchestrator/claude/references-claude/README.md`; `git -C d:/dev/Orchestrator/claude diff --check` | if truthful parity would require a new RU translation or broader inventory redesign, stop after documenting that residual rather than expanding scope silently |
| F. Standalone Codex reference-tree truth sync | Remove the remaining hardcoded or stale Codex reference-tree claims so the standalone addenda match current configurable bug-registry and plan-log policy | `codex/references-codex/operating-model-diagram.md`; `codex/references-codex/repository-task-memory.md`; `codex/references-codex/ru/repository-task-memory.md` | Change only the adjacent-findings wording in the diagram and the stale `.plans/` wording in the EN/RU task-memory docs | Codex sequential-model note; configurable tracked-task-memory framing; dated `.plans/YYYY-MM/` plan-log policy; no redesign of task-memory semantics | none beyond accepted canon; independent of Phases D-E | the diagram no longer hardcodes `work-items/bugs/` as the adjacent-findings destination; the EN and RU task-memory docs no longer call `.plans/` legacy local history in a way that conflicts with the current dated plan-log policy | targeted readback of the changed lines; `rg -n "adjacent findings|work-items/bugs/|\\.plans|YYYY-MM|plan logs" d:/dev/Orchestrator/codex/references-codex/operating-model-diagram.md d:/dev/Orchestrator/codex/references-codex/repository-task-memory.md d:/dev/Orchestrator/codex/references-codex/ru/repository-task-memory.md`; `git -C d:/dev/Orchestrator/codex diff --check` | if fixing the stale claims would force a broader task-memory redesign, stop and escalate rather than rewriting surrounding policy text |
| G. Standalone Codex root README truth-source sync | Correct the standalone Codex README so it does not overstate `references-codex/` as the canonical operator source after the newer operator-mode reference/addendum split | `codex/README.md` | Update only the operating-model source-of-truth paragraph and nearby linking sentence(s) needed to align README wording with current `docs/agents-mode-reference.md` plus `references-codex/` addendum framing | current standalone Codex README structure; branch-local docs index wording; accepted operator-mode semantics | Phase F only to the extent the same truth-source wording should stay consistent; otherwise independently implementable | `codex/README.md` clearly points to `docs/agents-mode-reference.md` as the operator-mode truth surface and frames `references-codex/` as companion/addendum documentation rather than the sole canonical operator source | targeted readback of the operating-model paragraph; `rg -n "source of truth|docs/agents-mode-reference|subagent-operating-model|references-codex" d:/dev/Orchestrator/codex/README.md`; `git -C d:/dev/Orchestrator/codex diff --check` | if this README line cannot be corrected without broader README restructuring or semantic reinterpretation, park Phase G and escalate instead of smuggling redesign into the wave |
| H. Verification and recovery closeout | Leave one honest post-implementation recovery state for the widened wave and record the no-patch Gemini result explicitly | `Orchestrarium/work-items/active/2026-04-12-control-plane-sync-cleanup/status.md` only after phases D-G land | Verification summary, exact touched-scope summary, exact residual boundary, and explicit note that the Gemini standalone tree required no patch from the accepted factual audit | accepted item history; prior accepted closeout history; no unverified "done" language | successful implementation and verification of the earlier phases | `status.md` names the completed patch slices, the exact checks run in each repo, any intentionally parked residual drift, and the explicit no-patch Gemini outcome | readback of `status.md`; confirmation that all repo-local checks above were run fresh after edits | if verification surfaces semantic drift instead of wording drift, reopen planning or route to review instead of forcing closeout wording |

## Phase-admission notes

- The final monorepo Claude addendum drift is admitted as a narrow extension of Phase D, not a new standalone phase.
- Reason: both `Orchestrarium/references-claude/subagent-operating-model.md` files live in the same monorepo addendum truth-sync surface and need only a bounded wording correction away from stale `template-based routing` framing.
- Phase G is admitted into this widened wave.
- Reason: `codex/README.md` currently still presents `references-codex/subagent-operating-model.md` as the repository-wide operating-model source of truth even though the same README already points readers to `docs/agents-mode-reference.md` for operator-mode behavior. This is a bounded truth-sync correction in one file, not a redesign of accepted semantics.

## Guardrails

- Truth-sync only:
  - no redesign of preset names, preset meanings, provider universe, consultant semantics, or routing semantics
  - no new provider is introduced; `claude-api` remains a Claude transport, not a fourth provider
  - no Gemini standalone edits unless fresh verification contradicts the accepted no-patch audit result
- Change-surface discipline:
  - keep each phase inside its named repo-local write scope
  - if a phase needs new files, shared-structure changes, or broader README restructuring, stop and escalate instead of normalizing that expansion inside implementation
- Verification discipline:
  - run the listed readback and `rg` checks in the touched repo immediately after each phase
  - run `git -C <repo> diff --check` in every repo touched by that phase before moving on

## Recommended next role sequence

1. `$knowledge-archivist` implements Phase D in `Orchestrarium/`.
2. `$knowledge-archivist` implements Phase E in `claude/`.
3. `$knowledge-archivist` implements Phase F in `codex/references-codex/`.
4. `$knowledge-archivist` implements Phase G in `codex/README.md` only if it stays a wording-only truth-sync patch on re-read.
5. Main-session verification runs the repo-local checks listed above after each touched phase and again at the end for each changed repo.
6. `$lead` records Phase H in `status.md` after fresh verification and names any parked residual drift explicitly.

Gate decision: `PASS`
