# Status

- Template: full-delivery
- Orchestrator: `$lead`
- Change class: behavioral
- Current state: ready-for-post-commit-review
- Current stage: `Closeout`
- Last accepted artifact: deep brigade-driven truth-sync wave plus one bounded follow-on systems/perf routing extension that adds `worker.systems-performance-implementation` with Codex first across the canonical matrices, mirrored docs, and validation surfaces, all with fresh verification
- Next concrete action: review the committed changes and the installed global runtimes, then decide whether to push or publish; no push or publication step has been taken yet
- Primary task status: completed within the widened admitted scope, including the explicitly admitted Codex-first systems/perf lane extension, commit packaging, and global install refresh
- Active agents:
  - `$lead` main session
- Completed agents:
  - `$knowledge-archivist` task-memory completeness audit (`PASS`)
  - `$planner` bounded phase plan (`PASS`)
  - `$knowledge-archivist` Phases A-C stewardship patch (`PASS`)
  - `$planner` standalone-reference plan addendum (`PASS`)
  - `$knowledge-archivist` standalone-reference truth-sync patch (`PASS`)
  - brigade factual audits for monorepo, standalone Claude, standalone Codex, and standalone Gemini (`PASS`; Gemini required no patch)
  - `$planner` deep truth-sync wave replan plus bounded Phase D extension (`PASS`)
  - `$knowledge-archivist` Phase D monorepo addendum pointer and Claude-addendum wording sync (`PASS`)
  - `$knowledge-archivist` Phase E standalone Claude diagram and README parity sync (`PASS`)
  - `$knowledge-archivist` Phase F standalone Codex reference-tree truth sync (`PASS`)
  - `$knowledge-archivist` Phase G standalone Codex README truth-source sync (`PASS`)
  - `$lead` bounded systems/perf routing extension across canonical matrices, mirrored help surfaces, and validation sync (`PASS`)
- Open obligations before closeout:
  - human review before any commit, push, or publication
  - any future wider non-reference or newly discovered legacy-doc cleanup must be admitted as a separate wave instead of folded into this closed batch implicitly
- Notes:
  - The current dirty batch in `Orchestrarium` already contains accepted-direction control-plane changes from the ongoing preset and routing work; this item exists to reconcile and close that batch honestly.
  - Existing active items from 2026-04-11 remain separate and should not silently absorb this cleanup batch.
  - During closeout validation, `.gitignore` was found to contradict the documented tracked-task-memory contract by ignoring `/work-items/`; the ignore rule was removed as part of this item's recovery-hygiene fix.
  - The item was widened by direct human decision into a brigade-driven deep truth-sync wave, but remained a truth-sync task rather than a redesign of the accepted preset or routing canon.
  - After the truth-sync closeout, the item was widened one additional bounded step by direct human decision to encode the already accepted provider-role split for systems/perf implementation as an explicit lane rather than leaving that rule implicit.
  - The prior bounded closeout remains accepted history; this deeper wave started from that accepted state rather than re-litigating it.
  - Fresh final verification across the admitted reference surfaces returned clean for the searched stale patterns `original architecture`, `template-based routing`, `toggle file`, and `No toggle file = disabled`.
  - The accepted factual audit found no standalone Gemini patch was needed in this widened wave.
  - The new canonical lane is `worker.systems-performance-implementation`, with `codex > claude > gemini` in both shipped profiles and opinion count `1`.
  - Fresh verification after the follow-on extension passed `git diff --check` in `Orchestrarium`, `codex`, `claude`, and `gemini`, plus the monorepo and standalone validators for Codex, Claude, and Gemini.
  - The monorepo Claude validator initially failed on a stale fingerprint constant for `references-claude/subagent-operating-model.md`; the validator hash was synced to the already accepted addendum content and the rerun passed.
  - A final release-surface audit confirmed `README.md`, `INSTALL.md`, and the canonical operator references were already aligned; `RELEASE_NOTES.md` was then updated with a new `2026-04-12` section covering the preset-family canon, explicit systems/perf lane routing, and the synchronized install/reference contract.
  - The final release-readiness polish also synchronized standalone Codex, Claude, and Gemini entry docs with the current preset family and explicit systems/perf lane, and it widened `docs/provider-runtime-layouts.md` so the Gemini and root-router install surfaces are named among the authoritative inputs for runtime-layout audits.
  - Fresh post-polish verification passed `git diff --check` on the touched entry-doc files plus the monorepo and standalone validators for Codex, Claude, and Gemini.
  - A final pre-release pass confirmed the dirty batch is still intentionally unstaged and spans 26 tracked monorepo files plus the new tracked `work-items/` tree and `src.codex/agents/` payload. The publication-safety script returned clean on the current state, but that run is only advisory until a human-reviewed staged snapshot exists.
  - A release commit-boundary plan now exists at `.plans/2026-04/plan(lead)-2026-04-12_16-58_release-commit-boundaries.md`, splitting the monorepo into Commit A (routing/install/release-surface canon) and Commit B (tracked task-memory plus provider addendum truth-sync), with separate standalone mirror commit scopes recorded for `codex`, `claude`, and `gemini`.
  - Commit packaging is complete. Monorepo commits: `6a18ab0` (`Sync agents-mode routing canon and install surfaces`) and `34992e0` (`Track task memory and sync provider addenda`). Standalone mirror commits: `bfa7a1b` for `codex`, `d779d46` for `claude`, and `df4f553` for `gemini`.
  - Global install refresh completed from the monorepo source tree with `scripts/install-codex.ps1 -Global`, `scripts/install-claude.ps1 -Global`, and `scripts/install-gemini.ps1 -Global`. Verified targets: `~/.codex`, `~/.claude`, and `~/.gemini`.
  - During standalone Codex packaging, the PowerShell publication gate passed but the bash publication gate exposed a worktree-path false-positive / path-resolution quirk. The standalone Codex publication checker was synced to the staged-path-scanning logic already used in the monorepo before commit.
