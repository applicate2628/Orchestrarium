# Brief

- Primary task: update the external-routing control plane so Gemini can participate in broader advisory/review roles when one independent opinion is not enough, with switchable provider priorities in `agents-mode`
- Business or user goal: keep all three external providers usable under one common role principle while letting the operator steer priority order honestly from config
- Scope:
  - shared governance and `agents-mode` schema/docs
  - provider-priority resolution for external advisory, review, and worker lanes
  - multi-opinion external fan-out rules
  - pack-local mirrors in `Orchestrarium/main`, `codex`, `claude`, and `gemini`
- Out of scope:
  - unrelated install changes
  - commit packaging
  - global install/readback
- Constraints and assumptions:
  - keep the provider universe `auto | codex | claude | gemini`
  - keep `claude-api` a Claude transport, not a fourth provider
  - keep the common worker/reviewer/advisory role principle intact
  - treat this as a new control-plane batch, not an appendix to the prior symmetry rewrite
- Critical risks and owners:
  - routing-model coherence -> `$architect`
  - task-memory integrity -> `$knowledge-archivist`
  - mirror completeness and drift -> `$analyst`
  - final independent governance review -> `$architecture-reviewer`
- Current stage: `Closeout`
- Next stage: `Complete after commit packaging is explicitly requested or parked`
- Open obligations before closeout:
- commit packaging for the broader routing and governance batch
