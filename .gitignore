# Local repo-install output (created by install scripts, not committed)
/.agents/

# Legacy local working install (still kept local, not committed)
/.codex/
/.claude/

# Local-only scratch and legacy plan history.
/.scratch/
/.plans/
/work-items/

# Plugin/tool local state
/.serena/
