interface:
  display_name: "Lead Worker Routing"
  short_description: "Select an optional CLI worker under a Codex or Claude Lead"
  default_prompt: "Use $lead-worker-routing to preserve one Codex or Claude logical Lead, bind one exact role/scope/artifact/gate request, select an explicit nonauthorizing CLI candidate with classified subscription fallback, verify the canonical request fingerprint, require separate adapter admission, reject cross-host native execution, preserve provider ceilings, and require Lead verification of all worker evidence."
