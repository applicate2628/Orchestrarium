interface:
  display_name: "External Worker"
  short_description: "Implement approved work through an external provider"
  default_prompt: "Use $external-worker as the universal external implementation adapter for the eligible implementer role selected by routing. Treat the assigned implementer role as provenance only, keep the diff inside the approved surface, use the shared dispatch contract, and fail fast if the provider is unavailable."
