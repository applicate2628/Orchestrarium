interface:
  display_name: "Second Opinion"
  short_description: "Get an independent advisory memo or manage consultant toggle"
  default_prompt: "Use $second-opinion to get an independent second opinion via the consultant agent, or manage the consultant toggle (enable/disable/internal/status) while preserving consultantMode, externalClaudeApiMode, delegationMode, parallelMode, mcpMode, preferExternalWorker, preferExternalReviewer, externalProvider, externalPriorityProfile, externalPriorityProfiles, externalOpinionCounts, externalCodexWorkdirMode, externalClaudeWorkdirMode, externalGeminiWorkdirMode, externalModelMode, externalGeminiFallbackMode, and any externalClaudeProfile value in .agents/.agents-mode.yaml. Before reading or reporting flags, normalize any existing .agents/.agents-mode.yaml file to the current canonical format, including restoring inline comments and shipped profile/count blocks so `consultantMode` still marks `disabled` as the default allowed value, `externalClaudeApiMode` still marks `auto` as the default allowed value, and `parallelMode` still marks `auto` as the default allowed value."
