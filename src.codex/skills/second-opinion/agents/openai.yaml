interface:
  display_name: "Second Opinion"
  short_description: "Get an independent advisory memo or manage consultant toggle"
  default_prompt: "Use $second-opinion for an independent advisory memo via the consultant agent, or to manage the consultant toggle (enable/disable/internal/status). Preserve consultantMode, delegationMode, parallelMode, mcpMode, preferExternalWorker, preferExternalReviewer, externalProvider, externalPriorityProfile, reserveResolver, externalPriorityProfiles, externalOpinionCounts, externalCodexWorkdirMode, externalClaudeWorkdirMode, externalModelMode, externalCodexProfile, and externalClaudeProfile in .agents/.agents-mode.yaml. Normalize any existing mode file to the current canonical format before reading or reporting flags, restoring shipped comments and profile/count blocks so defaults remain `consultantMode=disabled`, `parallelMode=auto`, and `reserveResolver=claude-sonnet`. Keep production auto routing on `codex | claude`; Gemini and Qwen remain explicit `WEAK MODEL / NOT RECOMMENDED` example-only paths."
