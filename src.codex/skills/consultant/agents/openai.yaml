interface:
  display_name: "Consultant"
  short_description: "Optional independent advisory memo"
  default_prompt: "Use $consultant to provide one non-binding advisory memo for this task. Stay advisory-only, do not route work or gate progress, and preserve any existing preferExternalWorker/preferExternalReviewer fields in .agents/.consultant-mode when consulting the shared dispatch contract."
