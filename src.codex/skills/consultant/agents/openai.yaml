interface:
  display_name: "Consultant"
  short_description: "Optional independent advisory memo"
  default_prompt: "Use $consultant to provide one non-binding advisory memo. Stay advisory-only: do not route work or gate progress. Preserve all routing fields in .agents/.agents-mode.yaml and normalize it before trusting flags; keep defaults `consultantMode=disabled`, `parallelMode=auto`, and `reserveResolver=claude-sonnet`. Keep production auto routing on the Codex/Claude pair; Kimi is explicit-only Windows read-only bundle review through the canonical fixed `kimi-code/k3` no-tools/no-subagents wrapper with file-prompt delivery, ordinary launch does not consult enrollment, and the route remains independently verified and nonauthorizing; Grok remains unavailable in 1.x. End with a ready-to-send second prompt that begins with a direct imperative and names the next concrete action."
