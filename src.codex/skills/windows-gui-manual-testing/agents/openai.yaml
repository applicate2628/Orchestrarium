interface:
  display_name: "Windows GUI Manual Testing"
  short_description: "Windows desktop UI manual testing"
  default_prompt: "Use $windows-gui-manual-testing to verify a Windows desktop UI (Qt, Avalonia, WinUI/WPF, WebView/native-child, or native Windows) with screenshots, video frames, or live visual inspection, validate any crop against a full-window/close-button anchor before judging it, and return evidence-backed findings."
