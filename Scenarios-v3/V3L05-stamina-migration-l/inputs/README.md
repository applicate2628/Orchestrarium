# Inputs

- `task.md` -- the migration contract.

The workspace under `candidate/workspace/` contains 36 `quote_*` consumers
and 18 decoys scattered across the modules in no particular order. The full
consumer list is hidden (it lives in `oracle/`, not here).
