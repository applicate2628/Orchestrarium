Date: 2026-04-30
Source id: `SRC-RUNTIME-TRACE`
Authority: current runtime evidence

# Runtime Evidence

Recent replay trace `trace-planbridge-2026-04-30T09:12Z` observed these facts:

| Fact id | Observation |
|---|---|
| `RT-1` | `seat-sync` consumed `legacy_plan_id` in `100%` of sampled plan-change events |
| `RT-2` | `seat-sync` dropped a synthetic event that omitted `legacy_plan_id` |
| `RT-3` | `billing-ledger` rejected an event whose `idempotency_key` was regenerated by a consumer shim |
| `RT-4` | `support-snapshot` accepted `account_ref` but preserved the v1 event body in audit export |

Runtime conclusion:

- Dropping `legacy_plan_id` now is unsafe.
- Consumer-side schema shims are unsafe because they can break idempotency and audit replay.
- A compatibility adapter at the PlanBridge boundary can preserve producer-owned idempotency.
