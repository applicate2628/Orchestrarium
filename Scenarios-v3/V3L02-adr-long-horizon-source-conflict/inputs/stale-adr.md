Date: 2026-02-14
Source id: `SRC-STALE-ADR`
Authority: stale historical ADR

# ADR-021 Draft: Entitlement Bus Consolidation

This stale draft recommended replacing PlanBridge with a global entitlement bus and dropping
`legacy_plan_id` after `14 days`.

Known stale assumptions:

- It assumed `seat-sync` had completed the account-ref migration.
- It assumed idempotency keys could be regenerated by consumers.
- It did not include `support-snapshot`.
- It predates the current runtime trace and downstream tests.

Allowed use:

- Use this source only as historical context and as a rejected option input.
- Do not treat it as current authority over the 2026-04-30 code, runtime, or downstream-test sources.
