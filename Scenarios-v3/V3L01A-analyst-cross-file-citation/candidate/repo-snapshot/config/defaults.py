# Package defaults.
#
# These are the DECLARED defaults. They are not necessarily the values the batch
# pipeline effectively runs with - see config/effective.py for the overrides.
RETRY_LIMIT = 3
REQUEST_TIMEOUT_MS = 2000
ACTIVE_PROFILE = "interactive"
