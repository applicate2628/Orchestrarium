# Candidate Workspace

Edit only `analyst-citation-memo.md`. Investigate `repo-snapshot/` and cite each effective value to its
authoritative source (`config/effective.py`), rejecting the declared defaults and the legacy doc.

Do not edit anything under `repo-snapshot/**` (read only), `inputs/**`, `oracle/**`, or `verifiers/**`.
