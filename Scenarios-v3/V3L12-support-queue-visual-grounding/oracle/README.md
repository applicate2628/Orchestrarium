# Oracle

`visual-review-oracle.json` defines the seeded visual defects and false-positive traps for the
support-queue triage console scene. `answer-schema.json` is the strict JSON Schema the candidate
answer must validate against. `scoring-anchors.md` documents the scoring rationale and the
fixture-independence evidence pointer.
