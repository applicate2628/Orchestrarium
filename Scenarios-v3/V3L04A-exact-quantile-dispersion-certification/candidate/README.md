# Candidate Workspace

Edit only these two files:

- `quantile-dispersion-memo.md` - the certification memo (decision, conventions, rejected methods,
  case witness table, invariants, falsification plan, residual risk).
- `witness-ledger.json` - the machine-checked witness with the exact per-case numbers.

Do not edit `README.md`, `inputs/**`, `oracle/**`, or `verifiers/**`. The verifier re-derives every
case number from `inputs/streams.json`; there is no answer key to copy.
