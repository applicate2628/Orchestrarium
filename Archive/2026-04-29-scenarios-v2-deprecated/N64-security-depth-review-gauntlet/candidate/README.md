# Candidate Root

Edit only `review-report.md`.

`review-target/` is read-only and exists so the reviewer can cite exact files and observed
behavior. Do not patch, rename, or replace anything inside it.
