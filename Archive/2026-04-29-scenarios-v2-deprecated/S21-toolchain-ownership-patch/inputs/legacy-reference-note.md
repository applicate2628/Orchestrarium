# Legacy Reference Note

A legacy `T29` packaging experiment influenced the problem shape, but it is reference material
only. The repaired bundle must keep `S21` as the canonical identity and must not import legacy
runner or fixture paths into the editable toolchain contract.
