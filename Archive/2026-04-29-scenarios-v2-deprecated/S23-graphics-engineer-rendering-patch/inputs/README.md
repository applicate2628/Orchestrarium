# Inputs

The inputs define the immutable task packet for `S23`.

- `task.md` states the required output and the graphics-specific constraints
- `render-intent.md` defines the intended staged-rendering semantics
- `pipeline-boundary.md` spells out the owned seam and the forbidden widening surfaces
- `start-state-observations.md` records the expected broken start-state behaviors
