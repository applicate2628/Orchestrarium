# Geometry Kernel Decoy

This read-only decoy exists to make the boundary explicit: `S23` is not a geometry predicate or
transform patch. Do not move the fix into a geometry-owned seam or reopen `S22`.
