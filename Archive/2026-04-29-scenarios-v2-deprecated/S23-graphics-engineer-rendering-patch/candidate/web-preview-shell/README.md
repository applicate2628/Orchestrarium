# Web Preview Shell Decoy

This read-only decoy exists to make the boundary explicit: `S23` is not a browser UI, DOM, or CSS
task. Do not translate the renderer repair into a web implementation or reopen `S16`.
