# Verifiers

Run:

```powershell
python Scenarios-v2/N66-conflicting-evidence-fact-memo-gauntlet/verifiers/check_conflicting_evidence_fact_memo.py --bundle-shape-only
```

For a completed run, pass the run bundle root with `--bundle-root`.
