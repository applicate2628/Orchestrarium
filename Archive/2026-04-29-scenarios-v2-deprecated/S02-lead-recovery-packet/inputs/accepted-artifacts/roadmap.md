# Admission Source

Date: `2026-04-17`
Decision owner: direct human admission

## Active item

Bootstrap `Scenarios-v2/` by materializing exactly one real bundle root:
`Scenarios-v2/S02-lead-recovery-packet/`.

## Scope

- create the `S02` bundle root with the six required entries
- make the bundle a real `R02 $lead` recovery packet
- keep the diff inside `Scenarios-v2/`

## Out of scope

- any sibling `Snn` bundle root
- scoring updates
- taxonomy changes
- provider reruns

## Completion target

The new bundle root can stand as the convention-setting first example for packet-style v2 bundles.
