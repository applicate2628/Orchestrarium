# Candidate Root

`model-view-owned/` is the only editable implementation surface in this bundle. Everything else
under `candidate/` is a protected neighboring surface that exists only to fence scope.
