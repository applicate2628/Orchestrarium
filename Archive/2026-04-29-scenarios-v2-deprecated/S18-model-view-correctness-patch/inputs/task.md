Repair the model-view-owned projection helper so hidden rows are filtered, proxy ordering follows
priority, and stale selection falls back to the first visible row.

Edit only the module and direct test file listed in `scenario.yaml`. Do not widen into Qt UI,
graphics, visualization, or scorer roots.
