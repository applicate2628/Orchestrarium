# Oracle

The verifier checks the JSON state spec against required state, breakpoint, affordance, copy,
handoff, and non-goal tuples. It allows natural wording inside values but requires the expected
concepts to appear in the correct object groups.
