# Verifier

Run from the bundle root:

```powershell
python verifiers/check_ux_runtime_state_spec.py
```

Preflight modes:

```powershell
python verifiers/check_ux_runtime_state_spec.py --bundle-shape-only
python verifiers/check_ux_runtime_state_spec.py --expect-start-state
```
