# Reference Note

`S21` is an existing toolchain bundle and is not part of this data-engineer scenario.

Do not borrow its mutable files, validators, or package wiring as an editable repair path. The
only allowed change surface for `S19` is the local SQL transform inside this bundle.
