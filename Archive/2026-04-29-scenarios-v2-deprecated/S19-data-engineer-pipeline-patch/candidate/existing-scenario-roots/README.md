# Existing Scenario Roots

This directory is a read-only reminder that existing scenario roots stay protected.

`S19` must not widen into `Scenarios-v2/S16-*`, `S21-*`, `S22-*`, or any other existing bundle
root. The repair stays local to this new bundle only.
