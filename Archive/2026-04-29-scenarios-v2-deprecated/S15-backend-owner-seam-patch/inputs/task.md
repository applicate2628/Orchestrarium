Repair the backend-owned session-window helper so preview-share grants obey revocation, cutoff
expiry, and per-user deduplication rules.

Edit only the module and direct test file listed in `scenario.yaml`. Do not widen into platform,
data, API-contract, or toolchain roots.
