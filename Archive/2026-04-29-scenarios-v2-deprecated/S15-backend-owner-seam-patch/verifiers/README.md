# Verifiers

- `run_backend_checks.py --bundle-shape-only` validates the bundle contract
- `run_backend_checks.py --expect-start-state` validates the intended failing baseline
- `run_backend_checks.py` validates a completed candidate run
- `check_scope.py --changed-path ...` validates diff scope
