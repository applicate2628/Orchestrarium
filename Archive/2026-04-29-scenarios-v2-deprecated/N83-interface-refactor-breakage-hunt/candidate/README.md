# Candidate

Only edit files listed in `scenario.yaml`. The starter intentionally uses the old interfaces
and only has a shallow visible test. Hidden consumers include a batch API caller and a report
caller that passes structured result objects.
