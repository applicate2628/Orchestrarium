# Candidate

Repair the bundle under `workspace/` only through the production implementation files in the
allowed change surface in `scenario.yaml`. The visible tests are protected immutable CI input.
