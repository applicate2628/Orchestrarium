# Verifiers

- `check_ui_visual_state.py` validates bundle shape, hidden UI/state/layout/raster invariants,
  direct tests, ledger, and closure.
- `check_scope.py` enforces the exact cumulative changed-path budget for staged runs.
