# Candidate Workspace

Patch only the allowed change surface in `scenario.yaml`. Preserve the `quoteperf` public API and update the required state, ledger, and closure artifacts after the implementation.
