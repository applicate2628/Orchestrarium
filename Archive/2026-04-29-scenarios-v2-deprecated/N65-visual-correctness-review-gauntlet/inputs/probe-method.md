# Probe Method

Screenshot probes are deterministic observations from the candidate UI. Treat the probe IDs as
evidence labels, not as oracle answers. Bind each finding to the reviewed file and line that causes
or exposes the visual defect, then cite the relevant probe ID in the Evidence cell.

If a probe documents a harmless calibration detail, mention it only in `## False Positives Avoided`.
