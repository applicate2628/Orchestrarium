# N91 - Real-Repo Staged Security Incident Reentry

Diagnostic hardening bundle for implementation role-fit separation.

The candidate repairs a small export-service package under
`candidate/workspace/`. The incident combines tenant-bound authorization,
signed download capabilities, redirect hardening, audit redaction, and reentry
evidence.

The verifier checks staged ledgers, hidden exploit probes, visible tests,
protected file hashes, and exact changed-path scope.
