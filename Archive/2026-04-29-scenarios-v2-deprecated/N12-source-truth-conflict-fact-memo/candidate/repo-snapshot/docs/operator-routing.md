# Operator Routing

Current routing uses `externalPriorityProfiles` plural.

`X4` must be launched through the secret-backed Claude transport when enabled.

Runtime route status is not a lane profile and must not be counted as a model capability failure.
