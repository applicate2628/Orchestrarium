# Verifiers

Run from the bundle root:

- `python verifiers/check_multi_file_dependency_patch.py --bundle-shape-only`
- `python verifiers/check_multi_file_dependency_patch.py --expect-start-state`
- `python verifiers/check_multi_file_dependency_patch.py`
- `python verifiers/check_scope.py --changed-path <path>` for each changed candidate path
