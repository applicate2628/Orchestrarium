# Legacy Profile Notes

Older operator notes used the singular `externalPriorityProfile` key in examples. That key remains
accepted as compatibility input, but this document is stale and does not define runtime precedence.
