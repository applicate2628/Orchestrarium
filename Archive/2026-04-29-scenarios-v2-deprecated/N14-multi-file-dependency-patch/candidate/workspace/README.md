# Routing Eval Workspace

This package builds a compact benchmark routing report from:

- provider profile config
- per-row attempt metadata
- local verifier outcomes
- route/runtime caveats

Run local tests with:

```powershell
python -m unittest discover -s tests
```
