# Candidate

Patch the live workspace under `workspace/`. Read the top-level `README.md`, `scenario.yaml`, and
`inputs/task.md` before editing.

Only the files listed in `scenario.yaml` are mutable. The neighboring docs, legacy, UI, oracle, and
verifier files are decoys or protected benchmark infrastructure.
