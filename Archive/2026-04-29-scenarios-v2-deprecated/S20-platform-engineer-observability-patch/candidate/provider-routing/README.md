# Provider Routing

This directory is read-only context for `S20`.

Provider selection and routing behavior are protected surfaces. The observability patch must not
reroute providers or change transport defaults.
