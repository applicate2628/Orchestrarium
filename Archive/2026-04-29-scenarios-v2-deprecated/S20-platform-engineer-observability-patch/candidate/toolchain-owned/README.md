# Toolchain-Owned Surface

This directory is read-only context for `S20`.

CI packaging and toolchain graph ownership live here. The observability repair must not be solved by
editing build or packaging metadata.
