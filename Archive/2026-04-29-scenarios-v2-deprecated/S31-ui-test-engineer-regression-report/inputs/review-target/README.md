# Review Target Snapshot

These files are the read-only Qt dialog snapshot referenced by the UI evidence packet.

- `export_snapshot_dialog.py` contains the dialog focus behavior
- `export_snapshot_dialog.ui` contains the dialog layout constraints
- `export_snapshot_dialog.qss` contains the refresh-era theme styling

They are evidence only. The candidate output remains a report in `candidate/ui-regression-report.md`.
