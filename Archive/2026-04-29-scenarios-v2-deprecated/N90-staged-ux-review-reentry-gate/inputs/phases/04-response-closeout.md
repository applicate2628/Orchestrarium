# Phase 04 - Response Gate And Closeout

Create `candidate/response-gate.json` and `candidate/closure.json`.

Classify all author responses, preserve exact changed-path scope, list
validation commands, state residual risk, and include the reentry cue.
