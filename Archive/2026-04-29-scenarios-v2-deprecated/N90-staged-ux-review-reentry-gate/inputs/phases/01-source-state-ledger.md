# Phase 01 - Source State Ledger

Create `candidate/ux-review-state.json`.

It must bind all source IDs `S1` through `S12`, reject the stale notes, and
record each phase with an owner and visible return cue.
