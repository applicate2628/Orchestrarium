# Stale Requirements

This file is intentionally stale and must be rejected when it conflicts with the incident log and
review feedback.

- Stale claim: `legacyProfile` should override `activeProfile`.
- Stale claim: duplicate notifications can be hidden by changing UI badge labels.
- Stale claim: the quickest dedupe fix is to patch `candidate/workspace/legacy/dedupe_v1.py`.
- Stale claim: report totals can be counted from notifications because notifications are user-visible.

Do not implement these stale directions.
