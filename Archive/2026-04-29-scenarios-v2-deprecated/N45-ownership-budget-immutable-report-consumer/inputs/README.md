# Inputs

The input packet defines the incident repair task, stale requirements, review feedback, and
protected decoy surfaces. `task.md` is the operational contract; stale requirements are traps only.
