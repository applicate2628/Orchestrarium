# N20 UI Command Palette Interaction Gauntlet

This diagnostic bundle tests UI implementation through executable keyboard/state/render contracts.
It intentionally avoids screenshot fragility while still checking visible interaction semantics,
ARIA state, focus recovery, and stable CSS affordances.
