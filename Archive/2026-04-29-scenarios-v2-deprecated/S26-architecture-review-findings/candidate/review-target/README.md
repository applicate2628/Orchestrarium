# Review Target

This subtree is the read-only implementation artifact under review.

It represents a bounded additive patch that attempted to materialize a reusable review-bundle
template. The architecture-reviewer should inspect these files together with
`inputs/bounded-diff.patch` and the accepted design packet in `inputs/accepted-design.md`.
