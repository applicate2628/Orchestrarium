---
template: full-delivery
orchestrator: lead
started: 2026-04-13
updated: 2026-04-16 20:30
---

## Current state

- **Primary task**: keep the benchmark canon honest while hardening the UI methodology and the suspicious fallback wins
- **Primary task status**: the first baseline checkpoint remains fixed, the active `G08` lane has now been redesigned into a real non-browser fixture with a new rerun artifact instead of a paper-only policy note, and an intermediate operator checkpoint now freezes the narrowed working basis as `X1..X5`
- **Interruption marker**: none
- **Stage**: Hardening-wave and benchmark-method correction
- **Main conv role**: orchestrating benchmark execution and updating canonical task memory
- **Last accepted artifact**: `runs/g08-static-ui-rerun-2026-04-16.md`
- **Open obligations before closeout**: none mandatory; current remaining provider/runtime gaps on `X4`, `X5`, and `Q1` are now explicit operational blockers for the redesigned `G08` lane rather than hidden browser-test debt, and the latest user cross-machine check points at upstream Gemini responsiveness rather than local machine debt

## Completion ledger

### Done

| Area | Current state |
| --- | --- |
| historical model-only waves | `W1..W5` executed and preserved as provenance |
| frozen recommendation layer | final model-only freeze now exists in `matrices/model-matrix-freeze-2026-04-15.md` and `matrices/model-only-recommendation-freeze-2026-04-15.md` |
| Qwen extension planning | official Qwen Code capability note and experimental admission plan now exist without contaminating the frozen `X1..X6` package |
| Qwen local readiness smoke | local runtime now proves `qwen` is installed and `qwen -p` works on this machine; auth normalization is the remaining admission question |
| Qwen first scout slice | `M01 PASS`, `M08 PASS`, `M03 INVALID` twice; branch remains experimental and unranked |
| Qwen full slice | full `M01..M10` evidence now exists; the corrected API-key path clears the earlier advisory/review contract weakness, so the branch is now comparison-ready but still unplaced |
| Qwen wave overlays | experimental `W1..W5` syntheses now exist in `runs/`, so Qwen can be read in the same wave vocabulary as the frozen providers without pretending it already has a second fallback path |
| Qwen local Playwright setup | official MCP contract is now exercised locally: `qwen mcp add playwright ... --isolated` connects successfully and a browser-title smoke returns `QWEN_PW_SMOKE` |
| Qwen provider template | `Tooling/provider-mcp-templates/qwen-isolated-worker.ps1` now exists, so future Qwen reruns do not need ad hoc shell glue |
| Qwen wrapper hardening | the isolated-worker wrapper now survives missing optional settings fields and empty prompt argv, so current `Qwen` failures surface as provider quota errors instead of local PowerShell wrapper debt |
| Qwen API-key normalization | `Qwen` now runs on the current MuleRouter OpenAI-compatible path rather than the dead `qwen-oauth` free-tier path |
| Qwen strict browser catch-up | `Q1` now has an admitted strict `G08` browser pass on the API-key path |
| Qwen advisory/review catch-up | `Q1` now clears `M03`, `M04`, and `M05` on the API-key path; `M04` is admitted as a weak-but-valid planning row under the historical `M04` threshold |
| extension comparison embargo | the selected catch-up pack is now green, so comparison work is unlocked but still awaits explicit execution |
| Claude parity | `X3↔X4` parity evidence exists and `X4` is runnable through the repo-canonical secret-backed wrapper |
| fallback triage | historical `W4` and `W5` evidence exists for `X2`; `X6` now also has a corrected current non-browser fallback overlay through `runs/x6-wave-overlay-refresh-2026-04-16.md` |
| browser hidden fixture | current strict `G08` state is now recorded in `runs/g08-browser-parity-current-2026-04-15.md`; current `X3`, `X4`, `X1`, and `X2` pass, while current Gemini paths fail |
| role-gap fixtures | `G01..G07`, `G09`, and `G10` are fixture-ready; `G08` fixture is also present; `G11` now exists as a targeted worker path-discovery probe; `G12..G14` now exist as worker-trust probes with broken-state sanity and control-fixed validation |
| worker-trust probe pack | `runs/worker-trust-probe-pack-2026-04-15.md` now records the continuity/build, path-recall, and persistence probes and confirms each is both broken as intended and solvable on a control copy |
| first worker-trust execution batch | `runs/worker-trust-first-batch-2026-04-15.md` now records the first direct comparison cohort (`X4`, `X1`, `X3`) on `G12..G14`; all three rows stayed green on all three stronger bounded worker probes |
| worker-trust extension batch | `runs/worker-trust-extension-batch-2026-04-15.md` now records `X2` also staying green on `G12..G14`, while current restored-auth `Q1` remains externally blocked by `qwen-oauth` free-tier shutdown rather than benchmark behavior |
| messy worker-ownership batch | `runs/g15-messy-worker-ownership-synthesis-2026-04-15.md` now shows the first quality-separating messy worker probe: `X2` and `X3` stay clean, `X1` stays green with extra test-surface drift, and `X4` drops to `REVISE` because it reaches green only through brittle exact-path logic |
| Gemini worker-trust + hardening catch-up | admitted batch now exists in `runs/gemini-catchup-g11-g18-2026-04-16.md`; `X5` is green across `G11..G18`, while `X6` is green on `G12`, `G13`, `G16`, `G17`, `G18` and red on `G11`, `G14`, `G15` |
| runnable role-gap execution | admitted parallel-wave evidence now exists for `G01..G07`, `G09`, and `G10` in `runs/role-gap-parallel-wave-synthesis-2026-04-14.md` |
| Gemini repin | active canonical Gemini targets are now `gemini-3-pro-high-explicit` for `X5` and `gemini-3.1-flash-lite-preview` for `X6`; the old compact-table `gemini3-flash` label is deprecated |
| exact-target Gemini reruns | admitted rerun synthesis now exists for `X5/M01..M10`, and the active `X6/M01..M10` exact-target refresh is now closed under `gemini-3.1-flash-lite-preview` |
| corrected `X6` role-gap refresh | `runs/x6-corrected-fallback-refresh-2026-04-16.md` now closes the current `X6` non-browser rerun debt on `G01..G07`, `G09`, and `G10` |
| corrected `X6` wave overlay | `runs/x6-wave-overlay-refresh-2026-04-16.md` now regenerates the active `X6` reading for `W1..W5` without overwriting the historical wave files |
| hardening-wave execution canon | `runs/hardening-wave-execution-synthesis-2026-04-16.md` now canonizes `G16..G18` across `X1`, `X2`, `X3`, and `X4` |
| working provisional reconciliation layer | current matrix and routing package now exist in `matrices/model-matrix-checkpoint-2026-04-15.md` and `matrices/model-only-recommendation-package-2026-04-15.md` |
| full pairwise comparison surface | `matrices/full-model-comparison-matrix-2026-04-15.md` now gives the first full admitted pairwise matrix across `X1..X6` plus `Q1` |
| compact provider-priority alignment | `matrices/external-priority-profile-alignment-2026-04-15.md` now compresses the pairwise matrix into simple provider rank by lane family and translates it directly into `externalPriorityProfiles` |
| compact 7-model lane ranking | `matrices/model-rank-by-role-type-2026-04-15.md` now maps every configured external lane to a practical full-model order across `X1..X6` plus `Q1` |
| full agents-stack role spectrum | `matrices/full-agent-stack-role-spectrum-2026-04-15.md` now separates the coarse external lanes from fuller role-family placement and explicitly limits `X4` to bounded-worker use rather than broad worker ownership |
| first 7-model baseline checkpoint | `matrices/first-baseline-role-matrix-2026-04-15.md` now fixes the current practical role-family baseline and names the exact rows that should be stress-tested next rather than rerunning the whole pack blindly |
| benchmark package facade | `benchmarks/README.md`, `benchmarks/benchmark-results-index-2026-04-15.md`, `benchmarks/experiment-design-and-notation-2026-04-15.md`, and `benchmarks/hardening-wave-candidates-2026-04-15.md` now provide a single human-facing benchmark package entry point over the deeper matrices and run artifacts |
| combined provenance baseline | `benchmarks/combined-baseline-wave-gap-matrix-2026-04-16.md` now makes the compact baseline traceable by showing exactly how `W1..W5`, `G01..G10`, and the later worker-trust pack feed the final practical lane table |
| intermediate operator checkpoint | `benchmarks/intermediate-full-table-and-basis-x1-x5-2026-04-16.md` now fixes one compact current full table with `W1..W5` pulled directly into the same table, and defines one narrowed `X1..X5` basis working surface for continued work |
| default baseline view replacement | the combined provenance baseline now replaces the old compact matrix as the default human-facing baseline surface; the old `matrices/first-baseline-role-matrix-2026-04-15.md` remains only as a compact companion view |
| non-browser UI policy | `benchmarks/non-browser-ui-benchmark-policy-2026-04-16.md` now makes primary UI scoring non-browser by default; the active `G08` lane is now static UI evidence, while the old browser parity artifacts remain supplemental browser-runtime context only |
| redesigned active `G08` | `runs/g08-static-ui-rerun-2026-04-16.md` now records the real non-browser `G08` redesign and rerun: `X3 > X1 > X2` remains the top trio, `X6` is now admitted after a later sequential long-timebox rerun, `X4` remains parked by user instruction, `X5` still times out even after a sequential `900s` run, and the user's later clean-laptop check points to upstream Gemini latency rather than local machine debt; `Q1` remains blocked by expired `qwen-oauth` token |
| narrowed future-work basis | continued work should now use the `X1..X5` basis surface while `X4` and `X5` remain frozen on current admitted evidence and deferred from fresh fairness-sensitive tests because of current latency or availability drag |
| benchmark governance rules | `benchmarks/model-benchmark-governance-rules-2026-04-15.md` now defines the operating rules for this benchmark line: entry point, baseline checkpoints, change discipline, and hardening-wave expectations |
| explicit rerun / do-run backlog | `benchmarks/rerun-dorun-backlog-2026-04-16.md` now records the closure state of the benchmark cleanup queue: all mandatory reruns, do-runs, and raw-green canonizations are complete |
| first hardening-wave pack | `runs/hardening-wave-probe-pack-2026-04-15.md` plus fixtures `G16..G18` now exist with both broken-state and control-pass evidence, so the next wave can begin from a validated harder surface instead of from design notes only |
| real-project trust envelope | the same role-spectrum artifact now records the current operational boundary that only `gpt-5.4` and native `opus 4.6max` are broadly trusted for messy real-project execution, while all other rows stay bounded or fallback-only |
| bounded worker path-discovery comparison | `runs/g11-worker-path-discovery-synthesis-2026-04-15.md` now shows that `X1`, `X2`, `X3`, `X4`, and `Q1` all solved the new owner-discovery probe cleanly, `X6` solved it behaviorally but timed out, and `X5` failed outright |
| Gemini local overload note | user-reported local runtime evidence for **2026-04-15** says Gemini is also falling over in other projects today; treat same-day Gemini instability as an operational condition to revisit later rather than a reason to spam more reruns immediately |
| methodology correction | canon now records that the historical wave package is asymmetrical and not a full exact 6-target matrix |
| template and harness corrections | Claude no longer uses `--bare`; provider wrappers now support `-PromptFile`; provider-parallel runners use `pwsh`; Gemini no-MCP now prefers the workspace as `cwd` when safe |
| provider wrapper capture fix | `codex`, `claude`, and `gemini` isolated-worker wrappers now expose `-OutputFile`; clean reruns no longer depend on outer-shell relative capture |
| clean `X6` corrective reruns | clean wrapper-captured reruns still exist for `M02`, `M03`, `M04`, `M06`, `M07`, and `M10` under the earlier `gemini-3-flash-high-explicit` alias; they now remain historical provenance only because the active corrected refresh is `gemini-3.1-flash-lite-preview` |
| appended corrective ledger rows | canonical `run-R-*` records now exist for the clean `X6` set and the clean `X2/M02` rerun |
| clean `X3/M09` corrective rerun | canonical `run-R-*` record now exists for native Claude `M09` on the corrected non-`--bare` path |
| fresh `C1` rerun evidence | current reruns now reconfirm that `X6` still fails strict `G08` on `browser_navigate`, `X5` still times out on the strict lane, and `Q1` now passes strict `G08` on the API-key path |
| Qwen current catch-up state | `C1..C3` are now fully admitted; `Q1/M04` is reclassified as a weak-but-valid planning row under the same historical threshold used for admitted `X1` and `X5` `M04` rows |

### Deferred / out of current scope

| Priority | Remaining work | Why it is still open |
| --- | --- | --- |
| `1` | MCP-aware routing track | explicitly deferred outside the current model-only freeze |
| `2` | later Gemini Playwright remediation | optional future work if browser parity for Gemini becomes worth the extra cycle |
| `2a` | later Gemini browser / fairness retest only if desired | the deferred non-browser `G11..G18` catch-up is now done; only browser-specific or future fairness refreshes remain optional |
| `2b` | wider non-Gemini `G12..G14` comparison batch | `X2` is now done and green; `Q1` is currently externally blocked on restored auth, so only revisit this lane if a fresh API-key-backed Qwen path returns |
| `3` | later native-Claude `G08` reruns | optional future refresh only; the current strict rerun already refreshed `X3` successfully on 2026-04-15 |

## Active agents

| Agent | Role | Status | Launched |
| --- | --- | --- | --- |
| none | none | idle | — |

## Completed agents

| Agent | Role | Result | Artifact |
| --- | --- | --- | --- |
| provider or MCP surface memo | `$analyst` | PASS | `evidence/provider-mcp-surfaces.md`, `matrices/provider-mcp-capabilities.md` |
| benchmark-input memo | `$analyst` | PASS | `evidence/benchmark-inputs.md`, `matrices/role-provider-evaluation-dimensions.md` |
| benchmark-system design package | `$architect` | PASS | `design.md` |
| phased execution plan | `$planner` | PASS | `plan.md` |
| run-ledger contract | `$knowledge-archivist` | PASS | `runs/run-ledger-contract.md` |
| MCP capability impact map | `$analyst` | PASS | `matrices/mcp-capability-impact-map.md` |
| task-fixture catalog | `$planner` | PASS | `runs/task-fixture-catalog.md` |
| bounded `codex-host` Phase B baseline memo | `$lead` | PASS | `runs/codex-host-phase-b-baseline-2026-04-13.md` |
| bounded `claude-host` Phase B baseline memo | `$lead` | REVISE | `runs/claude-host-phase-b-baseline-2026-04-13.md` |
| bounded `gemini-host` Phase B baseline memo | `$lead` | REVISE | `runs/gemini-host-phase-b-baseline-2026-04-13.md` |
| bounded plain-Claude vs SECRET-backed Claude parity probe | `$lead` | PASS | `runs/claude-opus-vs-secret-transport-ab-2026-04-13.md` |
| model-only benchmark memo | `$planner` | PASS | adopted into `plan.md` |
| model-only scoring rubric memo | `$architect` | PASS | adopted into `runs/model-only-scoring-rubric.md` |
| task-memory completeness audit | `$knowledge-archivist` | REVISE | `.reports/2026-04/report(knowledge-archivist)-2026-04-14_15-22_task-memory-completeness-audit.md` |

## Next action

The next concrete benchmark move is now optional rather than cleanup-driven:

1. keep `benchmarks/rerun-dorun-backlog-2026-04-16.md` as the closure ledger for the finished mandatory queue
2. use `runs/x6-corrected-fallback-refresh-2026-04-16.md`, `runs/x6-wave-overlay-refresh-2026-04-16.md`, and `runs/hardening-wave-execution-synthesis-2026-04-16.md` as the new current evidence surfaces
3. if the benchmark continues, expand the hardening-wave specifically against the still-surprising rows where `gpt-spark` or `Claude China` outrank stronger incumbents
4. treat any future Gemini browser or wider `Q1` worker-trust expansion as optional deferred work, not as unfinished mandatory cleanup
