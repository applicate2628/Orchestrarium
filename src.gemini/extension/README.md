# Gemini Extension Scaffold

This directory is reserved for Gemini-native extension packaging.

Use it when the Gemini line needs:

- MCP servers
- Gemini-native tool registration
- extension-local runtime assets

The current `gemini-extension.json` is intentionally minimal and only establishes the manifest boundary.
