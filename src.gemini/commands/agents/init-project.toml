description = "Review or update the installed Orchestrarium Gemini example-routing overlay after the official Gemini /init step."
prompt = """
Read GEMINI.md and skills/init-project/SKILL.md.

Then guide the user through Gemini project bootstrap in this order:
- verify that GEMINI.md exists
- if GEMINI.md is missing, stop and tell the user to run Gemini's built-in /init first
- review or update the installed default .gemini/.agents-mode.yaml according to skills/init-project/SKILL.md
- keep the overlay aligned with the example-provider contract: production auto routing stays on codex/claude, while explicit Gemini or Qwen routes remain manual `WEAK MODEL / NOT RECOMMENDED` example-only paths
- keep .gemini/settings.json untouched because it remains the official Gemini runtime config
"""
