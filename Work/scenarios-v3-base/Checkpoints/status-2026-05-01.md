Date: 2026-05-01
Owner: `$knowledge-archivist`
Status: `RUN / BINARY TIE`

## Current State

| Topic | Current read |
|---|---|
| source basis | embedded pre-v3 RF12 line map; no raw pre-v3 release package copied forward |
| active v3 discovery | `Scenarios-v3/` |
| admitted v3 roots | `1` |
| registry | `Scenarios-v3/_registry/scenarios-v3-base.json` |
| mutable workspace | `Work/scenarios-v3-base/` |
| first target | `V3L02-adr-long-horizon-source-conflict` run completed |
| primary purpose | split `X1` vs `X3` on ordinary/source-ranked ADR work |
| excluded rows | `X5` and `X6` deprecated; `X4` final-only; `X2` closed calibration only |

## Next Concrete Action

`V3L02` did not separate the primary pair: `binary tie remains` after `X1 PASS` and `X3 PASS`.
Use `X2` only as cheap calibration unless explicitly requested for denominator work. `X4` is disabled
for the current cycle and should stay `NOT-RUN`.

## Pre-Run Evidence

| Check | Result |
|---|---|
| oracle JSON parse | `PASS` |
| registry JSON parse | `PASS` |
| bundle shape | `PASS` |
| reference probe | `PASS` |
| `git diff --check` | `PASS` |
| `mcp-free` | `PASS`; `STATS kill: none` |
| model rows | `X1 PASS`; `X3 PASS`; `X2 scoreable FAIL calibration`; `X4 NOT-RUN disabled` |

## V3L02 Run Result

| Row | Model | Result | Scoreability | Notes |
|---|---|---|---|---|
| `X1` | `gpt-5.5` | `PASS` | scoreable | changed only `candidate/adr-decision.json` and `candidate/adr-decision.md` |
| `X3` | `opus 4.7max` | `PASS` | scoreable | changed only `candidate/adr-decision.json` and `candidate/adr-decision.md` |
| `X2` | `gpt-5.3-codex-spark` | `FAIL` | scoreable calibration | no candidate changes; verifier reported missing decision, ledgers, and sections |
| `X4` | Claude China opus max | `NOT-RUN` | disabled | user parked this route |

## Terms and Abbreviations

- `ADR`: Architecture Decision Record.
- `RF12`: role-fit scorecard over twelve routing lines plus one owner/control line.
- `v3`: Scenarios-v3 benchmark generation.
