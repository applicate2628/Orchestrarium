# V4L09F Pre-Pull-Request Review

This frontier fixture exercises structured pre-pull-request review scoring for
L09. Candidates read the visible source cards, write only
`candidate/review.json`, and are scored by the deterministic v4 rubric adapter.

The fixture contains raw source cards, hidden proof artifacts, required source
bindings, rejected sources, safe surfaces, and a measured profiler value.
