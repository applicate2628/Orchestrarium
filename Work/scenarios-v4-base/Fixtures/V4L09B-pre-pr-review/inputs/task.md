# Pre-Pull-Request Review

Read `inputs/sources/source-cards.md` and write `candidate/review.json`.
Use `inputs/output-schema.json` as the required wire shape.

Base your answer only on the raw card material. Use the stable card IDs exactly
as printed when binding evidence or rejecting a card. Include reviewed paths,
the merge-clearance field, and all required schema fields.
