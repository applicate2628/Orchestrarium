# Candidate artifact

Write only `candidate/numeric-answer.json`. Follow `inputs/output-schema.json`: put `N*`, `W*`, and `T*` cells in `measurements`, put `I*` and `F*` cells in `cases`, and use the exact units in `inputs/sources/casebook.md`. The visible property key is the identity; do not add `item_id`.
