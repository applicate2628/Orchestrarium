# V4C03 Implementation Runtime

Calibration-only root for observable behavior, interface preservation, negative cases, changed-path
precision, and test evidence. It scores local target behavior only, never provider wall time.
