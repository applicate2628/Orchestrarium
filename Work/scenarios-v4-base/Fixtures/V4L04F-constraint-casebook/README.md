# V4L04F Constraint Casebook

Frontier L04 numeric/logical pilot. Read `inputs/task.md` and `inputs/sources/casebook.md`, validate the answer against `inputs/output-schema.json`, and write only `candidate/numeric-answer.json` as described in `candidate/README.md`.

The deterministic entry point is `verifiers/score.py`. Oracle material is not a candidate input.
