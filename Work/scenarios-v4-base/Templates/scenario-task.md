# Task

Produce only the artifact named in this root's visible output schema. Use the stable source and case
identifiers from the task. Put decision-bearing facts in typed fields; prose is explanatory only.

Runtime, quota, route, and wrapper faults are not candidate failures and are outside this scorer.
