active scope
- Restart `W1` on `X3`, `X1`, and `X5`, then retry the blocked `X4` secret-backed fallback path before doing anything else.
ignored stale context
- none
- none
next three actions
- Reopen the blocked `X4` fallback path from the stale `W1` note.
- Start MCP scoring now so the provider order is complete.
- Expand to fallback-only waves before finishing the accepted top-target path.
open risks
- Maybe the old `W1` note is stale.
- Pairwise guidance can wait until after fallback expansion.
