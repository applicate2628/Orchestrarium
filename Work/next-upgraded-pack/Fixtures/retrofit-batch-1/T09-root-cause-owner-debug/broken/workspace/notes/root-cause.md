owner seam
- `workspace/src/ui/mergeLaneVerdict.js`

failure mechanism
- The UI preview is noisy, so hiding the note in the preview should make the failure go away.

do not patch
- `workspace/src/providers/mergeLaneVerdict.js`
