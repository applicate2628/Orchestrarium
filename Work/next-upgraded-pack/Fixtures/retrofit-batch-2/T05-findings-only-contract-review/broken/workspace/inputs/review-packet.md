M05 findings-only review packet

Admitted contracts:
- installed Gemini runtime target is `gemini-3.1-pro-preview`
- provider priority stays separate from provider-local path notes
- `X3↔X4` changes Claude path guidance first, not provider order directly
- execute `W1` before fallback expansion

Diff under review:
- `plan.md` switches `X5` from `gemini-3.1-pro-preview` to `gemini-3.1-pro`
- `model-verdict-to-lane-priority-mapping.md` promotes native and fallback transport entries into provider order
- `status.md` says to execute `W4` first and treat `X4` as a separate provider entry
- `w1-m04-top-path-summary-2026-04-14.md` only softens wording from `more generic` to `a bit more generic`
