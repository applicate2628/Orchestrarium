M07 performance memo packet

Observed signals:
- `X4` is semantically near-parity to `X3`, but broader ambient surface and runtime variance keep it fallback-only
- `X4` `M01..M05` durations and costs rose across the wave
- accepted notes mention launcher, plugin-sync, and manifest-warning envelope noise
- `X5` showed `tools.totalCalls = 0`

Required focus:
- benchmark harness execution
- replay stability
- result comparability

Do not drift into:
- application-performance tuning
- MCP-value scoring
