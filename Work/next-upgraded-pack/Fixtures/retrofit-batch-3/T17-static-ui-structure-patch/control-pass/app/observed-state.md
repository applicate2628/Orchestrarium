# Control-pass snapshot

- The note panel now flows below the expanded row instead of anchoring to the matrix origin.
- Collapsed notes leave layout because the live stylesheet restores `display: none`.
- The decoy component stylesheets remain unused, which keeps the ownership seam explicit.
- The active chip styling still uses the expected highlight tokens.
