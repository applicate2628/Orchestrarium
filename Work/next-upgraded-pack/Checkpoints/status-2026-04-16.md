Date: 2026-04-22
Owner: `$lead`
Status: `PASS`

## Current state

| Topic | Current read |
|---|---|
| workspace | `Work/next-upgraded-pack/` |
| current stage | W22..W69 are complete, including W41 frame-inversion, W42 security-depth review, W43 visual-correctness review, W44 conflicting-evidence memo, W45 cross-phase integration-owner, W46 actual-screenshot visual review, W57 staged UI visual-state reentry, W58 calibrated screenshot grounding, W59 evidence-conflict action plan, W60 UX runtime state spec, W61 interface-refactor breakage hunt, W62 security-review reproduction, W63 performance runtime replacement, W65 real interface downstream migration, W66 performance-review gate, W67 UX runtime event-policy simulator, W68 security runtime witness review, W69 staged UX review/reentry, and E51/N61 visual pixel-localization diagnostics. Current lane-fit read: `X3 primary` for compact single-session long-horizon, release-train, ordinary systems/toolchain, real-repo performance hot paths, UI/visual-state code patches, visual graphics code patches, owner-recovery, cross-role, ownership-budget, compact real-repo API migration, compact low-noise science/runtime, and explicit low-noise/operator-budget implementation when cache/scope hygiene is controlled; `X1 primary` for staged delivery/re-entry, staged systems/toolchain recovery, staged API/interface migration, staged review/advisory, staged owner recovery, staged incident-budget re-entry, staged cross-phase integration-owner / QA-stop closure, staged UI/visual-state reentry, calibrated screenshot grounding, and exact patch-hygiene or complete public-API migration-surface interface work after `N30`, `N35`, `N36`, `N37`, `N39`, `N40`, `N41`, `N44`, `N67`, `N79`, `N80`, and `N86`; ordinary repo-understanding, UX structure, staged UX review, security review, visual-correctness review, performance-review gate, and hidden-consumer interface refactor remain `X1/X3 near-tie` after `N66/N81/N82/N88/N90/N64/N65/N83/N84/N89/N87` |
| last accepted artifact | `Results-drafts/role-fit-scorecard-v1-2026-04-22.md` records the current lane-fit routing read; `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` contains admitted sections through `N90`; `Evidence/n16-long-horizon-rubric-2026-04-22.json` through `Evidence/n90-staged-ux-review-rubric-2026-04-28.json` record machine-readable rubrics/diagnostics where applicable; `Evidence/separator-audit-2026-04-21.md` remains the 43-scenario factual audit |
| next concrete action | X6 remaining `NOT-RUN` cells require Gemini auth repair before retry. `X2` is fully closed at `12 / 40`. `X6` is partially filled at `13 / 40`. `W1..W69` are complete. `N46`, `N47`, `N48`, `N54`, `N55`, `N56`, `N57`, `N58`, `N74`, and `N85` are honest compact `X1 FAIL / X3 PASS` separators. `N67`, `N76`, `N78`, `N79`, `N80`, and `N86` are honest `X1 PASS / X3 FAIL` diagnostic separators on staged, calibrated visual-grounding, or exact interface-migration-surface axes. `N81`, `N82`, `N83`, `N84`, `N87`, `N88`, `N89`, and `N90` are top-pair `PASS/PASS` negative separator datapoints. `X4` is admitted on the canonical `full-v2-hard` surface at `32 / 40` before N85 final-only closure. Next action is selecting W70 from the roadmap candidate axes. |
| model-version note | active mutable `X3` now maps to `opus 4.7max`; the archive keeps historical `opus 4.6max` labels |
| execution-surface note | current `T01..T33` results remain valid as the legacy upgraded-pack checkpoint; core `Scenarios-v2` has a quota-aware `S01..S33 + N01..N07` read for `X1..X6`; `N08..N10` is a separate scoreable slice with `X4` deferred; `N11..N13` is a diagnostic top-pair tiebreaker, not a routing-lane replacement |
| active execution cohort | `X1`, `X2`, `X3`, `X4`, `X5`, `X6` |
| X4 full-v2-hard closing comparison | `X4 / Claude China opus max` is admitted on `full-v2-hard` at `32 / 40`: `32 PASS`, `8 scoreable FAIL`, `0 NOT-RUN`; failures are `N25`, `N35`, `N36`, `N37`, `N39`, `N40`, `N43`, and `N57` |
| X2/X6 full-v2-hard fill | `X2 / gpt-spark` is closed at `12 / 40`: `12 PASS`, `28 FAIL`, `0 NOT-RUN`. `X6 / gemini3.1flash-lite-preview` is partially filled at `13 / 40`: `13 PASS`, `19 FAIL`, `8 NOT-RUN`; remaining `NOT-RUN` slots are `N56`, `N57`, `N35`, `N36`, `N04`, `S27`, `N37`, and `S29`. Post-reset retry was blocked by Gemini `IneligibleTierError`, not verifier failure |
| frozen rows | none |
| exploratory retry rows | none |
| current caveat | provider quota, route failures, and wrapper timeouts are tracked separately from verifier failures; `X3/N39`, `X3/N40`, and `X3/N41` are scoreable fails because `wrapperExitCode=0` and verifier failed; `X3/N38` is not a fail because the original run and two reruns ended without final `summary.json`; `X5` is route/runtime unhealthy after wrapper and direct Gemini Pro probes timed out without explicit quota output; `X6` staged retries for `N35/N36/N37` first hit `TerminalQuotaError`, then post-reset normal retries hit `IneligibleTierError` on the current Gemini account; API-key route probe found no `GEMINI_API_KEY`, so those cells remain `NOT-RUN`; `X4` final closing run is admitted on the repo-canonical secret-backed Claude route; codex-backed background launches still need `cmd /c "pwsh ... < NUL"` per single scenario |

## Accepted planning surfaces

| Surface | Path |
|---|---|
| phase brief | `Planning/next-phase/phase-brief-2026-04-16.md` |
| role-complete redesign brief | `Planning/next-phase/phase-brief-role-complete-redesign-2026-04-17.md` |
| taxonomy and naming | `Planning/next-phase/benchmark-taxonomy-and-naming-2026-04-16.md` |
| role coverage matrix | `Planning/next-phase/role-coverage-matrix-2026-04-17.md` |
| benchmark architecture v2 | `Planning/next-phase/benchmark-architecture-v2-2026-04-17.md` |
| scenario backlog v1 | `Planning/next-phase/scenario-backlog-v1-2026-04-17.md` |
| scoring and results model | `Planning/next-phase/scoring-and-results-model-2026-04-17.md` |
| pack specs v1 | `Planning/next-phase/pack-specs-v1-2026-04-17.md` |
| scenario materialization plan v1 | `Planning/next-phase/scenario-materialization-plan-v1-2026-04-17.md` |
| scenario materialization plan wave 2 | `Planning/next-phase/scenario-materialization-plan-wave-2-2026-04-17.md` |
| scenario materialization plan wave 3 | `Planning/next-phase/scenario-materialization-plan-wave-3-2026-04-17.md` |
| scenario materialization plan wave 4 | `Planning/next-phase/scenario-materialization-plan-wave-4-2026-04-17.md` |
| scenario materialization plan wave 5 | `Planning/next-phase/scenario-materialization-plan-wave-5-2026-04-17.md` |
| migration inventory | `Planning/next-phase/migration-inventory-and-hardening-status-2026-04-16.md` |
| retrofit batch spec | `Planning/next-phase/retrofit-batch-spec-2026-04-16.md` |
| upgraded probe specs | `Planning/next-phase/probe-specs-T29-T30-2026-04-16.md` |
| role-line model | `Planning/next-phase/role-line-unification-2026-04-16.md` |
| core execution pack | `Planning/next-phase/core-execution-pack-2026-04-17.md` |
| hardening program | `Planning/next-phase/test-hardening-program-2026-04-16.md` |
| execution roadmap | `Planning/next-phase/roadmap-2026-04-16.md` |
| hardening wave roadmap | `Planning/next-phase/hardening-wave-roadmap-2026-04-22.md` |
| execution plan | `Planning/next-phase/execution-plan-2026-04-16.md` |
| active fixture surfaces | `Fixtures/retrofit-batch-1/`, `Fixtures/retrofit-batch-2/`, `Fixtures/retrofit-batch-3/`, `Fixtures/T29-toolchain-false-root-ambiguity/`, `Fixtures/T30-static-ui-wrong-file-attraction/` |
| active v2 scenario roots | all role-complete `S01..S33` plus routing-basis `N01-static-visual-hierarchy-brief/`, `N02-interaction-state-flow-brief/`, `N03-generic-code-review-findings/`, `N04-regression-triage-gate/`, `N05-secret-exposure-review/`, `N06-authz-trust-boundary-review/`, and `N07-scalability-maintainability-cross-review/` are materialized under `Scenarios-v2/` |
| active reference extra-lane roots | `N08-autonomous-build-owner-continuity/`, `N09-autonomous-resume-path-recall/`, and `N10-constrained-multi-step-patch-no-drift/` are materialized under `Scenarios-v2/` as `worker.long-autonomous` extra-lane evidence |
| active diagnostic overlay roots | `N11-architecture-evidence-conflict-adr/`, `N12-source-truth-conflict-fact-memo/`, and `N13-adversarial-review-false-positive-traps/` are materialized under `Scenarios-v2/` as `top-pair-separator` evidence; `N14-multi-file-dependency-patch/` through `N61-visual-pixel-localization-gauntlet/` remain the admitted diagnostic chain through `E51`; `N62-frame-inversion-compact-staged-interface/` and `N63-frame-inversion-staged-compact-api-budget/` are W41 frame-inversion diagnostics; `N64-security-depth-review-gauntlet/` is W42 security-depth diagnostic and ties X1/X3; `N65-visual-correctness-review-gauntlet/` is W43 visual-correctness diagnostic and ties X1/X3; `N66-conflicting-evidence-fact-memo-gauntlet/` is W44 repo-understanding diagnostic and ties X1/X3 while X2/X6 fail scoreably; `N67-cross-phase-integration-owner-gauntlet/` is W45 staged integration-owner diagnostic and separates X1 over X3 scoreably; `N68` through `N83` are W46-W61 diagnostics for screenshot grounding, patch quality, entitlement migration, test-led regression, caller-spanning API refactor, DOM runtime UI, persisted-state, security, staged UI, calibrated screenshot grounding, evidence-conflict action planning, UX runtime-state design, and interface-refactor breakage |
| latest upgraded-pack checkpoint evidence | `Evidence/x1-x3-extended-batch-2026-04-17.md` |
| latest v2 materialization evidence | `Evidence/fifth-wave-scenario-materialization-architecture-review-2026-04-17.md` |
| latest v2 execution evidence | `Evidence/x1-x2-x3-x4-x5-x6-full-v2-s01-s33-n01-n07-2026-04-18.md`, `Evidence/x1-x2-x3-x5-x6-v2-n08-n10-worker-long-autonomous-2026-04-20.md`, `Evidence/x1-x3-x5-v2-n08-n10-worker-long-autonomous-hardened-2026-04-20.md`, `Evidence/x1-x3-x5-core12-tie-hardened-2026-04-20.md`, `Evidence/x1-x3-top-pair-separators-n11-n13-2026-04-20.md`, `Evidence/x1-x3-top-pair-rubric-e3-2026-04-20.md`, `Evidence/x1-control-plane-override-rerun-s16-s19-s20-2026-04-20.md`, `Evidence/x5-core12-timeout-closure-2026-04-20.md`, `Evidence/separator-audit-2026-04-21.md`, `Evidence/n16-long-horizon-rubric-2026-04-22.json` through `Evidence/n87-performance-review-gate-rubric-2026-04-28.json`, `Evidence/x4-full-v2-hard-2026-04-26.json`, `Evidence/x2-x6-fill-full-v2-hard-2026-04-26.json`, and `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| latest v2 result surface | `Results-drafts/full-v2-hard-results-current.md` is the current canonical hardened `/40` surface; `Results-drafts/v2-full-s01-s33-n01-n07-results-2026-04-18.md` is retained only as a pre-v3 ceiling-effect baseline. Targeted source reads remain `Results-drafts/v2-core12-tie-hardened-results-2026-04-20.md`, `Results-drafts/v2-top-pair-separators-n11-n13-results-2026-04-20.md`, `Results-drafts/v2-top-pair-rubric-e3-results-2026-04-20.md`, and `Results-drafts/role-fit-scorecard-v1-2026-04-22.md` |
| latest compact short table | `Results-drafts/short-results-current-2026-04-18.md` |

## Open obligations

| Obligation | State |
|---|---|
| fixture build-out in `Fixtures/` | the old upgraded-pack registry `T01..T33` remains runnable, but it is no longer the target architecture for future benchmark publication |
| v2 scenario bundle roots | all `S01..S33`, `N01..N07`, reference extra-lane `N08..N10`, and diagnostic overlay `N11..N13` are real and gate-backed |
| `worker.long-autonomous` extra lane | baseline: `X1`, `X3`, and `X5` passed `3/3`; tightened tiebreaker: `X1`, `X3`, and `X5` all pass `3/3`; `X4` remains deferred |
| hardened core-12 weak separator | `X1` and `X3` tie at `15/15`; `X5` separates lower with `12` passes, `3` fails, and `0` runtime timeouts |
| diagnostic top-pair separator | fresh hardened slice `N02`, `S30`, `N11`, `N12`, `N13` ties `X1` and `X3` at `5 / 5`; this overlay does not rank the top pair by binary gate |
| diagnostic top-pair rubric | fresh E3 over the 2026-04-21 `N11..N13` artifacts reads `X1 60 / 60`, `X3 59 / 60`; this is a one-point diagnostic delta, not a binary separator |
| wave-4 S22 functional pilot | `X1` and `X3` both pass the hardened `S22` adversarial geometry oracle (`23 / 23` truth cases each); `binary tie remains`; `X4` and `X5` preserved as `NOT-RUN` |
| Option (c) N14 implementation pilot | `X1` and `X3` both pass the new `N14` multi-file dependency oracle (`3 / 3` behavior cases each) and scope guard; `binary tie remains`; `X2` scoreably fails (`0 / 1`) due `.reports` bundle-shape drift; `X4` remains `NOT-RUN`; `X5` and `X6` are runtime `NOT-RUN` after no-output Gemini timeouts |
| N15 stateful implementation gauntlet | `X1` and `X3` both pass the new `N15` stateful invariant suite (`9 / 9` invariants each) and scope guard; `binary tie remains`; calibration rows not launched |
| N16 long-horizon integration rubric | `X1` and `X3` both pass the new `N16` integration verifier and scope guard; `binary tie remains`; scored diagnostic read favors `X3` at `95 / 100` over `X1` at `89 / 100`; calibration rows not launched |
| N17 owner orchestration rubric | `X1`, `X2`, `X3`, and `X6` all pass and score `100 / 100`; `X5` is runtime `NOT-RUN` because direct smoke timed out without worker output; compactness favors `X3` and `X6` |
| N18 scientist/constraints rubric | `X1`, `X2`, and `X3` pass and score `100 / 100`; `binary tie remains` for `X1`/`X3`; `X6` is runtime `ROUTE-FAIL` after Gemini CLI tool-loop/abort with partial artifact `60 / 100`; `X5` direct smoke passes but semantic N18 run times out at `900s` without summary or worker output |
| N19 systems/toolchain rubric | `X1`, `X2`, and `X3` pass; `binary tie remains` for `X1`/`X3`, but scored diagnostic read favors `X3` at `95 / 100` over `X1` at `86 / 100`; `X6` is scoreable `FAIL` after missing portable cache key and source-trace report; `X5` not launched |
| N20 UI interaction rubric | `X1` and `X3` pass; `binary tie remains` for `X1`/`X3`, but scored diagnostic read favors `X3` at `95 / 100` over `X1` at `87 / 100`; `X2` is scoreable `FAIL` after creating forbidden top-level `.reports`; `X6` is runtime `ROUTE-FAIL` with partial UI misses |
| N21 visual raster rubric | `X1`, `X2`, and `X3` pass; `binary tie remains` for `X1`/`X3`, but scored diagnostic read favors `X3` at `100 / 100` over `X1` at `89 / 100` from compactness/cost, not visual correctness; `X5` and `X6` are runtime no-summary timeouts after launch |
| N22 numerical stability rubric | `X1` and `X3` pass; `binary tie remains` for `X1`/`X3`; scored read gives `X1 100 / 100` and `X3 99 / 100`; `X2` scoreably fails after making no candidate edits; `X6` is runtime `ROUTE-FAIL` with partial witness errors |
| N23 owner recovery rubric | `X1` and `X3` pass; `binary tie remains` for `X1`/`X3`, but scored diagnostic read favors `X3` at `100 / 100` over `X1` at `90 / 100`; `X2` scoreably fails after creating forbidden top-level `.reports`; `X6` is runtime `ROUTE-FAIL` |
| N24 systems/toolchain repeat | `X1` and `X3` pass; `binary tie remains` for `X1`/`X3`, but same-lane repeat confirms `X3` at `95 / 100` over `X1` at `86 / 100`; `X2`, `X5`, and `X6` are scoreable verifier failures |
| N25 UI dirty-state repeat | `X1`, `X3`, and `X5` pass; `binary tie remains` for `X1`/`X3`, but same-lane repeat confirms `X3` over `X1`; `X5` narrowly leads N25 at `98 / 100`; `X2` scoreably fails and `X6` route-fails |
| N26 owner recovery repeat | `X1`, `X3`, and `X5` pass; `binary tie remains` for `X1`/`X3`, but same-lane repeat confirms `X3` over `X1`; `X5` ties X3 at `100 / 100`; `X2` and `X6` scoreably fail |
| N27 long-horizon integration repeat | `X1`, `X2`, and `X3` pass; `binary tie remains` for `X1`/`X3`, but second long-horizon repeat confirms `X3` over `X1` by compactness/cost; `X6` route-fails and `X5` requeues after quota-gated smoke |
| N28 incident-driven integration repair | `X1` and `X3` pass; `binary tie remains` for `X1`/`X3`, but cross-role repair repeats the compact integration split with `X3 99 / 100` over `X1 93 / 100`; `X2` scoreably fails, `X6` route-fails, and `X5` requeues after smoke timeouts |
| N29 ownership-budget incident repair | `X1` and `X3` pass exact runtime and four-path patch-budget gates; `binary tie remains` for `X1`/`X3`, but scored read favors `X3 100 / 100` over `X1 96 / 100` by output cost; `X2` scoreably fails and `X5/X6` are runtime no-summary/timeout caveats |
| N30 staged delivery re-entry | `X1` passes; `X3` scoreably fails after omitting `03-review-response` from persisted phase state; this is the first current hardened top-pair binary separator and supports `X1 primary` for staged delivery/re-entry accountability |
| N31 MoM analytical oracle | `X1` and `X3` both pass a real computational-electromagnetics Method of Moments PEC-cylinder task with cylindrical-harmonic analytical oracle; `binary tie remains`; scored read favors `X3 94 / 100` over `X1 92 / 100` by output compactness while X1 is faster |
| N32 dual physics analytical oracle | `X1` and `X3` both pass a combined MoM PEC-cylinder plus hydrogenic radial Schrodinger analytical-oracle task with measured solver-runtime scoring; `binary tie remains`; scored read favors `X3 100 / 100` over `X1 97 / 100` by output compactness while both get full solver-runtime points; `X2` scoreably fails and `X5/X6` are runtime caveats |
| N33 interface refactor breakage | `X1` and `X3` both pass structured interface migration, hidden consumer behavior, legacy wrapper removal, ledger, and exact scope; `binary tie remains`; scored read favors `X3 100 / 100` over `X1 96 / 100` by output compactness; `X2` scoreably fails and `X6` is runtime no-summary |
| N34 high-load science optimizer | `X1` and `X3` both pass staged high-load MoM plus hydrogenic radial Schrodinger performance gates; `binary tie remains`; both read `96 / 100`, with X1 faster on measured solver metrics and X3 much more compact; `X2` scoreably fails and Gemini rows remain route caveats |
| N35 staged interface migration re-entry | `X1 PASS 96 / 100` versus `X3 scoreable FAIL 71 / 100`; `X2 PASS 91 / 100`; X3 failed hidden runtime semantics and migration-ledger details with `wrapperExitCode=0`; Gemini rows are runtime-route failures |
| N36 real-repo staged API migration | `X1 PASS 97 / 100` versus `X3 scoreable FAIL 74 / 100`; `X2` scoreably fails at `70 / 100`; X3 failed hidden API semantics and migration-ledger details with `wrapperExitCode=0`; X6 is runtime no-summary after Gemini phase-2 quota/stall and X5 stayed smoke-gated |
| N37 staged adversarial review gate | `X1 PASS 98 / 100` versus `X3 scoreable FAIL 35 / 100`; `X2 PASS 97 / 100`; X3 failed ADR source binding, exact finding/source-id tuples, non-claim ledger, response cues, and closure markers with `wrapperExitCode=0`; X6 is runtime-route after Gemini quota/tool-loop/AbortError and X5 stayed smoke-gated after direct Pro smoke timeout |
| N38 staged UI/visual/state integration | scored: `X1 PASS 94 / 100`; `X2` scoreable `FAIL 78 / 100`; `X3` is `NOT-RUN` after three phase-4 no-summary stalls; `X5/X6` are route caveats. Useful positive X1 staged UI evidence, but not a scoreable top-pair separator |
| N39 staged systems/toolchain recovery re-entry | bounded-scope rerun: `X1 PASS 94 / 100`; `X3` scoreable `FAIL 78 / 100`; `X2` scoreable `FAIL 76 / 100`; `X6` scoreable `FAIL 78 / 100`; `X5` runtime-route. Staged systems/toolchain recovery is now an admitted X1-over-X3 separator |
| N40 staged owner recovery re-entry | scored: `X1 PASS 98 / 100` versus `X3 scoreable FAIL 55 / 100`; `X2` scoreable `FAIL 78 / 100`; `X6` scoreable `FAIL 40 / 100`; `X5` route-fail. Staged owner recovery is now an admitted X1-over-X3 separator |
| N41 staged incident-budget re-entry | scored: `X1 PASS 100 / 100` versus `X3 scoreable FAIL 78 / 100`; `X2` scoreable `FAIL 78 / 100`; `X5` route-fail; `X6` runtime no-summary. Staged incident-budget re-entry is now an admitted X1-over-X3 separator |
| N42 systems/toolchain immutable-CI hotfix | `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass; both change only five production `src/stagegate/*.py` files; protected visible tests remain untouched. `binary tie remains`; no honest `X1 FAIL / X3 PASS` separator found |
| N43 UI dirty-state immutable-test hotfix | `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass; both change only three production UI files; protected visible tests remain untouched. `binary tie remains`; no honest `X1 FAIL / X3 PASS` separator found |
| N44 interface-refactor sourceId hidden consumer | `X1 / gpt-5.5` passes at `96 / 100`; `X3 / opus 4.7max` scoreably fails at `72 / 100` because `.pytest_cache` files violated exact changed-path scope. Hidden interface/sourceId/report semantics pass for X3, so this is patch-hygiene separation, not an inverse semantic separator |
| N45 ownership-budget immutable report consumer | `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass hidden replay/report-consumer gates with exact three-path budget. `binary tie remains`; X3 wins `100 / 100` versus X1 `96 / 100` only through output-cost points |
| N46 operator-budget compact hotfix | `X1 / gpt-5.5` scoreably fails the visible operator-budget gate (`210369 > 40000`) while passing hidden ownership and exact-scope gates; `X3 / opus 4.7max` passes all gates (`2378 <= 40000`). This is the first compact `X1 FAIL / X3 PASS` separator |
| N47 UI compact operator-budget hotfix | `X1 / gpt-5.5` scoreably fails the visible operator-budget gate (`169913 > 40000`) while passing hidden UI dirty-state and exact-scope gates; `X3 / opus 4.7max` passes all gates (`2467 <= 40000`). This is the second compact `X1 FAIL / X3 PASS` separator and first on UI |
| N48 visual raster compact operator-budget hotfix | `X1 / gpt-5.5` scoreably fails the visible operator-budget gate (`77825 > 40000`) while passing exact visual raster semantics and renderer-only scope; `X3 / opus 4.7max` passes all gates (`813 <= 40000`). This is the third compact `X1 FAIL / X3 PASS` separator and first on visual graphics |
| N49 scientific compact operator-budget optimizer | `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass the full high-load MoM plus hydrogenic radial Schrodinger optimizer verifier, exact scope, and visible operator-budget gate. `binary tie remains`; X3 wins `100 / 100` versus X1 `96 / 100` by measured runtime |
| N50 systems compact operator-budget hotfix | `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass hidden stagegate systems semantics, protected visible CI hash, exact scope, and visible operator-budget gate. `binary tie remains`; rubric ties at `99 / 100`, but elapsed time favors X3 (`260.449s` versus `395.714s`) |
| N51 systems turnaround-budget hotfix | both top rows fail scoreably under combined hidden semantics, output budget, and `360s` prompt-to-output SLA. X1 preserves hidden systems semantics and turnaround but fails output budget (`987540 > 40000`); X3 passes output and turnaround budgets but fails hidden stagegate semantics |
| N52 interface-refactor compact operator-budget | both top rows fail scoreably. X1 passes hidden interface-refactor semantics and exact required paths but fails output budget (`39316689 > 40000`); X3 passes output budget (`2573 <= 40000`) but fails scope/bundle-shape through `.pytest_cache` drift |
| N53 interface-refactor cache-ignored operator-budget | both top rows pass after top-level `.pytest_cache/**` is explicitly ignored as generated test cache. `X1 PASS 100 / 100`; `X3 PASS 100 / 100`; `binary tie remains` |
| N54 release-train compact operator-budget | `X1 / gpt-5.5` scoreably fails visible operator budget (`300873 > 40000`) after hidden release-train semantics and exact scope pass; `X3 / opus 4.7max` passes all gates (`2618 <= 40000`); fourth compact inverse separator |
| N55 incident compact operator-budget | `X1 / gpt-5.5` scoreably fails visible operator budget (`352056 > 40000`) after hidden incident/reconciliation semantics and exact scope pass; `X3 / opus 4.7max` passes all gates (`1841 <= 40000`); fifth compact inverse separator |
| N56 owner recovery compact operator-budget | `X1 / gpt-5.5` scoreably fails visible operator budget (`135621 > 40000`) after hidden owner-recovery semantics and exact scope pass; `X3 / opus 4.7max` passes all gates (`1220 <= 40000`); `X2` scoreable `FAIL 10 / 100`; `X6` runtime no-summary; sixth compact inverse separator |
| N57 compact real-repo API migration operator-budget | `X1 / gpt-5.5` scoreably fails visible operator budget (`3792275 > 40000`) after hidden BillingMesh API migration semantics and exact scope pass; `X3 / opus 4.7max` passes all gates (`2313 <= 40000`); `X2` scoreable `FAIL 11 / 100`; `X6` runtime no-summary; seventh compact inverse separator and first compact real-repo API migration inverse |
| N58 repeated-RHS MoM batch runtime | `X1 / gpt-5.5` scoreably fails visible operator budget (`2530582 > 40000`) after dual-physics regression, new MoM batch runtime oracle, exact scope, and runtime gates pass; `X3 / opus 4.7max` passes all gates (`2818 <= 40000`); `X2` scoreable `FAIL 25 / 100`; `X6` runtime no-summary; eighth compact inverse separator and first science/runtime inverse |
| N59 real-repo performance cache | Historical diagnostic now superseded in canonical `/40` by N85. N59 tied X1/X3 by binary and favored X3 only by scored compactness/runtime (`100 / 100` vs `90 / 100`). |
| N60 UI visual-state reentry | `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass hidden UI state, ARIA/status, layout, raster, ledger, closure, tests, and exact scope; `binary tie remains`; scorer favors X3 (`100 / 100` vs `96 / 100`) from compact output; `X2` scoreable `FAIL 10 / 100`; `X6` runtime no-summary; first X3 attempt hit usage limit and is runtime-route |
| hardening wave roadmap | `W1` through `W54` are complete. Latest additions: `W41` frame-inversion audit confirms execution-shape routing, `W42` security depth ties `X1/X3`, `W43` visual correctness ties `X1/X3`, `W44` conflicting evidence ties `X1/X3` while separating X2/X6 lower, `W45` cross-phase integration owner separates `X1 PASS` over `X3 FAIL`, `W46` actual screenshot review gives X3 a scored non-binary edge, `W47/W48` real-repo patch-quality scorecards tie binary while splitting X3 cost versus X1 hygiene, `W49` test-led regression ties binary with an X3 cost edge, `W50` caller-spanning API refactor ties binary with an X3 cost edge, `W51` DOM event runtime UI ties by binary/rubric, `W52` DOM runtime output budget separates `X3 PASS` over `X1 FAIL`, `W53` persisted-state replay migration ties `X1/X3` while X2 also passes, and `W54` staged persisted-state reentry separates `X1 PASS` over `X3 FAIL` |
| lane-fit scorecard | `Results-drafts/role-fit-scorecard-v1-2026-04-22.md` now maps current evidence into execution-shape-specific lane recommendations: `X3` is primary for ordinary compact single-session systems/toolchain, real-repo performance hot paths, compact UI/visual-state versus X1, compact single-session owner recovery, compact long-horizon integration, cross-role incident repair, ownership-budget repair, compact real-repo API migration, compact low-noise science/runtime, and low-noise operator-budget compact hotfixes; `X1` is primary for staged delivery/re-entry, staged systems/toolchain recovery, staged API/interface migration, staged review/advisory gates, staged owner recovery, staged incident-budget re-entry, and when self-added tests or trace-heavy numerical/reporting evidence matter; hard compact+turnaround systems hotfixes need extra verification because N51 fails both top rows differently; `X5` remains a UI and single-session owner contender only when route health returns |
| hardened separator calibration rows | `X2` is `1 / 5`; `X6` is `0 / 5`; `X5` is `0 / 0 scoreable` because Gemini Pro timed out before worker output, including a direct smoke prompt |
| mutable evidence in `Evidence/` | active `X1 / gpt-5.5` is `74 / 74` on `S01..S33 + N01..N41`; the older `X1 / gpt-5.4` `39 / 40` full-v2 read is a pre-v3 ceiling-effect baseline after the `S16/S19/S20` control-plane rerun; `X1`, `X2`, `X3`, `X4`, `X5`, and `X6` remain scoreable across the legacy full core `Scenarios-v2` `S01..S33 + N01..N07`; `X5` hardened timeout cells are closed; `N08..N10` are separate extra-lane evidence |
| current draft results | `Results-drafts/full-v2-hard-results-current.md` is the live hardened `/40` classifier and now includes admitted `X4 = 32 / 40`; `Results-drafts/short-results-current-2026-04-18.md` is the compact live operator table; `Results-drafts/v2-full-s01-s33-n01-n07-results-2026-04-18.md` is a deprecated pre-v3 baseline only; `Results-drafts/v2-extra-lane-n08-n10-results-2026-04-20.md`, `Results-drafts/v2-core12-tie-hardened-results-2026-04-20.md`, `Results-drafts/v2-top-pair-separators-n11-n13-results-2026-04-20.md`, and `Results-drafts/v2-top-pair-rubric-e3-results-2026-04-20.md` remain supporting/diagnostic v2-facing drafts; `Planning/next-phase/rf12-primary-resolution-plan-2026-04-25.md` records W41 through W54 primary-resolution results; the old `x1-x3-*` files remain legacy upgraded-pack checkpoint surfaces |

## Resume point

If this work is interrupted, resume from:

1. treat the current `x1-x3-*` tables as legacy checkpoint surfaces for the old upgraded-pack architecture only
2. use `Results-drafts/full-v2-hard-results-current.md` as the current hardened `/40` classification source of truth; use `Evidence/x1-x2-x3-x4-x5-x6-full-v2-s01-s33-n01-n07-2026-04-18.md`, `Evidence/x1-control-plane-override-rerun-s16-s19-s20-2026-04-20.md`, and `Results-drafts/v2-full-s01-s33-n01-n07-results-2026-04-18.md` only as deprecated pre-v3 baseline/provenance for the superseded weak-contract full-v2 read
3. use `Evidence/x1-x3-x5-v2-n08-n10-worker-long-autonomous-hardened-2026-04-20.md` plus `Results-drafts/v2-extra-lane-n08-n10-results-2026-04-20.md` as the current tightened `worker.long-autonomous` extra-lane tiebreaker source
4. use `Evidence/x1-x3-x5-core12-tie-hardened-2026-04-20.md`, `Evidence/x5-core12-timeout-closure-2026-04-20.md`, and `Results-drafts/v2-core12-tie-hardened-results-2026-04-20.md` as the current hardened weak-separator source for `X1`, `X3`, and `X5`
5. use `Evidence/x1-x3-top-pair-separators-n11-n13-2026-04-20.md` plus `Results-drafts/v2-top-pair-separators-n11-n13-results-2026-04-20.md` as the diagnostic E2 source; do not claim it ranks `X1` versus `X3`
6. use `Evidence/x1-x3-top-pair-rubric-e3-2026-04-20.md` plus `Results-drafts/v2-top-pair-rubric-e3-results-2026-04-20.md` as the diagnostic E3 top-pair rubric source
7. use `Results-drafts/role-fit-scorecard-v1-2026-04-22.md` as the current lane-fit routing read; it keeps binary ties distinct from rubric, cost, compactness, and test-coverage tendencies
8. for further `X1`/`X3` separation, `N06`, wave 2 (`S27`, `S28`, `S29`, `N05`, `N07`), `S06`, `S22`, and `N14..N87` are complete and admitted. `N30`, `N35`, `N36`, `N37`, `N39`, `N40`, `N41`, `N67`, `N76`, `N79`, `N80`, and `N86` are the current admitted top-pair binary separators in favor of `X1` for staged delivery/re-entry, staged systems/toolchain recovery, staged API/interface migration, staged review/advisory gates, staged owner recovery, staged incident-budget re-entry, staged cross-phase integration-owner / QA-stop closure, staged persisted-state migration/re-entry, staged UI/visual-state re-entry, and calibrated screenshot grounding. `N46`, `N47`, `N48`, `N54`, `N55`, `N56`, `N57`, `N58`, `N74`, and `N85` are admitted compact inverse separators in favor of `X3`: X1 preserves hidden semantics/physics/runtime and exact scope but fails the visible operator-budget gate, while X3 passes all gates compactly. `N57` is the first compact real-repo API migration inverse; staged API/interface migration remains `X1 primary` after `N35/N36`. `N58` is the first compact science/runtime inverse; `N74` is the first DOM-runtime UI inverse after `N73` tied without output budget; pure scientific correctness remains near-tie after `N34/N49/N58` because X1 passes the physical oracles. `N49`, `N50`, `N53`, `N59`, `N60`, `N64`, `N65`, `N66`, `N69`, `N70`, `N71`, `N72`, `N73`, `N81`, `N82`, `N83`, and `N87` tied at PASS/PASS, so they are negative inverse or primary-resolution tie evidence; `N69/N70/N71/N72` add repeated scored splits where X3 wins output-cost and X1 does not gain a binary semantic edge; `N73` adds runtime UI near-tie evidence with equal rubric; `N74` adds paired DOM runtime output-budget separation in favor of X3; `N75` adds persisted-state replay migration near-tie evidence and also passes X2; `N76` converts persisted-state migration into an X1 staged separator. `N64` keeps ordinary security-depth review near-tie; `N65` keeps ordinary visual-correctness review near-tie; `N66` and `N81` keep conflicting-evidence repo-understanding near-tie; `N82` keeps UX runtime-state spec near-tie; `N83` keeps hidden-consumer interface refactor near-tie; `N86` adds an X1-over-X3 exact public-API migration-surface diagnostic after X3 passes hidden downstream semantics but misses `api.py` scope. `N68` gives X3 a scored actual-screenshot edge but both top rows fail; `N80` supersedes it for calibrated screenshot grounding in favor of X1. `N51` tested hard turnaround on systems/toolchain and produced both scoreable fail, not inverse separation. `N52` tested interface-refactor compactness before generated-cache isolation and produced both scoreable fail: X1 was semantically correct but too verbose, while X3 was compact but leaked `.pytest_cache`; the runner now classifies top-level `.pytest_cache/` as auxiliary generated cache. `N42`, `N43`, and `N45` explicitly tested inverse-separator hypotheses by protecting visible tests and adding hidden consumers; all tied `X1 / gpt-5.5` and `X3 / opus 4.7max` at PASS/PASS. `N44` tested hidden sourceId interface consumers and produced `X1 PASS` versus `X3 FAIL`, but the X3 fail is exact patch-hygiene drift from `.pytest_cache`, not a hidden semantic miss. Scored lane reads still support `X3 primary` versus `X1` for ordinary single-session systems/toolchain (`N19`, `N24`, and N50 elapsed time), UI implementation (`N20`, `N25`, `N47`, `N60`, and `N74` for low-noise UI/visual-state/runtime-budget work), visual raster graphics (`N21` by rubric and `N48` by binary low-noise gate), owner recovery (`N23`, `N26`, and `N56` for low-noise compact owner packets), compact real-repo API migration (`N57`), compact low-noise science/runtime (`N58`), compact real-repo performance runtime (`N85` by binary separator; `N59` by historical scored fit), compact patch cost (`N69/N70/N71/N72` by scored fit), compact long-horizon and release-train integration (`N16`, `N27`, and `N54`), cross-role incident repair (`N28` and `N55`), ownership-budget incident repair (`N29` and `N45` by cost only), and low-noise operator-budget compact hotfixes (`N46`, `N47`, `N48`, `N54`, `N55`, `N56`, `N57`, `N58`, `N74`). Scientific optimizer work remains top-pair co-primary by binary after `N34`, `N49`, and physical `N58` gates; ordinary cache-controlled small interface refactor remains top-pair tied after `N33`, `N53`, and `N83`, performance-review gate remains top-pair tied after `N87`, while exact public-API migration-surface work leans X1 after `N86`. `N38` is unresolved because X3 never produced a scoreable final summary across three attempts, while `N60` closes the single-session UI/visual-state branch as PASS/PASS with X3 cost edge. `X5` is a UI and single-session owner contender after N25/N26 but is currently quota-deferred; N27..N83 still have no scoreable X5 semantic evidence because smoke failed, route quota exhausted, runs stayed unscored, or the row was not launched. `X2` passes some calibration rows including N27/N35/N37 but separates lower on adjacent implementation repeats and fails N39/N40/N41/N56/N58/N59/N60/N64/N65/N66/N67/N69/N70/N71/N72/N73 scoreably; N74 and N79..N83 were not launched; N75 passes; N76 fails exact staged scope. `X6` remains mixed scoreable fail, route-fail, runtime-caveat, or runtime no-summary depending on the row. Review-class single-shot tuple tasks, factual-investigation, geometry tightening, conflicting-evidence repo-understanding, multi-file dependency, stateful rollback/resume, ordinary release-lane patch without operator budget, basic owner-boundary routing, bounded scientist/constraint decision tasks, current numerical-stability decision tasks, current owner-recovery tasks without operator budget, current single-session systems/toolchain implementation tasks without time budget, current UI interaction tasks without operator budget, current visual-raster tasks without operator budget, current cross-role incident repair tasks without operator budget, current ownership-budget repair tasks, scientific correctness tasks without strict low-noise budget, ordinary security-depth review, ordinary visual-correctness review, read-only performance-review gate tasks, cache-controlled small interface-refactor tasks, systems/toolchain output-budget-only tasks, systems/toolchain compact+turnaround tasks, patch-quality tasks without hard binary cost/hygiene policy, small test-led regression patches, caller-spanning single-session API refactors, DOM event runtime UI without strict output budget, single-session persisted-state replay migration, and immutable-test/hidden-consumer single-session probes can no longer be claimed as current top-pair binary separators.
9. when re-launching codex-backed runs (`X1`, `X2`) through a background channel, use `cmd /c "pwsh -ExecutionPolicy Bypass -File <runner.ps1> ... -ScenarioIds <ONE_SCENARIO> < NUL"` — `cmd /c` collapses `-ScenarioIds S27,S28,...` into a single comma-joined token across the pwsh `-File` boundary and the runner throws `Unknown scenario id`; use one background task per scenario and one `<NUL` per task instead. The `run_in_background` tool path also otherwise leaves `stdin` attached and codex blocks on `stdin` read indefinitely (observed `17 min / 0.03s CPU` dead-lock on the first `X1 N06` attempt). Claude-backed runs (`X3`, `X4`) and gemini-backed runs (`X5`, `X6`) accept comma-joined `-ScenarioIds` through the direct `& "script.ps1" -RowId X3 -ScenarioIds S27,S28,...` invocation when called from the PowerShell tool directly
10. the wave-2 verifier parser honors `\|` escape and merges trailing cells back into the last column when a candidate's Evidence cell contains raw `\|\|`; `N06` retains the earlier parser and has not been re-patched since its admitted `X1`/`X3` PASS reports did not exercise that path; if the cell is ever re-run, back-port the same parser fix
11. keep `X4` out of per-wave hardening probes and rerun it only in the final closing comparison batch
12. if the next step admits scored rubrics as a ranking surface, start from the N16/N28/N69 scorer patterns and decide whether time/cost/patch-quality weights should be normalized across multiple large tasks before comparing providers globally
13. run `mcp-free` periodically between long provider runs to clear stale orphan MCP helpers while preserving active parent-owned MCP servers

## 2026-04-23 W10 / W11 update

| Topic | Current read |
|---|---|
| staged delivery re-entry | `N30` is diagnostic `E20` and the first current top-pair binary separator: `X1 PASS 96 / 100`, `X3 scoreable FAIL 91 / 100`; X3 passed runtime/scope/review but omitted `03-review-response` from persisted `delivery-state.json` |
| calibration | `X2` scoreably fails N30 after forbidden top-level `.reports` drift; `X6` is `runtime-no-summary` after Gemini quota/tool-loop timeout; `X5` is `REQUEUE` because same-session smoke timed out without `X5_SMOKE_OK` |
| routing impact | keep `X3 primary` for compact single-invocation implementation surfaces (`N16`, `N19`, `N20`, `N23`, `N24`, `N25`, `N26`, `N27`, `N28`, `N29`); use `X1 primary` for staged delivery re-entry and phase-ledger accountability |
| latest artifacts | `Scenarios-v2/N30-staged-delivery-reentry-gauntlet/`, `Tooling/run-v2-staged-cohort-batch.ps1`, `Tooling/score-n30-staged-delivery-rubric.py`, and `Evidence/n30-staged-delivery-rubric-2026-04-23.json` |
| scientific hardening | `N31` is diagnostic `E21`: MoM PEC-cylinder solver with cylindrical-harmonic analytical oracle; `X1` and `X3` both pass, `X3 94 / 100`, `X1 92 / 100`; `N32` is diagnostic `E22`: combined MoM plus hydrogenic radial Schrodinger analytical oracle with solver-runtime scoring; `X3 100 / 100`, `X1 97 / 100`, `X2` scoreable FAIL, `X6` runtime no-summary, `X5` smoke REQUEUE |
| next admitted wave | none; optional W13 is high-load CEM/radial-Schrodinger performance if runtime separation is required |

## 2026-04-23 W13 / W14 update

| Topic | Current read |
|---|---|
| interface refactor hardening | `N33` is diagnostic `E23`: structured result interfaces, hidden consumers, legacy wrapper removal, migration ledger, and exact patch scope. `X1` and `X3` both pass; `X3 100 / 100`, `X1 96 / 100`; `X2` scoreable FAIL `5 / 100`; `X6` runtime no-summary; `X5` not admitted |
| high-load science optimizer | `N34` is diagnostic `E24`: staged state plus high-load MoM PEC-cylinder and hydrogenic radial Schrodinger performance. `X1` and `X3` both pass and both read `96 / 100`; X1 is faster on measured solver metrics (`0.319s` versus `3.448s`), X3 is much more compact (`2411` bytes versus `540758`); `X2` scoreable FAIL `27 / 100`; Gemini rows remain route caveats |
| staged interface migration | `N35` is diagnostic `E25`: four fresh staged invocations over interface migration plus review response and closeout. `X1 PASS 96 / 100`; `X3` scoreable FAIL `71 / 100`; `X2 PASS 91 / 100`; `X5/X6` route-fail |
| routing impact | `N30` and `N35` now both support `X1 primary` for staged delivery/re-entry and phase-ledger accountability. N35 specifically moves staged interface migration to `X1 primary`. Keep `X3 primary` for compact single-session implementation and ordinary interface migration when staged re-entry is not part of the task. N34 remains scientist/performance near-tie evidence: prefer X1 when measured runtime dominates, X3 when output/cost compactness dominates |
| latest artifacts | `Scenarios-v2/N33-interface-refactor-breakage-gauntlet/`, `Scenarios-v2/N34-high-load-science-optimizer-gauntlet/`, `Scenarios-v2/N35-staged-interface-migration-reentry-gauntlet/`, `Tooling/score-n33-interface-refactor-rubric.py`, `Tooling/score-n34-science-optimizer-rubric.py`, `Tooling/score-n35-staged-interface-rubric.py`, `Evidence/n33-interface-refactor-rubric-2026-04-23.json`, `Evidence/n34-science-optimizer-rubric-2026-04-23.json`, and `Evidence/n35-staged-interface-rubric-2026-04-23.json` |
| next admitted wave | none. If more separation is required, use a real-repo staged interface/API migration repeat rather than another single-shot synthetic correctness task |

## 2026-04-23 W16 update

| Topic | Current read |
|---|---|
| real-repo staged API migration | `N36` is diagnostic `E26`: four fresh staged invocations over a BillingMesh-style API migration with account lookup, entitlement policy, usage publishing, service/API/reporting consumers, review response, closeout, hidden runtime contracts, and exact scope. `X1 PASS 97 / 100`; `X3` scoreable FAIL `74 / 100`; `X2` scoreable FAIL `70 / 100`; `X6` runtime no-summary after Gemini phase-2 quota/stall; `X5` stayed smoke-gated |
| X3 failure signature | `wrapperExitCode=0`, verifier `FAIL`: `account-lookup-contract`, `entitlement-contract`, `publisher-contract`, `reporting-contract`, `phase-ledger-complete`, `migration-interfaceMap`, `migration-callSites`, `migration-compatibilityCases`, and `migration-validation` |
| routing impact | `N30`, `N35`, and `N36` now support `X1 primary` for staged delivery/re-entry and staged API/interface migration. `N36` confirms the N35 split on a fresh real-repo-style domain, so this is no longer a one-fixture artifact |
| latest artifacts | `Scenarios-v2/N36-realrepo-staged-api-migration-gauntlet/`, `Tooling/score-n36-staged-api-rubric.py`, `Evidence/n36-staged-api-rubric-2026-04-23.json`, and the W16 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next admitted wave | none synthetic by default. Next useful step should be a real-repo trial or a different lane where current role-fit still has an unresolved policy decision |

## 2026-04-23 W17 update

| Topic | Current read |
|---|---|
| staged adversarial review gate | `N37` is diagnostic `E27`: four fresh staged invocations over source ledger, source-bound ADR, exact findings/non-findings, response-gate decisions, and closure. `X1 PASS 98 / 100`; `X3` scoreable FAIL `35 / 100`; `X2 PASS 97 / 100`; `X6` runtime-route after Gemini quota/tool-loop/AbortError; `X5` direct Pro smoke timed out after `180s` and semantic run was skipped |
| X3 failure signature | `wrapperExitCode=0`, scope `PASS`, verifier `FAIL`: `ledger-validationMarkers`, `adr-source-bound`, `finding-tuples`, `finding-source-ids`, `non-finding-ledger`, `response-gate-complete`, and `closure-complete` |
| routing impact | `N37` extends the staged separator outside implementation. Use `X1 primary` for staged source arbitration, ADR traceability, response-gate closure, and exact review/non-claim ledgers. Ordinary single-shot tuple-exact review remains an `X1/X3` near-tie |
| latest artifacts | `Scenarios-v2/N37-staged-adversarial-review-gate-gauntlet/`, `Tooling/score-n37-staged-review-rubric.py`, `Evidence/n37-staged-review-rubric-2026-04-23.json`, and the W17 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next admitted wave | remaining unresolved lines are UX/visual provider-fit and harder real-repo or real-performance trials; do not add another synthetic staged review repeat unless a concrete repo review trial is available |

## 2026-04-23 W18 / W21 update

| Topic | Current read |
|---|---|
| staged UI / visual / state integration | `N38` is diagnostic `E28`: deterministic staged UI packet with command state, per-record dirty navigation, ARIA/status cues, layout geometry, raster pixels, implementation ledger, and closeout. `X1 PASS 94 / 100`; `X2` scoreable `FAIL 78 / 100`; `X3` is `NOT-RUN` after three phase-4 no-summary stalls; `X5/X6` are runtime-route caveats |
| staged systems / toolchain recovery | `N39` is diagnostic `E29`: staged toolchain recovery with stale-source arbitration, runtime-status discipline, implementation ledger, and closeout. Bounded-scope rerun reads `X1 PASS 94 / 100`; `X3` scoreable `FAIL 78 / 100`; `X2` scoreable `FAIL 76 / 100`; `X6` scoreable `FAIL 78 / 100`; `X5` runtime-route. Staged systems/toolchain recovery now reads `X1 primary` |
| staged owner recovery | `N40` is diagnostic `E30`: four staged packets over source-ledger, route-decision, runtime-policy, and closeout. `X1 PASS 98 / 100`; `X3` scoreable `FAIL 55 / 100`; `X2` scoreable `FAIL 78 / 100`; `X6` scoreable `FAIL 40 / 100`; `X5` route-fails on repeated Gemini capacity exhaustion |
| staged incident-budget re-entry | `N41` is diagnostic `E31`: staged DeployGrid runtime repair with repair ledger, reentry state, exact six-path budget, and closeout. `X1 PASS 100 / 100`; `X3` scoreable `FAIL 78 / 100`; `X2` scoreable `FAIL 78 / 100`; `X5` route-fails and `X6` is runtime no-summary |
| routing impact | keep `X3 primary` for compact single-session systems/toolchain, owner, and incident-repair work (`N19/N24`, `N23/N26`, and `N16/N27/N28/N29`), but use `X1 primary` for staged systems/toolchain recovery, staged owner recovery, and staged incident-budget re-entry (`N39/N40/N41`). UI stays `X3 primary` for single-session work, with `N38` still unresolved because X3 never completed scoreably |
| latest artifacts | `Scenarios-v2/N38-deterministic-ui-visual-state-integration-gauntlet/`, `Scenarios-v2/N39-staged-systems-toolchain-reentry-gauntlet/`, `Scenarios-v2/N40-staged-owner-recovery-reentry-gauntlet/`, `Scenarios-v2/N41-staged-incident-budget-reentry-gauntlet/`, `Tooling/score-n40-staged-owner-rubric.py`, `Tooling/score-n41-staged-incident-budget-rubric.py`, `Evidence/n38-ui-visual-state-rubric-2026-04-23.json`, `Evidence/n39-staged-toolchain-rubric-2026-04-23.json`, `Evidence/n40-staged-owner-rubric-2026-04-23.json`, and `Evidence/n41-staged-incident-budget-rubric-2026-04-23.json` |
| next admitted wave | full rerun after updating X1 to `gpt-5.5`. No more N39 repair work remains; N38 remains runtime-unresolved for X3 |

## 2026-04-24 X1 GPT-5.5 / Claude Hard-5 update

| Topic | Current read |
|---|---|
| active X1 model | `X1` is now `gpt-5.5`; older `gpt-5.4` labels in historical evidence are model-of-run labels, not the active row |
| X1 binary refresh | fresh rerun over `S01..S33 + N01..N41` is `74 / 74` unique verifier `PASS`; `S13` clean retry supersedes an earlier wrapper-nonzero summary; `N30` is verifier `PASS` with wrapper caveat |
| hard-5 staged subset | `N35,N36,N37,N39,N41`: `X1 / gpt-5.5` is `5 / 5`; explicit `X1 / gpt-5.4 xhigh` comparison is also `5 / 5`; admitted `X3 / opus 4.7max` is `0 / 5`; `X4 / Claude China opus max` is `1 / 5`; official opus 4.5 and opus 4.6 are `0 / 5`; official sonnet is `0 / 3 scoreable`; official haiku is `0 / 4 scoreable` |
| runtime caveats | Sonnet `N39/N41` and Haiku `N41` have no final summary / timeout and stay runtime-route, not model-quality failures |
| tooling | staged and cohort runners accept `BENCHMARK_CODEX_MODEL_OVERRIDE` for explicit Codex comparison runs; staged runner also accepts `BENCHMARK_CLAUDE_MODEL_OVERRIDE` / `BENCHMARK_MODEL_LABEL_OVERRIDE` for official Claude alias probes; defaults for active `X1`, `X3`, and `X4` are unchanged |
| next action | commit the refreshed live surfaces and keep staged integration/refactor/re-entry lanes as `X1 primary` |

## 2026-04-25 W50 update

| Topic | Current read |
|---|---|
| caller-spanning API refactor | `N72` is diagnostic W50: hidden API/service/CLI/report callers, legacy compatibility, payload immutability, exact five-path scope, and refactor ledger. `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass; `binary tie remains`. |
| calibration | `X2` scoreably fails at `15 / 100` after no patch; `X6` is runtime `NOT-RUN` because the shell command timed out after `1800s` without `summary.json`; a local probe of the partial X6 run root also failed visible and hidden contracts, but it is not a scoreable row; X5 smoke timed out after `244s` with no output and no explicit quota/reset. |
| routing impact | staged API/interface migration remains `X1 primary` from N35/N36. Ordinary single-session caller-spanning refactor is `X1 / X3 binary near-tie`; choose X3 when output cost matters and guard cache/scope hygiene explicitly. |
| latest artifacts | `Scenarios-v2/N72-caller-spanning-api-refactor-scorecard/`, `Tooling/score-n72-caller-refactor-rubric.py`, `Evidence/n72-caller-refactor-rubric-2026-04-25.json`, and the W50 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | change axis again; avoid more compact single-session refactor variants unless they introduce actual browser/UI execution, real-repo trial constraints, or staged persisted-state semantics. |

## 2026-04-25 W51 update

| Topic | Current read |
|---|---|
| DOM event runtime UI | `N73` is diagnostic W51: Node DOM/event harness over filter clicks, keyboard dirty toggles, save behavior, exact scope, ledger, and payload immutability. `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass and tie at `93 / 100`; `binary tie remains`. |
| calibration | `X2` and `X6` both scoreably fail at `15 / 100`; X6 wrapper is `0`, so this is a scoreable lower-row failure despite Gemini quota/tool-loop noise in stdout. |
| routing impact | compact UI remains X3-leaning only when low-noise/output budget is hard. Runtime UI correctness without strict output budget is `X1 / X3 near-tie`; verify behavior on the target UI. |
| latest artifacts | `Scenarios-v2/N73-dom-event-runtime-ui-scorecard/`, `Tooling/score-n73-dom-runtime-rubric.py`, `Evidence/n73-dom-runtime-rubric-2026-04-25.json`, and the W51 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | move to a harder real-repo or multi-step persisted-state task; more single-session UI correctness variants are low yield. |

## 2026-04-25 W52 update

| Topic | Current read |
|---|---|
| DOM runtime output budget | `N74` is diagnostic W52: it repeats the N73 Node DOM/event runtime harness and adds a visible `50000` byte operator-output budget verifier. `X1 / gpt-5.5` passes runtime semantics and exact five-path scope but scoreably fails the budget (`80 / 100`, `241980` bytes). `X3 / opus 4.7max` passes all gates (`100 / 100`, `2085` bytes). |
| calibration | `X2` and `X6` were not launched in W52 because W51 just calibrated the same DOM runtime base; lower-row calibration should resume on the next semantic task or final comparison sweep. |
| routing impact | runtime UI correctness without strict output budget stays `X1 / X3 near-tie` from N73. Runtime UI with a hard visible output budget is `X3 primary` after N74. |
| latest artifacts | `Scenarios-v2/N74-dom-runtime-output-budget-scorecard/`, `Tooling/score-n74-dom-runtime-budget-rubric.py`, `Evidence/n74-dom-runtime-budget-rubric-2026-04-25.json`, and the W52 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | switch to a harder real-repo or multi-step persisted-state task with semantic risk; do not keep mining single-session correctness variants unless the verifier promotes a real execution budget or failure mode. |

## 2026-04-25 W53 update

| Topic | Current read |
|---|---|
| persisted-state replay migration | `N75` is diagnostic W53: v1/v2 event migration, source immutability, idempotent replay, checkpoint rollback, schema-v2 persist/load envelope, exact scope, visible regression, and migration-ledger coverage. `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass at `83 / 100`; `binary tie remains`. |
| calibration | `X2` also passes at `75 / 100`, so the single-session fixture is weak as a top-pair separator. `X6` is runtime `NOT-RUN` after shell timeout with no `summary.json`. |
| routing impact | ordinary single-session persisted-state migration is `X1 / X3 near-tie`; do not assign a primary from N75. Next persisted-state work should be staged or real-repo-integrated. |
| latest artifacts | `Scenarios-v2/N75-persisted-state-replay-migration-scorecard/`, `Tooling/score-n75-persisted-state-rubric.py`, `Evidence/n75-persisted-state-rubric-2026-04-25.json`, and the W53 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | build a staged persisted-state re-entry or real-repo migration lane; avoid adding more hidden replay cases to the same single-session shape. |

## 2026-04-25 W54 update

| Topic | Current read |
|---|---|
| staged persisted-state reentry | `N76` is diagnostic W54: staged source ledger, migration implementation, re-entry validation, closeout, and the N75 runtime migration oracle. `X1 / gpt-5.5` passes at `85 / 100`; `X3 / opus 4.7max` scoreably fails at `15 / 100`; `X2` scoreably fails at `70 / 100`; `X6` is runtime `NOT-RUN`. |
| X3 failure | X3 missed exact scope (`migrator.py`), schema version, persist envelope, source/migration/reentry/closeout contracts. This is semantic/artifact failure, not quota or timeout. |
| routing impact | staged persisted-state migration/re-entry is `X1 primary`. Single-session persisted-state migration remains near-tie after N75. |
| latest artifacts | `Scenarios-v2/N76-staged-persisted-state-reentry-gauntlet/`, `Tooling/score-n76-staged-persisted-state-rubric.py`, `Evidence/n76-staged-persisted-state-rubric-2026-04-25.json`, and the W54 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | target another unresolved primary line; N75/N76 already establish the single-session versus staged persisted-state split. |

## 2026-04-25 W55 update

| Topic | Current read |
|---|---|
| security capability runtime patch | `N77` is diagnostic W55: hidden runtime exploit oracle over HMAC capability tokens, tenant/user/resource binding, expiry, replay, redirect traps, audit redaction, exact scope, regression test, and security ledger. `X1 / gpt-5.5` and `X3 / opus 4.7max` both pass, so `binary tie remains`; X3 scores higher (`93 / 100` vs `85 / 100`) by output cost. |
| calibration | `X2` is a scoreable no-op fail at `15 / 100` with wrapper `0`; `X6` timed out after `2400s` with no `summary.json` and stays runtime `NOT-RUN`. |
| routing impact | ordinary single-session security implementation is `X1 / X3 near-tie`; choose X3 when output compactness is first-class, and X1 when trace-heavy security reasoning or workspace hygiene matters. |
| latest artifacts | `Scenarios-v2/N77-security-capability-runtime-scorecard/`, `Tooling/score-n77-security-capability-rubric.py`, `Evidence/n77-security-capability-rubric-2026-04-25.json`, and the W55 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | build staged security re-entry if security lane needs a primary; avoid adding more single-session exploit cases to N77. |

## 2026-04-25 W56 update

| Topic | Current read |
|---|---|
| staged security re-entry | `N78` is diagnostic W56: staged threat ledger, security implementation, exploit validation, re-entry state, closeout, and the N77 runtime exploit oracle. `X1 / gpt-5.5` passes at `85 / 100`; `X3 / opus 4.7max` scoreably fails at `23 / 100`; `X2` scoreably fails at `45 / 100`; `X6` is runtime `NOT-RUN`. |
| X3 failure | X3 used current `opus 4.7max` route (`--model opus --effort max`), not Sonnet. Failure is scoreable: hidden percent-encoded CRLF redirect trap plus staged ledger/validation/closeout contract gaps. |
| routing impact | staged security implementation/re-entry is `X1 primary`. Single-session security implementation remains near-tie after N77. |
| latest artifacts | `Scenarios-v2/N78-staged-security-reentry-gauntlet/`, `Tooling/score-n78-staged-security-rubric.py`, `Evidence/n78-staged-security-rubric-2026-04-25.json`, and the W56 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | target another unresolved primary line; N77/N78 establish the single-session versus staged security split. |

## 2026-04-28 N79+ roadmap/design/plan update

| Topic | Current read |
|---|---|
| active planning surface | `Planning/next-phase/rf12-primary-resolution-plan-2026-04-25.md` is now `ACTIVE` and owns the N79+ roadmap -> design -> plan contract. |
| live wave queue | `Planning/next-phase/hardening-wave-roadmap-2026-04-22.md` remains the single live roadmap and now queues W57-W69 / N79-N90 plus W70 candidate axes. |
| priority | W57/N79 through W69/N90 are complete. Next priority is choosing a W70 axis that changes task family instead of tightening staged UX review. W64 promotion review is parked until another named replacement is admitted. |
| run policy | Run `X1 / gpt-5.5` and `X3 / opus 4.7max` first. Do not run `X4` during iterative hardening; reserve it for final lanes when explicitly approved. Park Gemini rows while route/auth health is broken or deprioritized. |
| promotion policy | New diagnostics stay outside canonical `full-v2-hard /40` until a slot-replacement decision names the removed slot and explains why the replacement is stronger. |
| next action | Completed through W69/N90. N85 is promoted over N59 in `full-v2-hard /40`; N86, N87, N88, N89, and N90 are diagnostic-only. Next action is selecting W70 from the roadmap candidate axes. |

## 2026-04-28 W57 update

| Topic | Current read |
|---|---|
| staged UI visual-state reentry | `N79` is diagnostic W57: four-phase source/state ledger, state/render implementation, layout/raster validation, and reentry closeout over the hidden N60 UI/raster oracle. |
| result | `X1 / gpt-5.5` passes at `96 / 100`; `X3 / opus 4.7max` scoreably fails at `63 / 100`; both wrappers exited `0`. |
| X3 failure | X3 passed exact changed-path scope and phase-path discipline, then failed visible blocked cue, focus return id, active descendant/accessibility, compact layout containment, raster overlay order, and ledger/closure completeness. |
| routing impact | staged UI/visual-state reentry is now `X1 primary`. Compact single-session UI remains `X3 primary` only when low-noise/output budget is first-class. |
| latest artifacts | `Scenarios-v2/N79-staged-ui-visual-state-reentry-v2/`, `Work/next-upgraded-pack/Tooling/score-n79-staged-ui-reentry-rubric.py`, `Work/next-upgraded-pack/Evidence/n79-staged-ui-reentry-rubric-2026-04-28.json`, and the W57 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Phase D from the active plan: materialize `N80-screenshot-grounding-review-v2`; run X1/X3 only after local visual/oracle validation. |

## 2026-04-28 W58 update

| Topic | Current read |
|---|---|
| screenshot grounding review | `N80` is diagnostic W58: deterministic `1600 x 1100` dashboard screenshot, ten seeded visual defects, `22 px` coordinate window, false-positive traps, exact JSON shape, and score-threshold verifier. |
| result | `X1 / gpt-5.5` passes at `82 / 100` with `8 / 10` matched and mean/max error `2.855 / 7.071 px`; `X3 / opus 4.7max` scoreably fails at `63 / 100` with `7 / 10` matched, mean/max error `8.067 / 17.117 px`, and one false-positive header ornament. Both wrappers exited `0`. |
| routing impact | calibrated actual screenshot grounding is now `X1 primary`. N68 remains historical non-binary context only. Compact raster/code visual patches remain X3-primary when output budget is the hard gate. |
| latest artifacts | `Scenarios-v2/N80-screenshot-grounding-review-v2/`, `Work/next-upgraded-pack/Tooling/generate-n80-screenshot-grounding-assets.py`, `Work/next-upgraded-pack/Tooling/score-n80-screenshot-grounding-rubric.py`, `Work/next-upgraded-pack/Evidence/n80-screenshot-grounding-rubric-2026-04-28.json`, and the W58 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Completed through W65/N86. N85 is promoted over N59 in `full-v2-hard /40`; N86 is diagnostic-only. Next action is W66/N87 performance-review gate. |

## 2026-04-28 W59 update

| Topic | Current read |
|---|---|
| evidence-conflict action plan | `N81` is diagnostic W59: source authority, evidence conflict ledger, command evidence, bounded action plan, non-claims, and product re-intake trigger over current code/tests, command output, accepted ADR, stale docs, and draft rollback ADR. |
| result | `X1 / gpt-5.5` passes at `100 / 100` with `24 / 24` matched; `X3 / opus 4.7max` also passes at `100 / 100` with `24 / 24` matched. Both wrappers exited `0` and changed only `candidate/action-plan.md`. |
| routing impact | `binary tie remains`; advisory repo-understanding/action-plan stays `X1 / X3 near-tie`. Future advisory separator work should avoid broad forbidden-literal traps and use decision-context or staged review-gate checks. |
| latest artifacts | `Scenarios-v2/N81-evidence-conflict-repo-action-plan/`, `Work/next-upgraded-pack/Evidence/n81-evidence-action-rubric-2026-04-28.json`, and the W59 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Completed through W65/N86; next action is W66/N87 performance-review gate. |

## 2026-04-28 W60 update

| Topic | Current read |
|---|---|
| UX runtime state spec | `N82` is diagnostic W60: valid JSON UX-owned state spec with five runtime states, three breakpoint invariants, six affordance rules, five copy-ledger entries, three handoffs, and five non-goals. |
| result | `X1 / gpt-5.5` passes at `100 / 100` with `27 / 27` matched; `X3 / opus 4.7max` also passes at `100 / 100` with `27 / 27` matched. Both wrappers exited `0` and changed only `candidate/ux-state-spec.json`. |
| routing impact | `binary tie remains`; UX structure/runtime-state stays `X1 / X3 near-tie`. Future UX separators need runtime simulation, staged UX review, or visual-grounded checks instead of term-matched JSON anchors. |
| latest artifacts | `Scenarios-v2/N82-ux-structure-runtime-state-spec/`, `Work/next-upgraded-pack/Evidence/n82-ux-state-rubric-2026-04-28.json`, and the W60 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Completed through W65/N86; next action is W66/N87 performance-review gate. |

## 2026-04-28 W61 update

| Topic | Current read |
|---|---|
| interface-refactor breakage hunt | `N83` is diagnostic W61: structured result dataclasses, legacy method removal, hidden batch API consumer, duplicate-state preservation, rejected-request non-dispatch, structured report input, exact ten-path scope, visible regression markers, and migration ledger. |
| result | `X1 / gpt-5.5` passes; `X3 / opus 4.7max` also passes. Both wrappers exited `0`, both passed `check_interface_refactor.py`, and both passed `check_scope.py` with exactly the ten required benchmark paths. |
| routing impact | `binary tie remains`; ordinary hidden-consumer interface refactor stays `X1 / X3 near-tie`. Staged interface/API remains X1-primary after N35/N36; compact operator-budget API migration remains X3-primary after N57. |
| tooling note | Fixed v2 runner cache classification: top-level `.pytest_cache/` and `.mypy_cache/` are now auxiliary generated artifacts, matching nested cache handling. |
| latest artifacts | `Scenarios-v2/N83-interface-refactor-breakage-hunt/`, `Work/next-upgraded-pack/Evidence/n83-interface-refactor-breakage-rubric-2026-04-28.json`, and the W61 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Completed through W65/N86; next action is W66/N87 performance-review gate. |

## 2026-04-28 W62 update

| Topic | Current read |
|---|---|
| security review reproduction | `N84` is diagnostic W62: one JSON review report with nine exact security findings, exploit reproduction binding to `R1..R9`, false-positive suppression for `B1..B3`, exact `REVISE` gate decision, and exact changed-path scope. |
| result | `X1 / gpt-5.5` passes; `X3 / opus 4.7max` also passes. Both wrappers exited `0`, both changed only `candidate/review-report.json`, and both passed `check_security_repro_review.py`. |
| routing impact | `binary tie remains`; ordinary single-session security review stays `X1 / X3 near-tie`. Staged security implementation/re-entry remains `X1 primary` after N78. |
| latest artifacts | `Scenarios-v2/N84-security-review-repro-gauntlet/`, `Work/next-upgraded-pack/Evidence/n84-security-repro-rubric-2026-04-28.json`, and the W62 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Completed through W65/N86; next action is W66/N87 performance-review gate. |

## 2026-04-28 W63 update

| Topic | Current read |
|---|---|
| performance runtime budget | `N85` is diagnostic W63 and promoted `/40` replacement for N59: hidden quote-pricing correctness, measured batch runtime, exact patch scope, evidence JSON, and hard `worker-output.txt <= 40000` operator budget. |
| result | `X1 / gpt-5.5` is a scoreable `FAIL`: hidden correctness/runtime/scope pass, but operator output is `266051 > 40000`. `X3 / opus 4.7max` is `PASS`: runtime `0.014301s <= 0.7s`, output `1827 <= 40000`, scope passes. |
| calibration | `X2` is scoreable `FAIL` with no patch, runtime `3.508s > 0.7s`, evidence and scope failures. `X6` is `NOT-RUN` due Gemini CLI `UNSUPPORTED_LOCATION` auth/route failure before model work. `X4` remains final-only; X5 is parked. |
| routing impact | compact performance hot-path work is now `X3 primary`. Staged systems/performance recovery remains `X1 primary` after N39. |
| latest artifacts | `Scenarios-v2/N85-performance-review-runtime-budget/`, `Work/next-upgraded-pack/Evidence/n85-performance-runtime-rubric-2026-04-28.json`, and the W63 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Continue with W66/N87 performance-review gate. W64 promotion review stays parked until a named replacement decision is admitted. |

## 2026-04-28 W65 update

| Topic | Current read |
|---|---|
| real interface downstream migration | `N86` is diagnostic W65: BillingMesh API migration without operator-output budget, hidden repo consumers, hidden downstream public-app contract, dataclass result serialization, source-bound ledgers, review response, closeout, and exact 12-path migration scope. |
| result | `X1 / gpt-5.5` passes. `X3 / opus 4.7max` wrapper exits `0` and passes hidden interface/downstream semantics, but scoreably fails exact scope because `candidate/workspace/src/billingmesh/api.py` is missing from changed paths. |
| routing impact | X1-leaning evidence for real interface migration when exact public-API migration surface is first-class. This is not an output-budget separator and not a hidden downstream semantic fail. |
| promotion | Diagnostic-only. Do not change `full-v2-hard /40` until a later replacement decision names the outgoing slot. |
| latest artifacts | `Scenarios-v2/N86-real-interface-downstream-migration/`, `Work/next-upgraded-pack/Evidence/n86-interface-downstream-rubric-2026-04-28.json`, and the W65 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | W66/N87 performance-review gate: read-only performance architecture review without output budget, based on benchmark admissibility and cache-boundary diagnosis. |

## 2026-04-28 W66 update

| Topic | Current read |
|---|---|
| performance-review gate | `N87` is diagnostic W66: read-only performance architecture review over warm-cache benchmark contamination, cache-key context loss, global cache lifetime growth, approval-gate incompleteness, false-positive restraint, author responses, and exact `REVISE` gate. |
| result | `X1 / gpt-5.5` passes. `X3 / opus 4.7max` also passes. Both wrappers exit `0`, both pass `check_performance_review_gate.py`, and both pass exact five-artifact scope. |
| routing impact | `binary tie remains`; performance-review gate work without output budget is top-pair near-tie. X3 is much more compact, but compactness is not a binary winner here. |
| promotion | Diagnostic-only. Do not replace N07 in `full-v2-hard /40` from this result. |
| latest artifacts | `Scenarios-v2/N87-performance-review-gate/`, `Work/next-upgraded-pack/Evidence/n87-performance-review-gate-rubric-2026-04-28.json`, and the W66 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | W67/N88 UX runtime event-policy simulator. |

## 2026-04-28 W67 update

| Topic | Current read |
|---|---|
| UX runtime event-policy simulator | `N88` is diagnostic W67: hidden runtime event-policy simulation over stale source, owner, regression-proof, export, breakpoint, and reentry states. |
| result | `X1 / gpt-5.5` passes at `100 / 100`; `X3 / opus 4.7max` also passes at `100 / 100`. Both wrappers exit `0` and change only the three UX policy JSON files. |
| routing impact | `binary tie remains`; hidden UX policy simulation is stronger than N82 but still top-pair near-tie. |
| promotion | Diagnostic-only. Do not replace N02 or another `/40` UX slot from this result. |
| latest artifacts | `Scenarios-v2/N88-ux-runtime-event-policy-simulator/`, `Work/next-upgraded-pack/Evidence/n88-ux-runtime-policy-rubric-2026-04-28.json`, and the W67 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | W68/N89 security runtime witness review. |

## 2026-04-28 W68 update

| Topic | Current read |
|---|---|
| security runtime witness review | `N89` is diagnostic W68: review-only security report with executable runtime witness rows, protected target hashes, opaque finding-tuple digests, and false-positive cardinality. |
| result | `X1 / gpt-5.5` passes at `100 / 100`; `X3 / opus 4.7max` also passes at `100 / 100`. Both wrappers exit `0` and change only `candidate/review-report.json` as benchmark surface. |
| routing impact | `binary tie remains`; ordinary single-session security review remains top-pair near-tie even with executable witness binding. Staged security implementation/re-entry remains `X1 primary` after N78. |
| promotion | Diagnostic-only. Do not keep hardening ordinary single-session security review without a new contract axis. |
| latest artifacts | `Scenarios-v2/N89-security-review-runtime-witness-gauntlet/`, `Work/next-upgraded-pack/Evidence/n89-security-runtime-witness-rubric-2026-04-28.json`, and the W68 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | W69/N90 staged UX review/reentry. |

## 2026-04-28 W69 update

| Topic | Current read |
|---|---|
| staged UX review/reentry | `N90` is diagnostic W69: review-only staged UX source/state, ADR, findings, response gate, and closeout over a protected publish-console runtime target. |
| result | `X1 / gpt-5.5` passes at `100 / 100`; `X3 / opus 4.7max` also passes at `100 / 100`. Both wrappers exit `0` and pass exact five-artifact benchmark scope; X3 has auxiliary target `__pycache__` only. |
| routing impact | `binary tie remains`; staged UX review/reentry is top-pair near-tie. Staged UI implementation remains `X1 primary` after N79; UX policy/review without implementation or output budget should not be used as a separator. |
| promotion | Diagnostic-only. Do not replace N02, S30, or another canonical `/40` UX review slot from this result. |
| latest artifacts | `Scenarios-v2/N90-staged-ux-review-reentry-gate/`, `Work/next-upgraded-pack/Evidence/n90-staged-ux-review-rubric-2026-04-28.json`, and the W69 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Select W70 from the roadmap. Prefer a new task family such as real-repo staged interface/incident work over more review-only hardening. |

## 2026-04-29 W70 update

| Topic | Current read |
|---|---|
| real-repo staged security incident | `N91` is diagnostic W70: staged security incident repair over tenant-bound export authorization, HMAC-bound download tokens, redirect allowlisting, audit redaction, regression tests, and incident/reentry/closeout ledgers. |
| result | `X1 / gpt-5.5` passes at `100 / 100`; `X3 / opus 4.7max` also passes at `100 / 100`. Both wrappers exit `0` and pass exact ten-artifact benchmark scope. |
| routing impact | `binary tie remains`; N91 narrows N78. Use N78 only for its synthetic staged-security contract; do not assume all real-repo staged security repair is X1-primary. |
| promotion | Diagnostic-only. Do not replace N78 or another canonical `/40` slot from this result. |
| latest artifacts | `Scenarios-v2/N91-realrepo-staged-security-incident-reentry/`, `Work/next-upgraded-pack/Evidence/n91-staged-security-incident-rubric-2026-04-29.json`, and the W70 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | W71 should switch to real-repo staged interface/downstream reentry or another implementation axis; avoid more security/review-only hardening unless a new scoreable contract is admitted. |

## 2026-04-29 W71 update

| Topic | Current read |
|---|---|
| staged interface/downstream reentry | `N92` is diagnostic W71: new SubscriptionMesh domain with staged source/migration/reentry ledgers, public package facade, legacy-event adapter, clean-room downstream SDK import, and exact fifteen-path scope. |
| result | `X3 / opus 4.7max` passes at `100 / 100`. `X1 / gpt-5.5` is `NOT-RUN / REQUEUE`: Codex worker hit usage limit before edits, produced no changed paths, and worker output says retry after `4:51 AM`. |
| routing impact | No top-pair verdict yet. Do not classify X1 as a model fail from the starter verifier; the failed verifier ran against unchanged starter code after quota exit. |
| promotion | Pending. Do not change canonical `/40` from N92 unless a complete X1/X3 result and replacement decision are admitted. |
| latest artifacts | `Scenarios-v2/N92-staged-interface-downstream-reentry-gauntlet/`, `Work/next-upgraded-pack/Evidence/n92-staged-interface-reentry-rubric-2026-04-29.json`, and the W71 section in `Evidence/x1-mainline-hardening-no-new-failures-2026-04-21.md` |
| next action | Retry `X1` on `N92` after Codex quota reset; then update W71 verdict and decide W72. |
