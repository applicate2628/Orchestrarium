# N105 Staged Screenshot Diff Review

`N105` targets `L12 review.ui-visual-correctness` with a baseline/current screenshot pair and a
release-gate decision. The worker must identify the actual visual regressions, avoid intentional
non-findings, and return a staged re-entry decision that is grounded in the screenshot evidence.

This is diagnostic role-fit evidence for X1/X3 visual review separation unless a later slot
replacement decision promotes it into the `/40` denominator.
