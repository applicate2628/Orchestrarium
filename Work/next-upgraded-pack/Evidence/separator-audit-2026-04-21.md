# Separator Audit — Scenarios-v2 Structural Capability Read

- Date: 2026-04-21
- Owner: `$lead`
- Status: PASS
- Gate: PASS
- Scope: factual read-only audit across all 43 scenarios in `D:\dev\Orchestrator\benchmarks\Scenarios-v2\` (`S01..S33` plus `N01..N13`).
- Purpose: classify which scenarios are structurally capable of separating near-ceiling models `X1` (`gpt-5.4`) and `X3` (`opus 4.7max`) once answer-leak removal and verifier tightening are considered. No fixes, no plans, no code changes.
- Evidence tier: canonical scenario files only (`scenario.yaml`, `inputs/task.md`, `oracle/*-contract.json`, `verifiers/check_*.py`, selected `inputs/*.md` and `oracle/expected-*.md`).

## Classification table

| id | role_class | answer_leak_level | answer_leak_evidence | verifier_strictness | verifier_key_checks | compliance_retelling_path | separation_potential | recommended_intervention | notes |
|---|---|---|---|---|---|---|---|---|---|
| S01 | owner | high | `inputs/initiative-cards.md:3-52` names all five initiatives and labels the required "Risk if delayed"; `inputs/task.md:18-19` says "choose and sequence the next two roadmap priorities"; `oracle/roadmap-contract.json:73-81` hard-codes the two expected priority headings and the three deferred headings | keyword-substring | `oracle/roadmap-contract.json:82-87 required_exact_lines`; `oracle/roadmap-contract.json:88-117 required_keyword_groups` (`contains_any` in `verifiers/check_roadmap_decision_package.py:89-91`); `verifiers/check_roadmap_decision_package.py:165-169` exact-line matching; `verifiers/check_roadmap_decision_package.py:171-176` keyword-group coverage | Use `inputs/initiative-cards.md` wording, copy the three mandatory exact lines from `oracle/roadmap-contract.json:82-87`, use the two required priority headings, add the three required deferred headings, end with `APPROVED FOR ROADMAP`. | high | leak-removal | Priority identity is fully fixed by the contract; leak removal only helps if it stops naming the exact heading strings |
| S02 | owner | high | `inputs/task.md:20-25` directly states resume point, prior accepted artifact, next role, later gate, and the side-request closure outcome; `oracle/recovery-contract.json:22-25` mirrors the same answers | keyword-substring | `verifiers/check_recovery_packet.py:83-107` checks `brief.md` and `status.md` for literal phrases; `verifiers/check_recovery_packet.py:108-116` ensures handoff contains the exact `$qa-engineer` string and all required sections | Paraphrase the 5 bullets from `inputs/task.md:20-25`, put `Phase 1 - Bootstrap Scenarios-v2 with S02` in brief, `Current stage: QA` and `Side request status: closed` in status, put `$qa-engineer` plus the 12 required sections in handoff. | medium | contract-redesign | The recovery answer is essentially dictated by task.md; only a harder interruption-record with conflicting evidence can separate |
| S03 | advisory | high | `oracle/consultant-contract.json:88 required_recommended_option` hard-codes "Option A - one local advisory pilot first"; `oracle/consultant-contract.json:71-75` fixes Option A/B/C headings; `oracle/consultant-contract.json:76-84` lists provenance lines verbatim | keyword-substring | `verifiers/check_consultant_memo.py:154-192` substring section checks; `verifiers/check_consultant_memo.py:176-181 contains_any` keyword groups; `verifiers/check_consultant_memo.py:182-192` section-term substring check | Copy the 7 required exact lines from `oracle/consultant-contract.json:76-84`, include the 3 required option headings, state "Option A" as recommended, end with `NON-BLOCKING` and a `Continue working:` line. | low | drop-from-separator-set | Recommendation is hard-coded; even strong reasoning cannot separate from trivial compliance |
| S04 | hygiene | high | `inputs/task.md:8-14` names `docs/benchmarks/README.md`, `source-of-truth-index.md`, `archive/2026-03-transition-notes.md`, the `$architecture-reviewer` escalation route, and the valid outcomes; `oracle/source-of-truth-contract.json:70-80` fixes the three target headings | keyword-substring | `verifiers/check_source_of_truth_update_packet.py:162-173` substring section and canonical-source checks; `verifiers/check_source_of_truth_update_packet.py:175-181 contains_any`; `verifiers/check_source_of_truth_update_packet.py:182-192` per-section term substrings | Reproduce the three target headings, paraphrase the three reconciliation bullets, mention `$architecture-reviewer`, pick one of the three outcomes. | medium | leak-removal | Removing the target enumeration in task.md would reinstate real reconciliation reasoning |
| S05 | factual | medium | `inputs/bounded-scope.md:3-7` and `inputs/task.md:1-12` frame the goal abstractly; `oracle/product-brief-contract.json:56-65` fixes seven required section headings; `oracle/product-brief-contract.json:82-114` pins specific section terms such as `milestone 1`, `role-first`, `v2 results`, `near-tie`, `handling` | keyword-substring | `verifiers/check_product_brief.py:117-134` section, marker, heading, anchor, and section-term substring checks; `verifiers/check_product_brief.py:135-139` requires literal `PASS` gate | Use the 7 required sections, paste the required-term bag from the contract, include the 4 anchor terms `milestone 1`, `open question`, `constraint`, `success`, end with `PASS`. | medium | verifier-tightening | Concept lattice is narrow enough that a model that understood the intake can pass without naming the canonical brief text |
| S06 | factual | medium | `inputs/task.md:3-14` lists the 5 investigation questions abstractly; `oracle/fact-contract.json:82-99` pins six concrete reference paths and three "false lead" terms plus two unknown-phrase fragments; `oracle/expected-findings.md:1-44` enumerates every confirmed fact and false lead in full | keyword-substring | `verifiers/check_factual_memo.py` requires `minimum_repo_references`, literal path anchors from `required_reference_anchors`, substring `required_false_lead_terms`, substring `required_unknown_terms`, per-section term substrings | Cite the six fixed paths (they are in the contract), list the three false leads by filename substring, include the two fixed unknown phrases, end with `PASS`. Deep repo reading not required. | high | leak-removal | If the verifier switched to tuple-exact `(path, line, category)` and stopped pinning specific filenames, real investigation would be required |
| S07 | design | high | `oracle/admissible-seams.md:1-30` explicitly names Option B as "Strong-pass seam" and lists the two non-admissible options with exact reasoning; `inputs/competing-seam-options.md` pre-frames the trade | keyword-substring | `verifiers/check_design_packet.py` uses substring section, term, and `contains_any` checks (13 substring matches detected); no machine-checkable JSON equality | Name Option B as the chosen seam, reject Options A and C with the reasoning from `oracle/admissible-seams.md`, use the required section headings, mention "universal `scenario.yaml`" and "bundle self-containment". | medium | leak-removal | Removing `oracle/admissible-seams.md` from the compliance surface (or keeping it out of the task-visible tree) is the main unlock |
| S08 | design | medium | `inputs/task.md:14-42` frames the cross-surface problem abstractly; oracle contract likely pins terminology but is not as enumerative as S07 | keyword-substring | `verifiers/check_ux_structure_brief.py` — 12 substring occurrences, section-plus-term substring style consistent with S05 | Follow the four restructure bullets from task.md, name the two surfaces, include "handoff" and "state model" substrings. | medium | verifier-tightening | Quality of structural thinking can diverge in principle, but verifier sees only term presence |
| S09 | planning | high | `oracle/expected-phase-order.md:6-30` enumerates all three expected phases and their exact names; `inputs/task.md:14-31` names the owner seam `tools/status_snapshot.py`, direct test file, output filename, preserved behaviors, and the no-write guarantee | keyword-substring | `verifiers/check_phase_plan.py` — 15 substring-style matches; section-plus-keyword gating | Copy the three phase titles from `oracle/expected-phase-order.md`, cite the four preserved surfaces, include `PASS`. | medium | leak-removal | The phase structure is effectively given; only a harder planning problem with multiple valid phase orderings can separate |
| S10 | design | low | `inputs/task.md:19-31` names the 10 required sections abstractly; evidence IDs `E1..E5` are referenced without enumerating the answer; `inputs/failure-traces.md` and `inputs/candidate-approach-notes.md` are domain data | keyword-substring | `verifiers/check_algorithm_invariant_proof_memo.py` — substring memo_lower matching at `:114`, `:144`; section and phrase coverage | Present the 10 mandated sections with generic algorithm-scientist prose referencing `E1..E5`, end with `PASS`. | high | verifier-tightening | Strong algorithm reasoning could differ across models, but verifier cannot detect correctness — only that the 10 sections and E-IDs appear |
| S11 | design | low | `inputs/task.md:19-30` names the 10 memo sections abstractly; validation data is domain-specific and does not dictate answers | keyword-substring | `verifiers/check_model_validation_memo.py:141, :169` — substring matching of phrases against the full memo lower-cased | Same compliance-retelling: 10 sections, cite `E1..E5`, note "uncertainty" and "governing equations", end `PASS`. | high | verifier-tightening | Thermal model reasoning could separate models if numerical check ran; as shipped, only keyword coverage is enforced |
| S12 | design | low | `inputs/task.md:19-30` abstractly names the 11 package sections; evidence E-IDs referenced without enumeration; `inputs/threat-boundary-hints.md` and `inputs/auth-flow-notes.md` provide raw material | keyword-substring | `verifiers/check_security_constraint_package.py:120-133` substring ID existence checks for `required_boundary_ids`, `required_abuse_case_ids`, `required_control_ids`, `required_must_fix_ids`, `required_verification_ids`; minimum claim count | Include all required trust-boundary and control IDs as substrings (likely discoverable from inputs), 11 sections, numbered claims, end `PASS`. | high | verifier-tightening | Real threat-modeling differences are invisible to the verifier |
| S13 | design | low | `inputs/task.md:19-30` names the 9 package sections; workload summary is domain data | keyword-substring | `verifiers/check_performance_constraint_package.py:141, :171` substring phrase coverage | Follow the 9 section template, cite `E1..E5`, mention the four required boundaries, end `PASS`. | high | verifier-tightening | Budget-envelope reasoning cannot be distinguished by a substring verifier |
| S14 | design | low | `inputs/task.md:2-5` abstract; `inputs/rollout-envelope.md`, `failure-mode-notes.md` provide raw data | keyword-substring | `verifiers/check_reliability_constraint_package.py:100-112` section + marker + expected gate substring matching | Produce the template sections naming stages, failure modes, rollback triggers, observability; end `PASS`. | medium | verifier-tightening | Rollout reasoning could diverge, but the verifier cannot observe it |
| S15 | implement | low | `inputs/task.md:1-4` names only the rule themes (revocation, cutoff, dedupe) | structural-json-equality (functional) | `verifiers/run_backend_checks.py:138` runs `pytest` via `subprocess.run`; `verifiers/check_scope.py` enforces allowed change surface; functional truth is in tests | No retelling path — code must actually pass the pytest suite. | high | drop-from-separator-set | Already a real functional separator; near-ceiling models could still both pass if the backend rules are learnable from the existing test |
| S16 | implement | low | `inputs/task.md:20-30` names behavioural requirements (aria-pressed, live-region, etc.) abstractly | structural-json-equality (functional) | `verifiers/check_s16_frontend_bundle.py:6 subprocess`; `:85 sha256 protected_candidate_hashes`; runs `node scripts/validate_...` | Cannot retell — UI patch must make the validator pass. | high | drop-from-separator-set | Real functional separator; retains separating potential if task specifics are hardened |
| S17 | implement | low | `inputs/task.md:18-29` names Qt dialog contract abstractly | structural-json-equality (functional) | `verifiers/run_qt_ui_checks.py:6 importlib`, `:8 subprocess`, `:97 import_module`, `:166 subprocess.run`; direct Qt widget interaction tests | No retelling path — Qt focus and tab behavior must actually work. | high | drop-from-separator-set | Already a real functional separator |
| S18 | implement | low | `inputs/task.md:1-5` names owner rules abstractly | structural-json-equality (functional) | `verifiers/run_model_view_checks.py:6 importlib.util`, `:8 subprocess`, `:143 subprocess.run`; test harness | No retelling — proxy ordering and fallback selection must pass tests. | high | drop-from-separator-set | Real functional separator |
| S19 | implement | low | `inputs/task.md:19-24` lists required outcomes abstractly (relation name, grain, dedupe, metric columns) | structural-json-equality (functional) | `verifiers/check_s19_pipeline_bundle.py:8 subprocess`; `:85 sha256`; `:103 subprocess.run scripts/validate_customer_day_rollup.py` | SQL must compile and produce the right rollup; no retelling path. | high | drop-from-separator-set | Real functional separator |
| S20 | implement | low | `inputs/task.md:10-19` specifies the exact required config end-state (/metrics endpoint, resource-before-batch, OTLP URL, namespace, env) | structural-json-equality (functional) | `verifiers/check_s20_platform_bundle.py:8 subprocess`, `:85 sha256`, `:103 subprocess.run validate_observability_patch.py` | Task.md already names the exact values — a near-ceiling model types them in. | medium | leak-removal | Though functional, the end-state is so fully leaked in task.md that the functional bar does not bite for near-ceiling models |
| S21 | implement | low | `inputs/task.md:19-25` names specific publish-surface, validation route, and legacy removals | structural-json-equality (functional) | `verifiers/check_s21_toolchain_bundle.py:6 subprocess`, `:85 sha256 protected_candidate_hashes`, `:227 subprocess.run` | Task.md names every concrete change needed; no separation beyond compliance. | medium | leak-removal | Same shape as S20: leak + functional check defaults to "type the answer in" |
| S22 | implement | low | `inputs/task.md:21-26` states the geometric invariants abstractly (right-handed convention, scale-aware signed-area tolerance) | structural-json-equality (functional) | `verifiers/run_geometry_checks.py:90 load truth-table.json`, `:113-130 evaluate_cases` functional predicate cases; exact boolean match against `oracle/truth-table.json` | No retelling path — predicate must actually work on edge cases. | high | drop-from-separator-set | Real functional separator — already tuple-style check on truth table |
| S23 | implement | low | `inputs/task.md:19-26` states ordering and compositing rules abstractly | structural-json-equality (functional) | `verifiers/run_graphics_checks.py:6 importlib.util`, `:8 subprocess`, `:203 subprocess.run`; oracle frames checked deterministically | Frames must actually render correctly — no retelling. | high | drop-from-separator-set | Real functional separator |
| S24 | implement | low | `inputs/task.md:19-26` states visualization invariants abstractly (zero-centered, signed diverging, depth orientation, gaps semantics) | structural-json-equality (functional) | `verifiers/run_visualization_checks.py:6 importlib.util`, `:8 subprocess`, `:206 subprocess.run`; oracle `encoding-oracle.json` structural equality | Section spec must match oracle JSON exactly. | high | drop-from-separator-set | Real functional separator |
| S25 | review | high | `inputs/task.md:9-15` enumerates AC1..AC4 and their exact criteria; `inputs/task.md:17-23` enumerates required QA behaviors | keyword-substring | `verifiers/check_qa_verdict.py` — 14 substring matches; section + term coverage + gate decision substring | Map AC1..AC4 from task.md directly, cite evidence files named in task, pick one of the three gate decisions. | medium | contract-redesign | QA verdict shape is too close to dictation — redesign the acceptance-criteria shape |
| S26 | review | medium | `inputs/known-risks.md` plus `inputs/accepted-design.md` describe architecture drift to find; task.md frames it abstractly but mentions "overlay_flags: []" and `SEVERITY_LABELS` as decoys | keyword-substring | `verifiers/check_architecture_review.py` — substring findings + term coverage; 14 matches | Echo known-risks wording, keep the two explicit decoy exclusions, order by severity, choose gate. | medium | leak-removal | Real architecture judgment is needed to avoid false positives but the verifier still checks substrings only |
| S27 | review | high | `inputs/review-observations.md:1-6` directly enumerates all three findings (`localStorage` token, visible token text, `postMessage "*"`); `inputs/secret-and-boundary-notes.md` provides the framing | keyword-substring | `verifiers/check_security_review.py` — 10 substring matches | Paraphrase the three bullets from `review-observations.md`, mark severity, cite `bounded-diff.patch` lines, end gate. | high | leak-removal | Strong candidate — removing `review-observations.md` and `secret-and-boundary-notes.md` forces actual analysis of the diff |
| S28 | review | high | `inputs/review-observations.md:1-6` directly enumerates the three findings (sort on every render, JSON serialize, unbounded metric history); `inputs/latency-and-memory-notes.md:1-6` reinforces | keyword-substring | `verifiers/check_performance_review.py` — 4 substring matches (minimal) | Paraphrase observations, mark severity, cite evidence, end gate. | high | leak-removal | Strong candidate for separator — remove observations file to force diff-level reasoning |
| S29 | review | high | `inputs/keyboard-and-at-observations.md:5-26` enumerates the seeded keyboard-walk findings and screen-reader findings exhaustively; task.md pins the finding priority order at `inputs/task.md:15-17` | keyword-substring | `verifiers/check_accessibility_review.py` — 9 substring matches; priority order substring gating | Paraphrase the keyboard-walk bullets in the required priority order, mark severity, end gate. | high | leak-removal | Strong candidate — remove observations file to force real a11y analysis |
| S30 | review | high | `inputs/review-observations.md:1-5` names the three UX defects (`Ship` label, `More` label, immediate close); verifier pins exactly three findings | keyword-substring | `verifiers/check_ux_review.py:139-194` — substring section + term + severity + evidence ledger rows + false-positive rows; 21 matches | Produce exactly three findings with the four required labels, fill the two required tables with terms from review-observations, end REVISE. | medium | leak-removal | Already hardened with table structure but answer content is still enumerated in inputs |
| S31 | review | medium | `inputs/task.md:9-16` frames verification abstractly; `inputs/keyboard-focus-run.md`, `high-dpi-captures.md`, `theme-variance.md` provide mixed observations | keyword-substring | `verifiers/check_ui_test_report.py` — 9 substring matches; section + stable-vs-regressed + gate | Separate stable checks from regressions by copying observations into the correct buckets, cite evidence paths, end gate. | medium | leak-removal | Good candidate for hardening because the stable-vs-regression decision is currently cuey |
| S32 | implement | low | `inputs/task.md:21-28` names provenance labels and verdict requirements abstractly; `oracle/provenance-contract.json` pins the allowed forms | keyword-substring | `verifiers/check_transport_report.py` — 5 substring matches; checks provenance label presence | Copy the provenance template lines, name the selected provider, mention "no internal fallback". | low | drop-from-separator-set | Transport-only compliance task; cannot separate reasoning quality |
| S33 | review | low | `inputs/task.md:23-28` names provenance labels and review-strategy verdict requirements abstractly | keyword-substring | `verifiers/check_transport_report.py` — 5 substring matches; same provenance-label pattern | Same as S32 — copy template, name provider and strategy, avoid finding language. | low | drop-from-separator-set | Transport-only compliance task; cannot separate reasoning quality |
| N01 | design | medium | `inputs/task.md:14-19` frames static-hierarchy goals abstractly; no direct enumeration of answer | keyword-substring | `verifiers/check_static_visual_hierarchy_brief.py` — 12 substring + ordered-headings + section-term pattern | Produce the UX structure sections named in task, include the anchors for "primary/secondary/deferred", align naming, end with a status. | high | verifier-tightening | Real hierarchy design could separate models if the verifier were structural, but substring coverage flattens differences |
| N02 | design | medium | `inputs/task.md:14-26` requires a state-transition Markdown table with fixed columns `Source failure`, `Proposed state response`, `Owner`, `Visible return cue`; content is not enumerated | mixed | `verifiers/check_interaction_state_flow_brief.py` — 20 substring + table-header checks and tuple-matching patterns | Produce the fixed-column table, populate with plausible paraphrases of the input failures, include required terms. | high | verifier-tightening | Good candidate — already one of the current top-pair diagnostic slice members; tightening table-row semantic checks could extend separation |
| N03 | review | medium | `inputs/task.md:18-33` frames review abstractly and explicitly names 3 decoys (MAX_CHANGED_PATHS, sha1 fingerprint, ReviewPacketView dataclass) as not-findings; `inputs/bounded-diff.patch` holds the real diff | keyword-substring | `verifiers/check_generic_review.py` — 18 substring + tuple match style | Find the 3 real issues in the diff; avoid the 3 named decoys; match severity anchors; end gate. Near-ceiling models avoid decoys easily. | high | verifier-tightening | Real correctness/diagnosability reasoning needed; tightening could pull separation |
| N04 | review | medium | `inputs/task.md:9-17` frames triage abstractly, names severity anchors and recheck scope | mixed | `verifiers/check_regression_triage.py` — 19 substring + tuple match style | Order the likely regressions, cite evidence, name recheck scope, end gate. | medium | verifier-tightening | Triage priority judgment could diverge but is indirectly checked |
| N05 | review | high | `inputs/review-observations.md:1-6` directly enumerates three findings (export token in URL, telemetry with raw token and email, raw email rendering) | keyword-substring | `verifiers/check_secret_exposure_review.py` — 10 substring + tuple-style matches | Paraphrase the three bullets, cite evidence, end gate. | high | leak-removal | Strong candidate for separator after removing observations file |
| N06 | review | high | `inputs/review-observations.md:1-6` directly enumerates three findings (query-string role, trusted boolean, forwarded client role) | mixed | `verifiers/check_authz_trust_review.py:142-169` substring + distinct finding-block pattern; 10 substring matches | Same retelling: paraphrase the three bullets, mark severity, end gate. | high | leak-removal | Strong candidate — removing observations file forces real trust-boundary reasoning |
| N07 | review | high | `inputs/review-observations.md:1-6` directly enumerates three findings (rescans every lane, duplicated lane-basis membership, unbounded snapshot history) | keyword-substring | `verifiers/check_cross_review.py` — 7 substring matches | Paraphrase three bullets, mark severity, end gate. | high | leak-removal | Strong separator candidate if inputs hardened |
| N08 | implement | low | `inputs/task.md:1-10` names the abstract build-owner continuity goal; no answer enumeration | structural-json-equality (functional) | `verifiers/check_long_autonomous_build_owner.py:6 subprocess`, `:125 node --test`, `:126 node scripts/verify-build.js`; returncode-based pass/fail | Cannot retell — actual Node tests must pass. | high | drop-from-separator-set | Real functional separator; already part of top-pair diagnostic slice (tied 5/5 currently) |
| N09 | implement | low | `inputs/task.md:1-5` names the recall-preference rule abstractly; no enumeration | structural-json-equality (functional) | `verifiers/check_autonomous_resume_path_recall.py:6 subprocess`, `:117 node --test` | Must pass node tests; no retelling. | high | drop-from-separator-set | Real functional separator |
| N10 | implement | low | `inputs/task.md:1-8` names the multi-step append rule abstractly; no enumeration | structural-json-equality (functional) | `verifiers/check_constrained_multi_step_patch.py:6 subprocess`, `:121 node --test` | Must pass node tests; no retelling. | high | drop-from-separator-set | Real functional separator |
| N11 | design | medium | `inputs/task.md:12-23` frames ADR hardening requirements abstractly, but enumerates 7 structural requirements including exact tables and JSON block; `oracle/architecture-conflict-contract.json:machine_decision` contains the expected JSON decision | mixed | `verifiers/check_architecture_evidence_conflict.py:157-202` — substring sections, table header equality, `required_evidence_bindings` with per-row term substring, forbidden-direction table row matching, per-claim `Verification:`/`Regression:` requirement, and `parsed_decision == expected_decision` JSON equality; 20 matches | Fill the required tables, fill the two per-claim labels, include the `providerRoutes` term, and produce a JSON object matching `oracle.machine_decision`; the JSON is the hardest gate. | high | verifier-tightening | Strong current top-pair member — already has structural JSON equality; further separation needs genuine decision-variance evidence |
| N12 | factual | medium | `inputs/task.md:10-29` names 14 exact required citations (`config/agents-mode.defaults.yaml:1`, `:10`, `:11`, etc.) and a legacy-top-pair rejection label; `inputs/source-rules.md:3-13` ranks source authority explicitly | keyword-substring | `verifiers/check_source_truth_conflict_memo.py` — 18 substring matches; citation presence + non-directional phrasing + `superseded_result` label + required tables | Copy the 14 listed citations, follow source-rules ranking, mark `legacy-top-pair.md` as `superseded_result`, add the two required tables. | medium | contract-redesign | Already part of top-pair diagnostic slice; a harder version would hide the authority order |
| N13 | review | medium | `inputs/task.md:8-26` names exactly 5 findings, the four label types, the causal ledger header, and files to inspect | mixed | `verifiers/check_adversarial_review_traps.py:128-177` — exact 5-finding count, per-finding severity + distinct block, false-positive term substring, required-table-header substring, causal-ledger table header equality, per-row term substring; 21 matches | Produce exactly 5 finding blocks with the 4 labels, build the causal ledger with the exact header, include required decoy-avoidance terms. | high | verifier-tightening | Current top-pair member; already enforces table-row structure; further separation needs decoy-hardening in the reviewed code, not in the contract |

## Synthesis

### Counts

Answer-leak bucket:
- high: 16 scenarios (S01, S02, S03, S04, S07, S09, S25, S27, S28, S29, S30, N05, N06, N07)
  plus `S06` sits on the border — the contract pins specific paths and phrases rather than naming all answers in task.md; counted as medium above for consistency with the citation-phrased leak, and 2 scenarios `S20`, `S21` have the full answer in task.md — if these are bucketed as high the count is 18.
- medium: 12 scenarios (S05, S08, S26, S31, N01, N02, N04, N11, N12, and borderline S06, S20, S21)
- low: 15 scenarios (S10, S11, S12, S13, S14, S15, S16, S17, S18, S19, S22, S23, S24, S32, S33, N08, N09, N10)
  — 16 if we keep S20 and S21 at low, 18 if conservative.

Verifier-strictness bucket:
- keyword-substring: approximately 23 scenarios — all the memo/brief/plan/review classes (S01-S14, S25-S33 except where noted, N01, N03, N05, N07, N12)
- mixed: approximately 6 scenarios (N02, N04, N06, N11, N13, S30) — substring plus table-header equality, tuple-matching, or JSON equality
- structural-json-equality (functional): 12 scenarios (S15, S16, S17, S18, S19, S20, S21, S22, S23, S24, N08, N09, N10)
- tuple-exact: 0 scenarios — no verifier currently requires `(file, line, category)` tuple equality at the finding level

Separation-potential bucket:
- high: 25 scenarios (S01, S06, S10, S11, S12, S13, S15, S16, S17, S18, S19, S22, S23, S24, S27, S28, S29, N01, N02, N03, N05, N06, N07, N08, N09, N10, N11, N13)
- medium: 13 scenarios (S02, S04, S05, S07, S08, S09, S14, S20, S21, S25, S26, S30, S31, N04, N12)
- low: 4 scenarios (S03, S32, S33, plus S14 sits near the low/medium boundary)

### Top 5-8 leak-removal unlocks (high separation × high leak)

These scenarios are structurally able to separate near-ceiling models once the answer-enumerating inputs are removed or rewritten:

1. **S27** (`security-review-findings`): `inputs/review-observations.md:1-6` directly enumerates all three findings. Removing that file and forcing analysis from `bounded-diff.patch` would reinstate real security-boundary reasoning as the gating signal.
2. **S28** (`performance-review-findings`): `inputs/review-observations.md:1-6` and `latency-and-memory-notes.md:1-6` enumerate the three findings. Same intervention.
3. **S29** (`accessibility-review-findings`): `inputs/keyboard-and-at-observations.md:5-26` contains an exhaustive keyboard-walk and screen-reader trace. Removing the enumerated walk forces real a11y analysis.
4. **N05** (`secret-exposure-review`): `inputs/review-observations.md:1-6` directly names the three findings. Same pattern.
5. **N06** (`authz-trust-boundary-review`): `inputs/review-observations.md:1-6` directly names the three findings. Same pattern.
6. **N07** (`scalability-maintainability-cross-review`): `inputs/review-observations.md:1-6` directly names the three findings. Same pattern.
7. **S06** (`analyst-repository-fact-memo`): The verifier pins six exact repository paths and three specific "false lead" filenames as substring targets, and `oracle/expected-findings.md:1-44` enumerates every confirmed fact. Switching to tuple-exact `(path, symbol, category)` evidence checks with a hidden-from-candidate expected-findings file would force real slice investigation.
8. **S01** (`product-manager-roadmap-prioritization`): `inputs/initiative-cards.md:3-52` explicitly frames which two initiatives are launch-critical and which three should defer. Rewriting `initiative-cards.md` so multiple defensible prioritizations exist and the verifier accepts any valid one would separate on prioritization judgment.

### Structurally hopeless candidates (drop from diagnostic slice)

These scenarios cannot plausibly separate near-ceiling models regardless of hardening because the task is intrinsically compliance with a template:

1. **S03** (`consultant-tradeoff-memo`): The recommended option is hard-coded to "Option A - one local advisory pilot first" in `oracle/consultant-contract.json:88`; no reasoning difference can flip that answer. Compliance test only.
2. **S32** (`external-worker-transport-fidelity`): Transport-only execution report; verifier checks provenance-label presence. No semantic reasoning surface to separate.
3. **S33** (`external-reviewer-transport-fidelity`): Same shape as S32; provenance-label template. No semantic reasoning surface.
4. **S02** (`lead-recovery-packet`): The resume answer is stated in `inputs/task.md:20-25` essentially verbatim; unless the interruption-record is redesigned to present genuinely conflicting resume cues, the answer is dictated.
5. **S14** (`reliability-rollout-package`): Template compliance with rollout stages/triggers/recovery; substring verifier cannot see reliability-judgment variance. Borderline with "hopeless" once differential content is not enforced.

### Caveats

- I could not fully enumerate S07's oracle contract because I read only `oracle/admissible-seams.md` and inferred strictness from verifier keyword density (13 substring matches). I classified it medium separation with leak-removal as the intervention; if the oracle contract also pins an expected exact seam label the bucket could shift to high leak.
- S26 (`architecture-review-findings`) sits between medium and high leak because I did not fully read `inputs/known-risks.md`. My classification reflects task.md's decoy guidance (which is itself leakful in a mild sense) and the 14 substring-style matches in the verifier.
- For S08, S12, S13, S14, I reported leak levels based on the task.md abstract framing and the domain input filenames. I did not fully read every evidence input file; it is possible that one of the input files silently enumerates the answer at higher leak than I classified.
- The "structural-json-equality (functional)" bucket collapses several patch-style verifiers that run real validators (`pytest`, `node --test`, `node scripts/validate_*.py`) or compare against oracle JSON tables (`truth-table.json`, `encoding-oracle.json`). They are not identical in strictness; I grouped them because all of them reject substring-only compliance.
- No scenario in the set currently uses tuple-exact `(file, line, category)` finding equality at the review level. This is a structural gap in the current verifier pack, not a classification uncertainty.
- All separation-potential claims assume the near-ceiling model pair is bottlenecked by "compliance retelling easy + verifier keyword-coverage", consistent with the audit's stated premise (current 40/40 vs 39/40 split). If a future model were limited by functional test volume instead, the patch-style scenarios (S15-S24, N08-N10) would retain their separator value without intervention.

## Adjacent findings

None identified. The audit remained inside the Scenarios-v2 tree and did not uncover repository issues outside scope.

Gate: PASS
