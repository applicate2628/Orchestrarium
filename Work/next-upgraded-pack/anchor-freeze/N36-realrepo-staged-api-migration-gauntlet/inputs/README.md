# N36 Inputs

Read `task.md` first, then the phase prompt for the current worker invocation. The interface
contract is intentionally precise because the verifier checks hidden consumers, staged ledgers,
review-response decisions, and closeout reconstruction, not just visible tests.
