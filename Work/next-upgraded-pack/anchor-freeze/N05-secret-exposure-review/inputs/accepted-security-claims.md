# Accepted Security Claims

- export tokens stay out of URLs and other operator-visible traces
- telemetry omits raw bearer secrets and customer data
- the UI shows aggregate audience information rather than raw customer addresses
