# N58 Oracle

`optimizer-contract.json` defines the bundle shape, single-case dual-physics regression cases,
repeated-RHS MoM batch cases, Mie-series analytical oracle tolerances, runtime budgets, ledger
requirements, exact scope, and operator-output budget.
